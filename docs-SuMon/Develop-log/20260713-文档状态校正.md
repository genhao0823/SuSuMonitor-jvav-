# 开发日志: 文档状态校正

**日期**: 2026-07-13  
**操作人**: MiMoCode  
**状态**: 已完成

## 一、目标

修正项目文档中与当前实际文件状态不一致的描述，避免后续开发误判接口、OpenAPI、协议文档和数据库脚本的完成状态。

## 二、改动内容

### 根需求文档状态校正

修改文件：

- `项目需求与规范.md`

修正内容：

- 将 `/api/health`、`/api/ready`、认证接口、管理员审核接口的状态从“已实现”修正为“MVP-1 后续，当前源码未实现”。
- 将 `docs-SuMon/OpenApi-SuMon/` 描述为“目录已建立，当前尚无正式 OpenAPI JSON 文件”。
- 将 `docs-SuMon/Protocol-SuMon/` 描述为“目录已建立，当前尚无正式协议文档”。
- 将 `scripts/local-mysql-init.sql` 的职责修正为只初始化本机 MySQL 数据库、开发账号和授权，不创建业务表。
- 将 WebSocket 协议约束修正为后续协议文档建立后保持一致。
- 将信息来源中的早期 OpenAPI 和协议文件引用修正为历史规划资料说明。

### 项目规划文档状态校正

修改文件：

- `docs-SuMon/Develop-plans/20260712-SuSuMonitor项目规划.md`

修正内容：

- 将项目状态从“工程初始化前阶段”修正为“已完成 Java 后端工程骨架初始化”。
- 将 `server-java-SuMon/` 状态修正为“已初始化工程骨架，当前继续实现 MVP-1”。
- 将阶段 2 的任务状态更新为 Maven 工程、启动类、配置文件、logback、基础测试已完成，Flyway 迁移目录和迁移脚本尚未发现。
- 将下一步执行建议调整为直接进入横切基础能力、Flyway 表结构、认证/服务器/SSH 等 MVP-1 实现。

### 本机开发环境文档状态校正

修改文件：

- `docs-SuMon/本机开发环境配置.md`

修正内容：

- 将“Java 后端工程尚未初始化”修正为“Java 后端工程骨架已初始化”。
- 补充说明当前尚未实现 `/api/health`、`/api/ready` 等业务接口。
- 将验证命令描述调整为基础接口实现后的目标验证命令。

## 三、涉及文件

- `项目需求与规范.md`
- `docs-SuMon/Develop-plans/20260712-SuSuMonitor项目规划.md`
- `docs-SuMon/本机开发环境配置.md`
- `docs-SuMon/Develop-log/20260713-文档状态校正.md`

## 四、验证结果

- 已确认 `scripts/local-mysql-init.sql` 实际只建库、建本机用户和授权，不创建业务表。
- 当时确认 `docs-SuMon/OpenApi-SuMon/` 无正式 OpenAPI JSON 文件；后续已在后端横切基础能力阶段新增 `openapi-system.json`。
- 已确认 `docs-SuMon/Protocol-SuMon/` 当前无正式协议文档。
- 已确认 Java 后端当前仍为工程骨架阶段，尚未实现业务接口。

## 五、下一步

进入 MVP-1 横切基础能力实现：统一响应、错误码、全局异常、request_id、`GET /api/health` 和 `GET /api/ready`。
