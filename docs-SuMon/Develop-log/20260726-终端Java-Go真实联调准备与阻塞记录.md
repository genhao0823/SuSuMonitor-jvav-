# 终端 Java-Go 真实联调准备与阻塞记录

**日期**: 2026-07-26
**状态**: 已完成真实 Java-Go Linux PTY 联调

## 已实现的联调链路

1. 浏览器通过 `/ws/monitor` 发送 `terminal.open`，参数为 `server_id`、`cols`、`rows`。
2. Java 回查当前 `review_status=approved`，创建持久化元数据并生成 `session_id`。
3. Java 向已认证 `/ws/agent` 注入持久化 `server_id`、生成的 `session_id`，下发 `terminal.open`。
4. Go Agent 校验下行 `server_id` 与本地配置相同，使用本地固定 Shell 创建 PTY。
5. Agent 的 `opened/output/closed/error` 经过 Java 服务器和会话归属校验，仅回送创建会话的 Monitor WebSocket。
6. Monitor 或 Agent 断开时，Java 收口路由与元数据；Monitor 断开会尝试下发 `terminal.close`。

## 联调环境

- 已存在：`agent-go-SuMon/susumonitor-agent.exe`。
- 不存在：`agent-go-SuMon/.env`。
- 不存在：`server-java-SuMon/.env`。
- Java 服务运行在 Windows 独立短生命周期凭据注入会话，监听 `0.0.0.0:18081`。
- Go Agent 运行在既有 WSL2 Ubuntu，使用 Linux `/bin/bash` 创建 PTY。
- WSL 原生安装 Go 1.23.0 于 `/opt/go`，未替换系统 Go 1.22.2。
- WSL 到 Windows Java 服务端口可达。
- Java 使用隔离库 `susumonitor_metrics_validation`，未操作正式库 `susumonitor`。
- 管理员应用登录密码、MySQL 密码、JWT 和 Agent Token 均通过独立短生命周期交互会话注入，不写入仓库、环境文件、日志或文档。

## 真实联调结果

- 管理员应用登录成功。
- 创建唯一隔离测试服务器元数据并签发临时 Agent Token。
- WSL Go Agent 完成 `/ws/agent` 认证。
- Monitor WebSocket 依次验证 `terminal.open`、`terminal.input`、`terminal.output`、`terminal.resize` 和 `terminal.close`。
- PTY 输出包含预期测试标记；测试运行器未打印 PTY 输入或输出内容。
- 收到 `terminal.closed` 后完成会话关闭。
- 运行器退出时停止 WSL Agent 并撤销临时 Agent Token。
- Agent 安全运行日志仅记录启动、认证和正常退出；未记录密码、JWT、Agent Token 或 PTY 内容。
- 最终状态：`PTY_RELAY_INTEGRATION_OK`。

## 联调中修复的问题

- Agent 实际使用固定 Shell 路径 `/bin/bash`，但此前将路径直接发送到 `terminal.opened.shell`。
- Java 协议只允许不含路径分隔符的 Shell 标识，导致其正确返回 `terminal invalid payload`。
- 已改为 Agent 内部仍使用 `/bin/bash` 创建 PTY，同时仅上报协议安全标识 `bash`。
- 新增 `TestShellIdentifierDoesNotExposeLocalPath` 回归测试。

## 已完成的替代验证

- Java：`mvn test`，307 tests，0 failures，0 errors，0 skipped。
- Go Windows：`go test ./...`，全部包通过。
- Go WSL：`go test ./cmd/susumonitor-agent ./cmd/terminal-integration` 通过。
- 中继定向、服务器归属校验、会话归属、断连收口均有 Java 单元测试。

这些验证不替代真实 WebSocket 或真实 Linux PTY 联调。

## 已执行的隔离环境流程

在短生命周期 Linux 测试主机或容器中，通过环境变量安全注入以下值，不写入仓库文件或命令行：

- Java：`DB_NAME=susumonitor_metrics_validation`、`DB_USER`、`DB_PASSWORD`。
- Go Agent：`SUSUMONITOR_BACKEND_URL`、`SUSUMONITOR_SERVER_ID`、`SUSUMONITOR_AGENT_TOKEN`、`SUSUMONITOR_TERMINAL_ENABLED=true`、受控 `SUSUMONITOR_TERMINAL_SHELL`。

执行顺序：

1. 启动 Windows Java 隔离实例和 WSL Linux Agent。
2. 使用 approved 管理员签发 Monitor Ticket 和临时 Agent Token。
3. 建立 `/ws/agent` 与 `/ws/monitor`。
4. 验证 `open -> input/output -> resize -> close` 完整真实 PTY 往返。
5. 正常停止 Agent，撤销临时 Agent Token。

## 后续风险

- Java 中继目前是单 JVM 内存路由，不支持多实例故障转移。
- 终端帧尚未使用独立于既有 Agent Metrics 限流器的配额；真实压测前需确定消息速率、输出带宽和浏览器慢消费者阈值后再实现，避免任意引入未验证的默认参数。
- 本次真实联调未覆盖 Agent 非正常断连、Monitor 非正常断连、跨 Agent 冒充、非 approved 用户、会话超额与高频输出背压；这些仍由单元测试或后续专项真实测试覆盖。
