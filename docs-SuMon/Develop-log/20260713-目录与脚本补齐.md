# 开发日志: 目录与脚本补齐

**日期**: 2026-07-13  
**操作人**: OpenCode  
**状态**: 已完成

## 一、目标

补齐 SuSuMonitor Java 新项目进入后端工程初始化前所需的目录、脚本和本机开发说明。

## 二、改动内容

### 新增目录

- `scripts/`
- `docs-SuMon/OpenApi-SuMon/`
- `docs-SuMon/Protocol-SuMon/`
- `server-java-SuMon/`

说明：

- `OpenApi-SuMon/` 当前只创建目录，后续新增或修改 REST API 时再同步 OpenAPI JSON 文档。
- `Protocol-SuMon/` 当前只创建目录，后续实现 WebSocket、Agent、SSH 终端协议时再补充协议文档。
- `server-java-SuMon/` 当前只创建空目录，下一阶段再初始化 Maven + Spring Boot 3 + Java 21 工程。

### 新增脚本

- `scripts/local-mysql-init.sql`

脚本职责：

- 创建本机开发数据库 `susumonitor`。
- 创建本机开发用户 `susumonitor@localhost`。
- 创建本机开发用户 `susumonitor@127.0.0.1`。
- 授权用户访问 `susumonitor` 数据库。
- 刷新 MySQL 权限。

本机开发数据库密码：

```text
732682
```

说明：

- 该密码仅用于本机开发环境。
- 生产环境必须替换为安全密码。
- 业务表结构后续由 Flyway 管理，本脚本不创建业务表。

### 新增 API 调试模板

- `api-test/susumonitor.http`

包含 MVP-1 接口调试模板：

- `GET /api/health`
- `GET /api/ready`
- `POST /api/auth/register`
- `POST /api/auth/login`
- `POST /api/auth/logout`
- `GET /api/auth/me`
- `GET /api/admin/users/pending`
- `PUT /api/admin/users/{id}/approve`
- `PUT /api/admin/users/{id}/reject`
- `GET /api/servers`
- `POST /api/servers`
- `GET /api/servers/{id}`
- `PUT /api/servers/{id}`
- `DELETE /api/servers/{id}`
- `GET /api/servers/{id}/status`
- `POST /api/servers/{id}/ssh/test`

同时保留 MVP-3 指标接口和 MVP-6 告警接口的注释占位。

### 新增本机开发环境文档

- `docs-SuMon/本机开发环境配置.md`

记录内容：

- 当前 Windows 本机开发环境约定。
- MySQL 8.4、本机数据库、用户、密码、端口和字符集。
- Java 后端目标目录和端口。
- 后续环境变量规划。
- `.env` 和敏感信息安全要求。
- API 调试文件位置。

## 三、涉及文件

- `scripts/local-mysql-init.sql`
- `api-test/susumonitor.http`
- `docs-SuMon/本机开发环境配置.md`
- `docs-SuMon/Develop-log/20260713-目录与脚本补齐.md`
- `docs-SuMon/OpenApi-SuMon/`
- `docs-SuMon/Protocol-SuMon/`
- `server-java-SuMon/`

## 四、验证结果

- 缺失目录已补齐。
- 本机 MySQL 初始化脚本已创建。
- API 调试模板已创建。
- 本机开发环境配置文档已创建。
- 本阶段未初始化 Java 后端工程，未创建 `pom.xml` 或 Java 源码。

## 五、下一步

进入下一阶段：Java 后端工程初始化。

下一阶段目标：

- 初始化 `server-java-SuMon/` Maven 工程。
- 添加 Spring Boot 3 + Java 21 基础依赖。
- 添加启动类。
- 添加 `application.yml`、`application-local.yml`、`logback-spring.xml`。
- 添加 Flyway 迁移目录。
- 添加基础测试，确保 `mvn test` 可执行。

## 六、注意事项

- Java 后端代码必须遵循《阿里巴巴 Java 开发手册》。
- SQL 查询禁止使用 `SELECT *`。
- `.env`、密钥、证书、Token、SSH 私钥、数据库真实生产密码不得提交 Git。
- 本阶段完成后应按项目级 Git skill 要求进行一次本地 Git commit。
