# B1 后端独立 MySQL 验收

**执行日期**：2026-07-22
**执行批次**：B1
**验证层级**：独立 MySQL、Flyway、真实 Mapper SQL 和 Metrics 清理服务
**代码状态**：未修改业务代码；未修改测试配置

## 一、执行边界

本次只使用隔离数据库，不连接开发库 `susumonitor`，不停止现有 `18080` 服务，不使用 `18081`。

目标数据库：

- `susumonitor_metrics_cleanup_validation_20260721`
- `susumonitor_agent_ws_validation_20260721`

数据库密码通过当前 PowerShell 进程环境变量注入，未写入配置、命令参数、日志或 Git。日志不记录密码、JWT 或 Agent Token。

## 二、修改前备份

B1 未修改已有文件，因此没有新增业务文件备份。历史 Metrics 验收备份仍存在：

```text
C:\Backup\SuSuMonitor\metrics-cleanup-validation-20260721\susumonitor_metrics_cleanup_validation_20260721-before-flyway.sql
```

备份用途：隔离库历史验收恢复点。B1 未执行数据库删除、重建、清空或 Flyway 历史重置。

## 三、隔离数据库只读检查

执行了 MySQL 只读检查，确认两个数据库均存在，且均包含：

```text
alert_records
alert_rules
auth_bootstrap_state
commands
flyway_schema_history
metrics
servers
ssh_sessions
users
```

`susumonitor_metrics_cleanup_validation_20260721` 初始状态：

- Flyway 版本 V1-V9 全部 `success=1`。
- `metrics` 表为固定宽表。
- `idx_metrics_collected_at` 存在。
- `idx_metrics_server_time(server_id, collected_at)` 存在。
- 初始 Metrics 行数为 0。
- 初始 active server 行数为 0。
- 初始 deleted server 行数为 0。

`susumonitor_agent_ws_validation_20260721` 检查结果：

- Flyway 版本 V1-V9 全部 `success=1`。
- `servers`、`metrics` 表结构存在。
- `servers.deleted`、`servers.deleted_at` 和 Agent Token 生命周期字段存在。
- Metrics 索引存在。
- 检查时有 3 条 Metrics、5 条 active server、0 条 deleted server。

`SHOW CREATE TABLE metrics` 确认没有 `metric_name`、`metric_value` 或 `metric_unit` 动态指标列。

## 四、Metrics 专用测试

首次使用数据库凭据运行时，Spring 配置校验提示缺少 `JWT_SECRET` 和 `AES_GCM_KEY`。这不是数据库连接失败，也不是业务断言失败。随后使用测试资源中已有的非生产测试密钥通过环境变量补齐配置，未修改测试配置文件。

最终执行命令对应的有效环境变量为：

```text
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=susumonitor_metrics_cleanup_validation_20260721
DB_USER=root
DB_PASSWORD=<通过环境变量注入>
APP_ENV=metrics-validation
JWT_SECRET=<测试密钥，通过环境变量注入>
AES_GCM_KEY=<测试密钥，通过环境变量注入>
```

执行命令：

```powershell
mvn -Dtest=MetricsCleanupMySqlValidationTests test
```

结果：

```text
Tests run: 2, Failures: 0, Errors: 0, Skipped: 0
BUILD SUCCESS
```

Flyway 启动校验同时确认：

```text
Successfully validated 9 migrations
Current version of schema: 9
Schema is up to date. No migration necessary.
```

## 五、业务验收结果

### cutoff 边界

- `collected_at < cutoff`：删除 1 条。
- `collected_at = cutoff`：保留。
- `collected_at > cutoff`：保留。
- 最终目标测试数据保留 2 条。

### 分批和单轮上限

- 测试批次大小为 2。
- 单轮最大批次数为 2。
- 第一轮删除 4 条，执行 2 个批次。
- 第一轮后保留 1 条过期数据。
- 第二轮删除剩余 1 条。
- 最终目标测试数据为 0 条。

### 测试后数据库状态

测试结束后再次查询：

- `server_id=900001` 的 Metrics 行数：0。
- Metrics 总行数：0。
- active server 行数：0。
- deleted server 行数：0。
- Flyway V1-V9：全部 `success=1`。

本次数据库写入范围限定为测试类使用的固定服务器 ID `900001` 和其 Metrics 验证行；没有执行数据库对象删除、批量业务数据删除或迁移重置。

## 六、验证结论

B1 独立 MySQL/Flyway/Metrics 清理验收通过：

- 连接配置通过。
- Flyway V1-V9 通过。
- 固定宽表结构通过。
- 指标索引通过。
- cutoff 边界通过。
- 分批删除通过。
- 单轮批次上限通过。
- 下一轮继续清理通过。
- 测试后隔离数据状态符合预期。

未覆盖范围：

- 未验证多 JVM 分布式互斥。
- 未验证生产数据库。
- 未验证 Go Agent 真实上报链路。
- 未修改或验证四个遗留 Bug 的真实 HTTP 行为。

## 七、后续动作

进入 B2，优先修复 `M4-server-put-existence-check`。修复前先备份 Controller、Service、DTO 和相关测试文件；完成测试、真实 HTTP 验证、Bug 文档和开发日志同步后独立提交 Git。
