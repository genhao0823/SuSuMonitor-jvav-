# 项目状态与契约测试收口

**日期**：2026-07-23
**操作人**：opencode
**验证层级**：文档一致性、Controller MockMvc、OpenAPI 静态契约、Java/Go/Vue 常规自动化

## 改动摘要

- 统一总规划、根需求、后端阶段计划、Go Agent 计划、Metrics 闭环计划及三端 README 的当前状态。
- 将旧前端计划明确标记为历史基线，避免正文中的执行前状态被误读为当前状态。
- 将 MVP-1 调整为“核心实现完成、收口验收未全部完成”，明确首管理员空库并发、真实 SSH `50003`、部署和多实例仍未验证。
- 将 MVP-2、MVP-3、MVP-5A 与最新 Agent、Monitor、MySQL 和前端验收记录对齐。
- 明确 Dashboard 当前通过既有接口前端聚合，专用 `/api/dashboard/summary` 后置评估。
- Agent Token 日志安全收口属于并行 Agent 工作树改动，本次提交不纳入该预先已脏文件。
- 新增 Metrics 和 Agent Token Controller MockMvc 测试，覆盖权限、参数边界和响应契约。
- 将缺失查询参数和查询参数类型转换失败统一映射为 `40002`，避免 Metrics 时间参数错误落入通用 500。
- 增强 OpenAPI 校验，增加本地 `$ref`、唯一 `operationId`、`responses` 和 Java Controller 路径双向漂移检查。

## 涉及文件

- `项目需求与规范.md`
- `docs-SuMon/Develop-plans/20260712-SuSuMonitor项目规划.md`
- `docs-SuMon/Develop-plans/20260720-Web前端详细开发计划.md`
- `docs-SuMon/Develop-plans/20260721-Metrics-Agent-Web监控闭环详细开发计划.md`
- `docs-SuMon/Develop-plans/20260723-后端下一阶段详细开发计划.md`
- `web-vue-SuMon/README.md`
- `web-vue-SuMon/src/router/index.ts`
- `web-vue-SuMon/scripts/check-openapi.mjs`
- `web-vue-SuMon/scripts/README.md`
- `server-java-SuMon/src/test/java/com/susumonitor/server/module/metrics/controller/MetricsControllerTests.java`
- `server-java-SuMon/src/test/java/com/susumonitor/server/module/server/controller/AgentTokenControllerTests.java`
- `server-java-SuMon/src/main/java/com/susumonitor/server/common/GlobalExceptionHandler.java`

## 备份

修改前文件已备份到：

```text
C:\Backup\SuSuMonitor\review-remediation-20260723
```

初始备份验证时共有 16 个非空文件，共 289,110 字节；后续修改前另行备份了 `auth.spec.ts`。未删除项目文件，未执行数据库操作。

## 验证结果

定向 Controller 测试首次因全局 `@MapperScan` 在 Web 测试切片中注册 Mapper 而加载失败；补充 Mapper Mockito 替身后重新执行，11/11 通过。首次失败不作为功能回归结果。

| 验证对象 | 命令 | 实际结果 |
|---|---|---|
| Java Controller 定向测试 | `mvn -Dtest=MetricsControllerTests,AgentTokenControllerTests test` | 13/13 通过 |
| Java 全量常规测试 | `mvn test` | 211/211 通过，`BUILD SUCCESS` |
| Java 编译 | `mvn -DskipTests compile` | `BUILD SUCCESS` |
| Go 测试 | `go test ./...` | 全部 package 通过 |
| Go 静态检查 | `go vet ./...` | 退出码 0 |
| Go 构建 | `go build ./...` | 退出码 0 |
| Vue 单元测试 | `npm run test` | 28/28 通过 |
| Vue ESLint | `npm run lint` | 退出码 0 |
| Vue 构建 | `npm run build` | 构建通过；只有第三方 `@vueuse/core` 注释位置警告 |
| OpenAPI 契约 | `npm run openapi:check` | 3/3 文件、23 个端点通过，Controller 无路径漂移 |
| 前端静态审计 | `npm run audit:catchup` | 最终 0 ERROR / 0 WARN / 1 INFO；仅保留已拆分子组件的 Dashboard 525 行提示 |

普通单元测试、MockMvc、静态契约和构建结果不替代真实 MySQL、真实 HTTP、真实 WebSocket、真实 SSH 或部署验证。

## 未验证范围

- 首管理员独立空库真实并发。
- 当前受控 WSL 环境的真实 SSH `50003` 分类。
- 生产部署、多实例状态共享和跨实例广播。
- RabbitMQ 仍处于规划阶段，未实现或运行时验证。
