# 终端协议契约实现

**日期**: 2026-07-26
**状态**: 已实现并完成 Java 回归

## 目标

冻结 root PTY 终端的 Java WebSocket 外层模型、消息方向、载荷边界和稳定错误码，不接入现有运行时路由。

## 备份留痕

- 备份目录：`C:\Backup\SuSuMonitor\20260726-terminal-contract`
- 已备份并验证可读：`ErrorCode.java`、`websocket-protocol.md`、`20260724-公网Agent反向终端与云端部署计划.md`。
- 本模块未删除文件、未执行数据库变更、未修改隔离数据库。

## 改动

- 新增 `TerminalMessage`、`TerminalMessageType` 与 `TerminalProtocolValidator`。
- 固定浏览器、Java、Agent 三个方向允许的 `terminal.*` 类型。
- 校验 UUID、UTC ISO-8601、行列范围、Base64 和 16 KiB 解码载荷边界。
- 新增终端错误码 `40003/40302/40403/40903/40904/42903/42904`。
- 同步协议文档，明确全部 approved 用户均可请求 root 终端的高风险权限模型。
- 修正规划中的终端会话迁移编号为 V12，避免复用已执行的 V10/V11。

## 验证记录

- 已通过：`mvn test -Dtest="TerminalProtocolValidatorTests,ErrorCodeTests"`。
- 结果：9 tests，0 failures，0 errors，0 skipped。
- 已通过：`mvn test`。
- 结果：291 tests，0 failures，0 errors，0 skipped。
- 已通过：`git diff --check`。
- 未覆盖：本模块尚未接入运行时 WebSocket Handler，不包含 MySQL、真实 Agent、Go PTY 或公网验证；这些验证属于后续终端会话与路由模块。
