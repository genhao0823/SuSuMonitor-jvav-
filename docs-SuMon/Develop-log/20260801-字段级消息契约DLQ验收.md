# 开发日志：字段级消息契约 DLQ 验收

**日期**：2026-08-01
**操作人**：genhaosan

## 改动内容

### 新增

- `MetricsReportedMessageValidator`：在告警评估前校验 `metrics.reported.v1` 的 UUID、固定 producer、UTC 时间、正 server ID、百分比、非负指标和 `used <= total` 不变量。
- 单实例真实 RabbitMQ 验收覆盖字段契约越界消息直入 DLQ。

### 修改

- `AlertMessageConsumer` 将字段校验失败分类为 `AmqpRejectAndDontRequeueException`，保持零重试直接死信。
- `verify-mvp11.mjs` 的 C3 从两类扩展为三类：非法 JSON、schema 版本不符、字段契约非法；关闭 Monitor WebSocket，确保脚本自然退出。
- 消息契约与 RabbitMQ 拓扑文档同步字段级校验的实际边界。

## 备份留痕

- `C:\Backup\SuSuMonitor\20260801-single-instance-hardening\contract-validation-before-20260801\`
  - 已备份：Consumer、validator、两组测试、消息契约、RabbitMQ 运维文档。
  - `SHA256SUMS.txt` 已生成；7 个非空文件。
- `C:\Backup\SuSuMonitor\20260801-single-instance-hardening\contract-validation-script-before-20260801\`
  - 已备份：`api-test/verify-mvp11.mjs`。
  - `SHA256SUMS.txt` 已生成；2 个非空文件。
- `C:\Backup\SuSuMonitor\20260801-single-instance-hardening\contract-validation-topology-before-20260801\`
  - 已备份：`rabbitmq-topology-v1.md`。
  - `SHA256SUMS.txt` 已生成；2 个非空文件。

未删除任何文件；未写入或记录 RabbitMQ 密码。

## 验收结果

| 检查 | 结果 |
|---|---|
| 本机单实例服务 | PASS：`http://127.0.0.1:18081/api/health` 与 `/api/ready` 均 HTTP 200 |
| RabbitMQ Management API | PASS：本机验证账户可访问；首次误用 `guest` 向 `susumonitor` vhost 发布返回 401，已改用配置的本机验证账户，不是业务链路故障 |
| Maven 全量回归 | PASS：386 tests，0 failures/errors |
| Consumer/契约定向单测 | PASS：`AlertMessageConsumerTests,MetricsReportedMessageTests` |
| C1 正常消费 | PASS：exchange 路由、`alert.push`、1 条告警记录、队列归零 |
| C2 重复 event ID 幂等 | PASS：无第二次推送、告警记录数不变、队列归零 |
| C3 死信分类 | PASS：非法 JSON、`schema_version=2`、`cpu_percent=101` 三类消息均零重试进入 DLQ；本轮从 5 增至 8 条 |

本次验收的合法事件 ID：`4aff97c2-a4fa-4127-bc07-a380227fb3a4`。脚本输出提示该事件在 `message_consume_records` 应仅一行；当前 API 脚本尚未直接连接数据库自动断言，此项保留为人工 DB 对账补充。

## 当前进度

字段级契约校验与单实例 DLQ 验收完成，待通过精确 Git 暂存创建本地提交。完整 JSON Schema 引擎未实现；当前能力仅为无外部依赖的字段级运行校验。
