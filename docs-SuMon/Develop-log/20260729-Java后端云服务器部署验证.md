# 2026-07-29 Java 后端云服务器部署验证

## 范围

在用户授权下，将 `main` 分支最新后端部署到云服务器 `82.156.245.102`：

- 主机：OpenCloudOS 9.6 / Tencent Kona JDK 21.0.11-ga。
- 数据库：本机 MySQL 8.4，宝塔创建 `susumonitor` 数据库。
- 部署方式：systemd unit + 仅 loopback 监听 `127.0.0.1:18080`。
- 没有域名，仅 IPv4，不做 HTTPS/WSS 公网验收。

## 准备

1. 备份：

   ```text
   C:/Backup/SuSuMonitor/java-backend/main-20260729T130920Z-before-deploy-commit
   C:/Backup/SuSuMonitor/java-backend/main-20260729T131042Z-alert-validation-input
   ```

2. 构建产物：

   ```text
   target/server-java-SuMon-0.0.1-SNAPSHOT.jar
   sha256: 9c2bef9447d09247fae0bf9fd9e17c2fb98ad090e8b192f79b6c9898d3ccfeef
   ```

3. SSH key:

   ```text
   $env:USERPROFILE\.ssh\susumonitor_cloud_ed25519
   ```

4. 上传到目标主机：

   - `/opt/susumonitor/releases/cloud-20260729/server-java-SuMon-0.0.1-SNAPSHOT.jar`
   - `/etc/systemd/system/susumonitor-server.service`
   - `/opt/susumonitor/server/smoke-127.0.0.1.sh`

## 配置变更

1. 数据库用户密码策略临时降至 LOW，理由：宝塔创建 `susumonitor` 用户使用 `732682`，不满足 MySQL MEDIUM 长度 8、大小写、数字、特殊字符要求。已创建：

   ```sql
   CREATE USER 'susumonitor'@'localhost' IDENTIFIED BY '732682';
   CREATE USER 'susumonitor'@'127.0.0.1' IDENTIFIED BY '732682';
   CREATE USER 'susumonitor'@'%' IDENTIFIED BY '732682';
   GRANT ALL PRIVILEGES ON susumonitor.* TO 'susumonitor'@'localhost';
   GRANT ALL PRIVILEGES ON susumonitor.* TO 'susumonitor'@'127.0.0.1';
   GRANT ALL PRIVILEGES ON susumonitor.* TO 'susumonitor'@'%';
   CREATE DATABASE IF NOT EXISTS susumonitor DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
   ```

2. 生产 secret 注入到 `/etc/susumonitor/server.env`（权限 0600，root:root）。仅包含：

   - `APP_ENV=prod`
   - `DB_*` 基础连接
   - `JWT_SECRET` 标准 base64 编码
   - `AES_GCM_KEY` 标准 base64 编码
   - `AGENT_REGISTER_KEY`
   - `AGENT_TRUSTED_PROXY_CIDRS=127.0.0.1/32,::1/128`

   注意：JWT 密钥生成必须使用标准 base64（保留 `=` 填充），否则 Spring 启动时会报 `Illegal base64 character`。

3. systemd unit:

   - User=`susumonitor`
   - EnvironmentFile=`/etc/susumonitor/server.env`
   - `JAVA_TOOL_OPTIONS=-Xms256m -Xmx768m -Dfile.encoding=UTF-8`
   - 限制为 `/var/lib/susumonitor` 写权限，UMask=0077。

## 验证结果

### 服务启动

```text
$ systemctl status susumonitor-server --no-pager
● susumonitor-server.service - SuSuMonitor Java Backend
     Active: active (running) since Wed 2026-07-29 22:24:58 CST; 14s ago
   Main PID: 971792 (java)
        CPU: 22.574s
```

启动耗时约 10.5 秒；Flyway 成功执行 13 个 migration；Tomcat 监听 `127.0.0.1:18080`；HikariPool 已建立。

### Smoke

```text
$ bash /opt/susumonitor/server/smoke-127.0.0.1.sh
[smoke] verifying IPv4 loopback listener at http://127.0.0.1:18080
[smoke] GET /api/health
[smoke] GET /api/ready
[smoke] all smoke checks passed
```

`/api/health` 返回 `status=UP`，`/api/ready` 返回 `status=UP,database=ok`。

### 公网 IPv4 直连

- `curl http://82.156.245.102:18080/api/health` 没有响应：防火墙未放行 18080，符合 `DEPLOYMENT.md` 安全约束。
- `curl http://82.156.245.102/api/health` 返回 `404 nginx`：宝塔 Nginx 80 端口在监听，但未为后端添加 `location /api/` 反代；本阶段未修改 Nginx 配置。
- `curl http://82.156.245.102:888/api/health` 没有响应：该端口不是后端服务。

## 已知差距

1. **未验证 Nginx 反代**。当前云端宝塔 Nginx 默认配置没有 `/api/`、`/ws/agent`、`/ws/monitor` 反代片段；后续要按 `deploy/nginx-susumonitor.conf.example` 手工合并进 HTTPS `server` 块。
2. **未验证 WebSocket Upgrade**。`/ws/agent` 和 `/ws/monitor` 反代需要 Nginx `proxy_http_version 1.1` 和 `Upgrade/Connection upgrade` 头；当前不在本轮范围。
3. **未验证 HTTPS/WSS**。仅有 IPv4 无域名，不能做公网证书验证。
4. **未跑真实 Agent 或终端会话**。需要在 WSL 安装 Go Agent 跳 SSH 后连接 `ws://82.156.245.102/ws/agent`，本轮未做。
5. **MySQL 密码策略临时降低**。MEDIUM 策略允许短密码会带来安全风险；生产应保持 MEDIUM，并在 `server.env` 使用符合策略的密码。
6. **仅单个 deploy 单元**。`/opt/susumonitor/releases/cloud-20260729` 当前版本目录，没有保留回滚链。

## 单 JVM 限制

`TerminalSessionServiceImpl` 仅保证单 JVM 配额正确。云端当前为单实例 systemd unit，因此配额生效；如果后续横向扩展，需要重新评估。

## 后续步骤

- 在 WSL 中安装 Go Agent 跳 SSH，连接 `ws://82.156.245.102/ws/agent` 验证 Agent 首帧认证、心跳和断线重连。
- 按 `deploy/nginx-susumonitor.conf.example` 合并宝塔 Nginx HTTPS `server` 块，验证 `/api/`、`/ws/agent`、`/ws/monitor` 反代。
- 验证 HTTPS/WSS（需要域名 + 公网 CA 证书）。
- 在目标主机执行数据库备份恢复演练。
- 评估多实例部署前的分布式协调方案。
