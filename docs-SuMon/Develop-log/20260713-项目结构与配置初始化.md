# 开发日志: 项目结构与配置初始化

**日期**: 2026-07-13  
**操作人**: OpenCode  
**状态**: 已完成部分结构与配置初始化，后续继续补齐目录和脚本

## 一、目标

为 SuSuMonitor Java 新项目建立基础工程配置、Git 本机备份仓库、opencode skill 规范和后续目录补齐计划。

## 二、已完成内容

### Git 配置

- 在当前项目初始化 Git 仓库。
- 创建本机 bare Git 备份仓库：`D:\develop\Git\SuSuMonitor.git`。
- 添加 Git remote：`backup`。
- 当前默认分支为 `main`。
- 当前尚未执行首次 commit 和 push。

### 根配置文件

新增项目根配置文件：

- `.editorconfig`
- `.gitattributes`
- `.gitignore`
- `.dockerignore`

配置目标：

- 统一编码、缩进和换行规则。
- 忽略 `.env`、密钥、构建产物、本地依赖、`.codex/`。
- 保留 `.opencode/skills` 作为项目级 skill 配置。

### opencode skills

- 从 Go 项目参考迁移 `.opencode/skills`。
- 保留与 Go 语言无关的 skill：
  - `docker-standards`
  - `git-conventions`
  - `kotlin-coding`
  - `mysql-standards`
  - `vue3-coding`
- 保留并改造 `susumonitor-docs`：
  - 移除 Go 编码规范。
  - 改为 SuSuMonitor 文档规范 + Java/Spring Boot 后端规范。
  - 明确 Java 代码遵循《阿里巴巴 Java 开发手册》。

### 旧目录处理

- `server-susumonitor/` 不再作为正式后端目录。
- 删除前已备份到：`C:\Backup\SuSuMonitor-Jvav-server-susumonitor-20260713-0053`。
- 已删除原 `server-susumonitor/` 目录。
- 后续统一使用 `server-java-SuMon/`。

### 规划文档更新

已更新：

- `docs-SuMon/Develop-plans/20260712-SuSuMonitor项目规划.md`

新增或更新内容：

- Git 本机备份仓库路径。
- remote 名称 `backup`。
- `.opencode/skills` 保留策略。
- `.codex/` 不保留。
- Java 编码规范遵循《阿里巴巴 Java 开发手册》。
- 环境配置策略。
- AI 运维中枢与 Android 审批规划。

## 三、待完成内容

下一步补齐目录和脚本：

- `scripts/`
- `scripts/local-mysql-init.sql`
- `api-test/susumonitor.http`
- `docs-SuMon/OpenApi-SuMon/`
- `docs-SuMon/Protocol-SuMon/`
- `docs-SuMon/本机开发环境配置.md`
- `server-java-SuMon/`

## 四、本机开发数据库约定

当前本机 MySQL 开发配置：

- 数据库名：`susumonitor`
- 数据库用户：`susumonitor`
- 数据库密码：`732682`
- 数据库地址：`127.0.0.1:3306`
- 字符集：`utf8mb4`
- 排序规则：`utf8mb4_unicode_ci`

说明：

- 该密码仅用于本机开发环境。
- `.env` 不提交 Git。
- 生产环境必须替换为安全密码。

## 五、当前 Git 状态

当前仓库尚未创建首次提交。

已配置 remote：

```text
backup -> D:\develop\Git\SuSuMonitor.git
```

后续需要确认后再执行：

```text
git add .
git commit -m "chore(project): 初始化项目结构与配置"
git push -u backup main
```

## 六、风险与注意事项

- 当前运行中的 opencode 不会热加载新的 skill，若需完全生效，需要重启 opencode。
- `D:\develop\Git\SuSuMonitor.git` 是 bare 仓库，只用于备份，不直接编辑源码。
- 本机开发密码 `732682` 不可用于生产环境。
- 后续 Java 代码必须遵循《阿里巴巴 Java 开发手册》。
- 后续所有敏感信息不得提交 Git。
