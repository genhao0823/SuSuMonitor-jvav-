# Java 后端遗留 Bug 收口执行记录

**执行日期**：2026-07-23
**操作人**：opencode
**会话范围**：仅 Java 后端
**当前模块**：J0 Java 后端现有实现基线审计

## 一、执行范围

本轮只处理 `server-java-SuMon/` Java 后端基线，不处理 Go Agent、Vue 前端、Docker、部署脚本或其他并行修改。

## 二、修改前备份

本轮备份了完整 Java 后端目录：

```text
C:\Backup\SuSuMonitor\java-backend-closure-20260723\J0-java-backend-before-audit\server-java-SuMon\
```

备份验证结果：

| 项目 | 实际结果 |
|---|---|
| 文件数量 | 351 |
| 文件总大小 | 54,972,300 字节 |
| 备份目录 | 存在且可读取 |
| 删除操作 | 未执行 |
| 数据库操作 | 未执行 |

## 三、基线审计

审计并纳入版本控制的范围：

- Java 主源码及测试源码。
- MyBatis Mapper 接口和 XML。
- Flyway V7-V9 迁移脚本。
- Java 测试资源。
- 认证、管理员、Metrics、服务器、SSH、安全和调度模块。

未纳入本次提交：

- `server-java-SuMon/README.md`。
- `server-java-SuMon/.env.example`。
- Go、Vue、Apifox 和其他并行修改。

审计结果：

- 共确认 58 个未跟踪 Java 后端路径，其中 50 个 Java 文件、8 个资源文件。
- 未发现私钥、证书、JWT 明文或真实数据库密码。
- `application.yml` 中的数据库密码、JWT 密钥和 AES 密钥均通过环境变量占位符读取。
- 测试中的密码、Token 和密钥均为测试占位值或 Mockito 数据，不是运行时凭据。
- Mapper 未发现 `SELECT *`。
- 发现 Metrics 清理 Mapper 含业务所需的 `DELETE FROM metrics`，本轮未执行该 SQL。

## 四、验证结果

### 4.1 Java 编译

命令：

```powershell
mvn -DskipTests compile
```

结果：

```text
BUILD SUCCESS
```

### 4.2 Java 常规回归

命令：

```powershell
mvn test
```

结果：

```text
Tests run: 166, Failures: 0, Errors: 0, Skipped: 0
BUILD SUCCESS
```

该命令默认不发现 `MetricsCleanupMySqlValidationIT`，不连接真实 MySQL；不证明真实数据库 SQL、真实 HTTP、真实 SSH 或部署运行时已通过。

### 4.3 OpenAPI 钩子

J0 提交前触发 Husky OpenAPI 校验：

```text
openapi-auth.json    7 endpoints
openapi-server.json  14 endpoints
openapi-system.json  2 endpoints
3 / 3 通过
```

该校验只证明当前脚本支持的最低结构检查通过，不等同于完整 `$ref`、Schema 或运行时契约一致性验证。

## 五、Git 留痕

提交前执行：

- `git diff --cached --check`：通过。
- 暂存范围检查：仅 `server-java-SuMon/src/**`。
- 敏感信息模式复核：未发现私钥、JWT 结构或真实凭据；测试占位值已人工区分。
- 未暂存 README、`.env.example`、Go、Vue 和其他并行修改。

独立提交：

```text
260819d chore(server): 纳入Java后端现有实现基线
```

提交内容：

- 82 个 Java 后端源码、资源、迁移和测试文件。
- 6,471 行新增、44 行删除。
- 没有执行文件删除。

## 六、未验证范围

- 普通 `mvn test` 尚未通过 Maven 配置证明不会连接真实 MySQL；当前使用显式排除 Metrics 测试的命令完成回归。
- 排序和软删除尚未完成真实 Mapper SQL 与真实 HTTP 联合验收。
- SSH 连接、握手、认证阶段超时分类尚未修复和验证。
- 当前 README 和部分 Bug 文档状态仍需后续模块同步。
- 当前工作树仍有其他并行修改，本轮未清理或回滚。

## 七、下一模块计划：J1 真实 MySQL 测试隔离

目标：避免常规 Maven 测试意外连接或写入数据库，将真实 MySQL 验收改为显式执行。

计划步骤：

1. 备份将重命名或修改的测试、`pom.xml` 和 README 文件。
2. 将 Metrics 真实数据库测试调整为显式集成测试入口。
3. 增加数据库目标安全校验，拒绝空库名、开发库名和非本机目标。
4. 验证普通测试不连接真实 MySQL。
5. 使用明确隔离库配置执行真实 Metrics 测试。
6. 同步 Java 后端 README 和本执行日志。
7. 只暂存 J1 文件并独立提交。

预计技术栈：Java 21、Spring Boot Test、JUnit 5、Maven Surefire/Failsafe、MySQL 8.4、Flyway、PowerShell 环境变量注入。

预计提交：

```text
test(server): 隔离真实MySQL验收测试
```

## 八、J1 执行结果

### 8.1 修改和备份

J1 修改前备份：

```text
C:\Backup\SuSuMonitor\java-backend-closure-20260723\J1-mysql-isolation-before\
```

备份文件：`pom.xml`、Metrics MySQL 测试文件、Java 后端 README；3 个文件均存在且非空。

### 8.2 实现内容

- 将真实 Metrics 测试调整为 `MetricsCleanupMySqlValidationIT`，避免普通 Surefire 测试发现。
- 增加 `mysql-validation` Maven Profile，使用 Failsafe 执行 `*IT.java`。
- 增加 `RUN_MYSQL_VALIDATION_TESTS=true` 显式启用条件。
- 增加 Spring 上下文初始化前的数据库安全校验：只允许本机 host、包含 `validation` 的数据库名，拒绝开发库 `susumonitor`。
- README 同步普通测试和隔离 MySQL 测试命令。

### 8.3 验证结果

普通测试：

```powershell
mvn test
```

结果：`166/166` 通过，未发现或执行真实 MySQL IT。

安全闸门：

```powershell
$env:RUN_MYSQL_VALIDATION_TESTS = 'true'
$env:DB_HOST = '127.0.0.1'
$env:DB_NAME = 'susumonitor'
mvn -Pmysql-validation "-Dit.test=MetricsCleanupMySqlValidationIT" verify
```

结果：在 Spring DataSource 创建前因开发库名称被拒绝；未执行数据库连接和写入。该命令的非零结果是预期安全拒绝，不是 MySQL 业务验收通过。

真实隔离 MySQL 测试：本轮未执行。原因是没有通过安全运行时环境提供隔离数据库凭据，避免凭据落盘或误操作数据库。

### 8.4 J1 Git 留痕

J1 将在完成 `git diff --check`、暂存区敏感信息检查和提交钩子后独立提交。提交内容仅包含 Maven 测试隔离、Metrics IT、README 和本日志。

## 九、J2 服务器列表排序验证

### 9.1 修改前备份

J2 修改前备份：

```text
C:\Backup\SuSuMonitor\java-backend-closure-20260723\J2-server-sort-before\
```

已备份 7 个排序相关源码、测试和文档文件，均存在且非空；未删除文件，未操作数据库。

### 9.2 代码和测试实现

- Controller 增加默认排序、非法 `sort_order` 和六个白名单字段透传测试。
- Service 增加 null 默认值、非法排序方向和六字段双方向透传测试。
- 新增 `ServerMapperMySqlValidationIT`，使用 J1 的 Failsafe Profile 作为真实 SQL 验收入口。
- 集成测试准备三条有效服务器和一条软删除服务器，覆盖六字段升降序、排序后分页、关键词和软删除排除。
- 集成测试名称断言不依赖隔离库自增 ID，测试数据使用独立前缀并在事务中回滚。
- 未修改 Controller、Service、Mapper XML 生产实现，因为当前静态实现链路符合排序契约。

### 9.3 验证结果

定向测试：

```powershell
mvn "-Dtest=ServerControllerTests,ServerServiceTests" test
```

结果：`64/64` 通过。

编译：

```powershell
mvn -DskipTests compile
```

结果：`BUILD SUCCESS`。

常规回归：

```powershell
mvn test
```

结果：`188/188` 通过。

真实 MySQL Mapper：

```powershell
mvn -Pmysql-validation "-Dit.test=ServerMapperMySqlValidationIT" verify
```

本轮已在 `susumonitor_server_sort_validation_20260723` 执行并通过。曾使用 Surefire 直接运行 `*IT`，被 J1 安全闸门拒绝，这是错误入口验证，不作为业务测试结果。正确入口必须使用 `mysql-validation` Failsafe Profile。

真实 HTTP/Apifox：本轮已执行真实 HTTP 只读排序矩阵；Apifox CLI 排序用例未执行。

### 9.4 J2 下一步

1. 补充 Apifox CLI 排序用例，或明确保留为未执行边界。
2. 若真实 SQL 失败，先保存复现结果，再最小修改 Mapper/XML 并重新测试。
3. 进入 J3 服务器软删除验证。

## 十、J2 真实 MySQL 与 HTTP 验收补充

### 10.1 隔离数据库准备

经用户授权使用 MySQL root 运行时凭据创建隔离库：

```text
数据库：susumonitor_server_sort_validation_20260723
用户：susumonitor@127.0.0.1
权限：该隔离库 ALL PRIVILEGES
```

创建后只验证了隔离库名称和授权，未修改开发库 `susumonitor`。密码未写入文件、日志或 Git。

### 10.2 真实 MySQL Mapper 验收

命令：

```powershell
mvn -Pmysql-validation "-Dit.test=MetricsCleanupMySqlValidationIT,ServerMapperMySqlValidationIT" verify
```

实际结果：

| 测试 | 结果 |
|---|---|
| Flyway V1-V9 | 通过，隔离库迁移到 V9 |
| `MetricsCleanupMySqlValidationIT` | 2/2 通过 |
| `ServerMapperMySqlValidationIT` | 3/3 通过 |
| Failsafe | `BUILD SUCCESS` |

日志确认真实 MySQL 执行了各排序字段的 `ORDER BY ... LIMIT` SQL；列表和 count 均使用 `deleted = 0`，软删除测试行未进入结果，排序后分页返回两页 `2+1` 条且无重复。

### 10.3 真实 HTTP 前置验收

隔离 Java 服务曾使用最新 JAR 和隔离数据库启动在 `18081`，日志记录 Flyway 已是 V9、Tomcat 已监听 18081；由于当前工具环境会回收跨命令后台进程，未形成稳定跨命令服务实例，未把该次启动当作完整 HTTP 验收证据。

对现有 `18080` 实例执行只读健康检查：

```text
/api/health：HTTP 200
/api/ready：HTTP 200
```

随后使用运行时新管理员凭据只读登录：

```text
POST /api/auth/login
结果：HTTP 200，业务码 0，Token 仅内存使用，`X-Request-ID` 存在
```

随后只执行 `GET /api/servers` 排序请求，没有执行服务器创建、更新、删除，也没有重置开发库管理员密码。

| 场景 | HTTP | 业务码 | total | ID 顺序 | `X-Request-ID` |
|---|---:|---:|---:|---|---|
| `id asc` | 200 | 0 | 2 | `1,3` | 存在 |
| `id desc` | 200 | 0 | 2 | `3,1` | 存在 |
| `name asc` | 200 | 0 | 2 | `1,3` | 存在 |
| `name desc` | 200 | 0 | 2 | `3,1` | 存在 |
| `host asc` | 200 | 0 | 2 | `1,3` | 存在 |
| 非法 `sort_by` | 400 | `40002` | - | - | 存在 |
| 非法 `sort_order` | 400 | `40002` | - | - | 存在 |

当前有效服务器只有 2 条，name/host 缺少更多独立样本；六字段完整顺序由真实 MySQL IT 覆盖。Apifox CLI 排序用例未执行。

### 10.4 本轮边界

- 真实 MySQL 排序：已验证通过。
- 真实 HTTP 排序：已通过当前只读矩阵。
- Apifox CLI 排序：未执行。
- 开发库 `susumonitor`：未写入、未删除、未重置账号。
- 隔离库：保留，未删除；当前只有隔离管理员用户，无测试服务器。
- J2 当前标记为真实 MySQL 通过、真实 HTTP 通过、Apifox CLI 未执行。

本轮 HTTP 文档修改前备份：

```text
C:\Backup\SuSuMonitor\java-backend-closure-20260723\J2-http-validation-before\
```

## 十一、J3 服务器软删除验收

### 11.1 修改前备份和数据范围

J3 修改前备份：

```text
C:\Backup\SuSuMonitor\java-backend-closure-20260723\J3-server-soft-delete-before\
```

已备份并验证 14 个源码、测试和文档文件，共 172,248 字节。开发库写入前只读检查确认 J3 唯一名称和 host 均不存在；本轮只创建服务器 `j3-soft-delete-validation-20260723`，目标 ID 为 `37`。

软删除前生成并验证可读取的开发库备份：

```text
C:\Backup\SuSuMonitor\java-backend-closure-20260723\J3-susumonitor-before-soft-delete-20260723.sql
```

本轮未执行物理删除、TRUNCATE、DROP、数据库重建或 Flyway 重置。仅通过 `DELETE /api/servers/37` 软删除本轮新建记录，删除后保留该行作为验收证据。

### 11.2 自动化验证

新增 Controller/Service 覆盖删除不存在 ID、非法 ID 无副作用、错误映射；真实 Mapper 覆盖 `deleted`、`deleted_at`、`delete_token` 写入、active 条件幂等性，以及详情、凭据、状态、主机密钥、SSH 和 Agent Token 查询过滤。

| 验证层级 | 命令/范围 | 实际结果 |
|---|---|---|
| Java 编译 | `mvn -DskipTests compile` | `BUILD SUCCESS` |
| Controller/Service 定向测试 | `mvn "-Dtest=ServerControllerTests,ServerServiceTests" test` | `72/72` 通过 |
| 常规回归 | `mvn test` | `196/196` 通过 |
| 真实 MySQL Mapper | `mvn -Pmysql-validation "-Dit.test=ServerMapperMySqlValidationIT" verify` | `5/5` 通过，`BUILD SUCCESS` |

### 11.3 真实 HTTP 验收

使用运行时管理员凭据登录，Token 仅在内存中使用，未写入文档、日志或 Git。目标 ID `37` 的结果如下：

| 场景 | 实际结果 |
|---|---|
| DELETE | HTTP 200，业务码 0 |
| 列表 | `total=21`，目标 ID 不出现 |
| 详情 | HTTP 404，业务码 40400 |
| 状态 | HTTP 404，业务码 40400 |
| 更新 | HTTP 404，业务码 40400 |
| SSH test | HTTP 404，业务码 40400 |
| 数据库 | `active=21`、`deleted=12`；ID 37 行保留且 `deleted=1`、`deleted_at`、`delete_token` 非空 |

所有 HTTP 响应均存在 `X-Request-ID`。第一次 SSH 请求因缺少 JSON Content-Type 被请求解析层拒绝；补充正确的 `application/json` 空请求后得到预期 `40400`，不将第一次请求计入通过证据。

### 11.4 J3 边界

- 软删除生产实现未修改；本轮补充测试和真实验收证据。
- 开发库新增的 J3 测试记录 ID 37 已软删除并保留，未触碰其他服务器。
- `M4-server-list-soft-delete` 已完成真实 MySQL、真实 HTTP 和自动化验收。
- `M4-server-list-sort-ignored` 的 Apifox CLI 用例已在 J5 完成；真实 SSH `50003` 已在 J6 完成。

## 十三、J5 受控 SSH 与 Apifox 排序验收

### 13.1 备份与 WSL/OpenSSH 检查

J5 修改前备份：

```text
C:\Backup\SuSuMonitor\java-backend-closure-20260723\J5-ssh-runtime-and-apifox-before\
```

已备份 14 个文件，共 171,195 字节。另生成开发库软删除前备份：

```text
C:\Backup\SuSuMonitor\java-backend-closure-20260723\J5-susumonitor-before-ssh-apifox-20260723.sql
```

只读检查结果：Ubuntu `24.04.4 LTS`、WSL2；`openssh-server` 已安装，现有 `sshd` 监听 WSL `0.0.0.0:2222`，Windows `127.0.0.1:2222` TCP 可达。未安装新软件，未修改全局 SSH 配置。

创建专用用户：`susu-ssh-j5-20260723`。密码仅运行时设置，未写入文件、日志、Apifox 或 Git。

### 13.2 真实 SSH 认证失败验收边界

创建专用服务器 `ID=39`，SSH 目标为 `127.0.0.1:2222`，保存故意错误密码。Java 出站策略通过后，sshj 主机密钥握手返回 `50002`，未到达认证阶段；使用实际 ED25519 和 RSA 指纹重试仍返回 `50002`。因此没有真实 `50003` 通过证据，不修改生产错误映射代码。

测试服务器随后仅通过 `DELETE /api/servers/39` 软删除：HTTP 200、业务码 0；数据库行保留，`deleted=1`、`deleted_at` 非空、`delete_token` 非空。

### 13.3 Apifox 排序验收

创建独立 AI 分支：

```text
项目：8585366
分支：ai/20260723-from-main-server-sort-validation
分支 ID：8400084
```

从 `main` 显式导入 `GET /api/servers` 后创建 9 个排序测试用例：

```text
396855386, 396855390, 396855393, 396855394, 396855395,
396855397, 396855400, 396855403, 396855404
```

执行命令使用 Apifox CLI `test-case run --endpoint 489125512`，环境为本地开发服务 `47408671`，管理员 Token 仅通过运行时 `--env-var` 注入，未上传报告。

实际结果：

| 指标 | 结果 |
|---|---:|
| HTTP 请求 | 9 |
| 断言 | 30 |
| 失败 | 0 |
| CLI 退出码 | 0 |

覆盖 ID 升降序、name 升降序、host 升序、created_at 降序、非法 `sort_by`、非法 `sort_order` 和排序分页。

### 13.4 J5 边界

- Apifox 排序用例已创建并执行，但仍保留在独立 AI 分支，未合并 `main`。
- J5 时真实 SSH `50003` 未通过；该历史阻塞已在后续 J6 受控验收中解除。
- WSL 专用用户仍保留，测试服务器 ID 39 已软删除；未物理删除系统用户或数据库记录。

## 十二、J4 SSH test 认证错误码

### 12.1 契约决策

按确认后的正式契约执行：保留 `50400 ssh connection timeout`，仅新增 `50003 ssh authentication failed`，不引入文档中未实现的 `50700/50701`。

### 12.2 实现内容

- 新增 `SshConnectionException.Category.AUTHENTICATION_FAILED`。
- sshj `UserAuthException` 在通用 `IOException` 前单独捕获并映射为认证失败。
- `ErrorCode` 新增 `SSH_AUTHENTICATION_FAILED(50003, ..., HTTP 502)`。
- `ServerSshService` 和现有参数化测试同步认证失败映射。
- Controller 测试验证 `50003` 返回 HTTP 502，并保留 `X-Request-ID`。
- OpenAPI 错误码枚举和 SSH 502 examples 同步 `50003`。

### 12.3 验证边界

本轮自动化验证覆盖 Service 分类映射、Controller HTTP 映射和既有错误码回归；真实 SSH 认证失败需要受控 SSH 服务及专用凭据，本轮未执行，不将 Mock 结果表述为真实 SSH 验收。

## 十四、J6 真实 SSH `50003` 收口（2026-07-25）

### 14.1 目标和隔离边界

目标是完成 J5 未覆盖的真实错误密码路径：正确主机指纹通过后，确认 sshj `UserAuthException` 经业务层映射为 HTTP 502、业务码 `50003`。

- 独立临时 SSHD 只监听 `127.0.0.1:2223`，未修改或停止现有 `2222`。
- 临时 RSA 主机密钥和受控本机验证凭据只在运行时使用；没有写入仓库、Apifox 或开发日志。
- 测试服务器 `ID=50` 仅通过业务接口创建和软删除；未执行物理 DELETE、TRUNCATE、DROP 或数据库重置。

### 14.2 运行时结果

重新打包包含 SSH 诊断日志的 Spring Boot JAR 后，主机指纹登记返回 HTTP 200、业务码 0。随后对 `POST /api/servers/50/ssh/test` 发出真正无请求体、无 `Content-Type` 的请求，实际结果如下：

| 验收项 | 实际结果 |
|---|---|
| HTTP 状态 | 502 |
| 业务码 | 50003 |
| 响应消息 | `ssh authentication failed` |
| `X-Request-ID` | 存在且为 UUID |
| 响应敏感信息 | 未发现密码、JWT、私钥或底层异常 |
| 测试记录清理 | `DELETE /api/servers/50` 返回 200/0，数据库 `deleted=1` |

客户端诊断日志记录 `findExistingAlgorithms() -> []`、`ssh-rsa` 主机密钥验证成功、`rsa-sha2-512` 协商成功，随后记录 `UserAuthException`。SSHD 服务端日志记录 `userauth-request method password` 和 `Failed password`，确认失败发生在密码认证阶段。

### 14.3 备份与清理

诊断备份：

```text
C:\Backup\SuSuMonitor\java-backend-closure-20260723\ssh-50003-diagnostic-20260725\sshdiag50003.tar.gz
```

临时 `2223`、临时用户和临时文件已清理；`2222` 继续监听。该结果解除真实 SSH `50003` 未验证边界。

↳ 文档对齐性参考: [docs-SuMon/Bug-fix/2026-07-25-文档对齐性修复.md](2026-07-25-文档对齐性修复.md)

## 十五、J7 真实 SSH `50002` 收口（2026-07-25）

### 15.1 受控连接失败场景

为避免将主机身份或认证问题误判为连接失败，J7 先启动只监听 `127.0.0.1:2223` 的独立 SSHD，登记正确的 SHA-256 主机指纹后停止 SSHD，再调用 `POST /api/servers/{id}/ssh/test`。因此 SSH 客户端只能在已登记主机身份的同一目标上遇到 TCP 连接拒绝。

### 15.2 Apifox 结果

在独立 AI 分支 `ai/20260725-from-main-ssh-50003-validation` 创建用例 `397698534`：`J7 SSH connection refused returns 50002`。该用例不包含持久化 Token、密码、主机指纹或服务器 ID，只读取运行时变量。

| 断言 | 实际结果 |
|---|---|
| HTTP 状态 | 502 |
| 业务码 | 50002 |
| 响应消息 | `ssh connection failed` |
| `X-Request-ID` | 存在 |
| JSON/JUnit 断言 | 全部通过 |

该结果确认 `CONNECTION_FAILED -> SSH_CONNECTION_FAILED -> HTTP 502 / 50002`。运行时 JWT 已从报告中脱敏；临时服务器仅通过业务接口软删除，后端、SSHD 和临时密钥目录均已停止或清理。
