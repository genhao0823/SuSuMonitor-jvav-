# Java 后端遗留 Bug 收口执行记录

**执行日期**：2026-07-23
**操作人**：opencode
**会话范围**：仅 Java 后端
**当前模块**：J0 Java 后端现有实现基线审计

## 一、执行范围

本轮只处理 `server-java-SuMon/` Java 后端基线，不处理 Go Agent、Vue 前端、Docker、部署脚本或其他并行修改。

## 二、修改前备份

本轮备份了完整 Java 后端目录：

```text
C:\Backup\SuSuMonitor\java-backend-closure-20260723\J0-java-backend-before-audit\server-java-SuMon\
```

备份验证结果：

| 项目 | 实际结果 |
|---|---|
| 文件数量 | 351 |
| 文件总大小 | 54,972,300 字节 |
| 备份目录 | 存在且可读取 |
| 删除操作 | 未执行 |
| 数据库操作 | 未执行 |

## 三、基线审计

审计并纳入版本控制的范围：

- Java 主源码及测试源码。
- MyBatis Mapper 接口和 XML。
- Flyway V7-V9 迁移脚本。
- Java 测试资源。
- 认证、管理员、Metrics、服务器、SSH、安全和调度模块。

未纳入本次提交：

- `server-java-SuMon/README.md`。
- `server-java-SuMon/.env.example`。
- Go、Vue、Apifox 和其他并行修改。

审计结果：

- 共确认 58 个未跟踪 Java 后端路径，其中 50 个 Java 文件、8 个资源文件。
- 未发现私钥、证书、JWT 明文或真实数据库密码。
- `application.yml` 中的数据库密码、JWT 密钥和 AES 密钥均通过环境变量占位符读取。
- 测试中的密码、Token 和密钥均为测试占位值或 Mockito 数据，不是运行时凭据。
- Mapper 未发现 `SELECT *`。
- 发现 Metrics 清理 Mapper 含业务所需的 `DELETE FROM metrics`，本轮未执行该 SQL。

## 四、验证结果

### 4.1 Java 编译

命令：

```powershell
mvn -DskipTests compile
```

结果：

```text
BUILD SUCCESS
```

### 4.2 Java 常规回归

命令：

```powershell
mvn test
```

结果：

```text
Tests run: 166, Failures: 0, Errors: 0, Skipped: 0
BUILD SUCCESS
```

该命令默认不发现 `MetricsCleanupMySqlValidationIT`，不连接真实 MySQL；不证明真实数据库 SQL、真实 HTTP、真实 SSH 或部署运行时已通过。

### 4.3 OpenAPI 钩子

J0 提交前触发 Husky OpenAPI 校验：

```text
openapi-auth.json    7 endpoints
openapi-server.json  14 endpoints
openapi-system.json  2 endpoints
3 / 3 通过
```

该校验只证明当前脚本支持的最低结构检查通过，不等同于完整 `$ref`、Schema 或运行时契约一致性验证。

## 五、Git 留痕

提交前执行：

- `git diff --cached --check`：通过。
- 暂存范围检查：仅 `server-java-SuMon/src/**`。
- 敏感信息模式复核：未发现私钥、JWT 结构或真实凭据；测试占位值已人工区分。
- 未暂存 README、`.env.example`、Go、Vue 和其他并行修改。

独立提交：

```text
260819d chore(server): 纳入Java后端现有实现基线
```

提交内容：

- 82 个 Java 后端源码、资源、迁移和测试文件。
- 6,471 行新增、44 行删除。
- 没有执行文件删除。

## 六、未验证范围

- 普通 `mvn test` 尚未通过 Maven 配置证明不会连接真实 MySQL；当前使用显式排除 Metrics 测试的命令完成回归。
- 排序和软删除尚未完成真实 Mapper SQL 与真实 HTTP 联合验收。
- SSH 连接、握手、认证阶段超时分类尚未修复和验证。
- 当前 README 和部分 Bug 文档状态仍需后续模块同步。
- 当前工作树仍有其他并行修改，本轮未清理或回滚。

## 七、下一模块计划：J1 真实 MySQL 测试隔离

目标：避免常规 Maven 测试意外连接或写入数据库，将真实 MySQL 验收改为显式执行。

计划步骤：

1. 备份将重命名或修改的测试、`pom.xml` 和 README 文件。
2. 将 Metrics 真实数据库测试调整为显式集成测试入口。
3. 增加数据库目标安全校验，拒绝空库名、开发库名和非本机目标。
4. 验证普通测试不连接真实 MySQL。
5. 使用明确隔离库配置执行真实 Metrics 测试。
6. 同步 Java 后端 README 和本执行日志。
7. 只暂存 J1 文件并独立提交。

预计技术栈：Java 21、Spring Boot Test、JUnit 5、Maven Surefire/Failsafe、MySQL 8.4、Flyway、PowerShell 环境变量注入。

预计提交：

```text
test(server): 隔离真实MySQL验收测试
```

## 八、J1 执行结果

### 8.1 修改和备份

J1 修改前备份：

```text
C:\Backup\SuSuMonitor\java-backend-closure-20260723\J1-mysql-isolation-before\
```

备份文件：`pom.xml`、Metrics MySQL 测试文件、Java 后端 README；3 个文件均存在且非空。

### 8.2 实现内容

- 将真实 Metrics 测试调整为 `MetricsCleanupMySqlValidationIT`，避免普通 Surefire 测试发现。
- 增加 `mysql-validation` Maven Profile，使用 Failsafe 执行 `*IT.java`。
- 增加 `RUN_MYSQL_VALIDATION_TESTS=true` 显式启用条件。
- 增加 Spring 上下文初始化前的数据库安全校验：只允许本机 host、包含 `validation` 的数据库名，拒绝开发库 `susumonitor`。
- README 同步普通测试和隔离 MySQL 测试命令。

### 8.3 验证结果

普通测试：

```powershell
mvn test
```

结果：`166/166` 通过，未发现或执行真实 MySQL IT。

安全闸门：

```powershell
$env:RUN_MYSQL_VALIDATION_TESTS = 'true'
$env:DB_HOST = '127.0.0.1'
$env:DB_NAME = 'susumonitor'
mvn -Pmysql-validation "-Dit.test=MetricsCleanupMySqlValidationIT" verify
```

结果：在 Spring DataSource 创建前因开发库名称被拒绝；未执行数据库连接和写入。该命令的非零结果是预期安全拒绝，不是 MySQL 业务验收通过。

真实隔离 MySQL 测试：本轮未执行。原因是没有通过安全运行时环境提供隔离数据库凭据，避免凭据落盘或误操作数据库。

### 8.4 J1 Git 留痕

J1 将在完成 `git diff --check`、暂存区敏感信息检查和提交钩子后独立提交。提交内容仅包含 Maven 测试隔离、Metrics IT、README 和本日志。

## 九、J2 服务器列表排序验证

### 9.1 修改前备份

J2 修改前备份：

```text
C:\Backup\SuSuMonitor\java-backend-closure-20260723\J2-server-sort-before\
```

已备份 7 个排序相关源码、测试和文档文件，均存在且非空；未删除文件，未操作数据库。

### 9.2 代码和测试实现

- Controller 增加默认排序、非法 `sort_order` 和六个白名单字段透传测试。
- Service 增加 null 默认值、非法排序方向和六字段双方向透传测试。
- 新增 `ServerMapperMySqlValidationIT`，使用 J1 的 Failsafe Profile 作为真实 SQL 验收入口。
- 集成测试准备三条有效服务器和一条软删除服务器，覆盖六字段升降序、排序后分页、关键词和软删除排除。
- 集成测试名称断言不依赖隔离库自增 ID，测试数据使用独立前缀并在事务中回滚。
- 未修改 Controller、Service、Mapper XML 生产实现，因为当前静态实现链路符合排序契约。

### 9.3 验证结果

定向测试：

```powershell
mvn "-Dtest=ServerControllerTests,ServerServiceTests" test
```

结果：`64/64` 通过。

编译：

```powershell
mvn -DskipTests compile
```

结果：`BUILD SUCCESS`。

常规回归：

```powershell
mvn test
```

结果：`188/188` 通过。

真实 MySQL Mapper：

```powershell
mvn -Pmysql-validation "-Dit.test=ServerMapperMySqlValidationIT" verify
```

本轮未执行，原因是未提供隔离数据库运行时凭据；没有连接或写入数据库。曾使用 Surefire 直接运行 `*IT`，被 J1 安全闸门拒绝，这是错误入口验证，不作为业务测试结果。正确入口必须使用 `mysql-validation` Failsafe Profile。

真实 HTTP/Apifox：本轮未执行。当前证据仅覆盖 Controller、Service、编译和默认 Java 回归，不宣称真实 SQL 顺序或真实接口排序已通过。

### 9.4 J2 下一步

1. 在安全运行时注入隔离 MySQL 凭据后执行 `ServerMapperMySqlValidationIT`。
2. 真实 SQL 通过后，使用最新 Java 服务和隔离数据执行排序 HTTP/Apifox 矩阵。
3. 若真实 SQL 失败，先保存复现结果，再最小修改 Mapper/XML 并重新测试。
4. 真实排序验收完成后进入 J3 服务器软删除验证。
