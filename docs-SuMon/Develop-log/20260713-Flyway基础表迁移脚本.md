# 开发日志: Flyway 基础表迁移脚本

**日期**: 2026-07-13  
**操作人**: Codex  
**状态**: 已完成脚本补齐，真实数据库执行待账号确认

## 一、目标

确认当前本机数据库初始化状态，并补齐 MVP-1 所需的 Flyway 基础表迁移脚本。

## 二、数据库初始化确认

- 已确认本机 MySQL 服务 `MySQL84` 正在运行。
- 已确认 `server-java-SuMon/src/main/resources/db/migration/` 目录此前为空。
- 使用文档中的 `susumonitor/732682` 连接本机 MySQL 失败，返回访问拒绝。
- 使用 `root/732682` 连接本机 MySQL 失败，返回访问拒绝。
- 因缺少可用 MySQL 管理员凭据，本轮未执行 `scripts/local-mysql-init.sql`，也未真实执行 Flyway 建表。

## 三、改动内容

新增 Flyway 迁移脚本：

- `server-java-SuMon/src/main/resources/db/migration/V1__create_users_table.sql`
- `server-java-SuMon/src/main/resources/db/migration/V2__create_servers_table.sql`
- `server-java-SuMon/src/main/resources/db/migration/V3__create_metrics_table.sql`
- `server-java-SuMon/src/main/resources/db/migration/V4__create_commands_table.sql`
- `server-java-SuMon/src/main/resources/db/migration/V5__create_alert_tables.sql`
- `server-java-SuMon/src/main/resources/db/migration/V6__create_ssh_sessions_table.sql`

更新规划文档：

- `docs-SuMon/Develop-plans/20260712-SuSuMonitor项目规划.md`

## 四、验证结果

- 使用 Java 17 直接运行 `mvn test` 失败，原因是已有测试类由 Java 21 编译，Java 17 无法运行 class file version 65。
- 临时指定 Java 21 后执行 `mvn test` 通过。
- 当前测试排除了真实 DataSource 和 Flyway 自动配置，因此只能证明工程测试通过，不能证明本机 MySQL 已完成建表。

## 五、下一步

- 需要确认 MySQL 管理员账号或重新执行本机初始化脚本。
- 初始化账号可用后，再启动后端或运行 Flyway 验证真实建表。
- 随后进入认证与审核模块，实现 `users` 表的业务闭环。
