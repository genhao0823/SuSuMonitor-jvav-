# 开发日志: M5+ 用户审核页 (AdminUsersView)

**日期**: 2026-07-21
**操作人**: opencode
**关联计划**: `docs-SuMon/Develop-plans/20260719-Web前端并行开发计划.md`
**前置日志**: `20260720-Web前端M1dev运行时验证.md`

## 状态

M5 用户审核页交付完成。
路由 `/admin/users` 从 `NotFoundView` 占位替换为真实 `AdminUsersView.vue`,
复用既有 `src/api/admin.ts` 的 `listPendingUsers` / `approveUser` / `rejectUser` 三个函数,
类型对齐已有 `CurrentUser`(`src/types/api.d.ts`)。

## 本次落地的工程文件

### 新增文件

- `web-vue-SuMon/src/views/AdminUsersView.vue`
  - PageHeader "用户审核" 标题
  - 玻璃形态 el-card 包裹 el-table
  - 表格列: ID / 用户名 / 注册时间 / 操作
  - 行内操作: "通过" 按钮 + "拒绝" 按钮(el-popconfirm 二次确认)
  - 顶部"刷新"按钮(玫红渐变,与 Dashboard / ServerListView 风格一致)
  - 行级 loading 状态(`busyId` ref):防止同一用户并发审批
  - 错误码映射:`40900` / `40400` / `40300` / `40002`

### 修改文件

- `web-vue-SuMon/src/router/index.ts`
  - `/admin/users` 路由 `component` 从 `NotFoundView` 改为 `AdminUsersView`
  - 移除 `meta.placeholder` 和 `meta.placeholderHint`(已非占位)

### 不动文件

- `src/api/admin.ts` 已含全部所需函数,无新增
- `src/types/api.d.ts` `CurrentUser` 字段已对齐 OpenAPI,无新增
- `src/stores/auth.ts` 仅消费 `auth.isAdmin`,通过路由守卫控制
- `src/router/guards.ts` `requiresAdmin` 守卫已覆盖非 admin 场景

## 关键设计要点

- **行级并发保护**:用单值 `busyId` + `busyAction` 控制当前正在处理的行,
  其他行的按钮 `:disabled` 锁定,防止并发审批同用户。
- **二次确认策略**:
  - 通过 = 直接调 API(高频操作,不二次确认)
  - 拒绝 = el-popconfirm 二次确认(低频高代价,避免误点)
- **操作后立即 reload**:approve/reject 成功后调 `fetchPending()` 重拉,
  不在本地 splice(避免后端状态与本地状态不一致)。
- **错误处理**:`explainError` 函数把 `ApiBusinessError.code` 映射为用户可读 toast。
  常规 UI 流程不会触发 40900(详见下文"已知限制")。

## 验证清单

| 检查 | 命令 | 结果 |
|---|---|---|
| typecheck | `npm run typecheck` | 0 错误 |
| lint | `npm run lint` | 0 错误 0 警告 |
| dev server | HMR 自动应用 | `/admin/users` 200 |
| 浏览器手测 | 登录 admin → 侧栏"用户审核" | 见下 |

## 浏览器手测路径(已实测)

1. 登录 admin → 左侧栏点击"用户审核" → 进入 `/admin/users`
2. 页面加载 5 条 pending 用户(此前探测已知)
3. 点某行"通过" → 该行按钮 loading → toast "已通过 xxx" → 该行从列表消失
4. 点另一行"拒绝" → 弹 el-popconfirm 二次确认 → toast "已拒绝 xxx" → 该行消失
5. 操作某行时,其他行的"通过/拒绝"按钮均 disabled(行级并发保护)
6. 右上角"刷新"按钮 → 重新拉列表
7. 退出登录后访问 `/admin/users` → 跳 `/login?redirect=/admin/users`(路由守卫)
8. 以 user 身份登录 → 访问 `/admin/users` → 跳 `/forbidden`(admin 守卫)
9. admin 登录 → Dashboard → "管理员快速入口"卡 → "前往审核"按钮 → 跳到本页

## 已知限制(明确不做)

| 项 | 推迟到 |
|---|---|
| 批量审核(多选 + 批量 approve/reject) | M5+ 后续 |
| 用户搜索 / 筛选 / 分页 | 后端 `/api/admin/users/pending` 无分页参数,无搜索字段 |
| 已审核用户列表 / 历史审核记录 | 后端无对应接口 |
| 用户头像 | `CurrentUser` VO 无 avatar 字段 |
| 邮箱/角色/创建时间附加列 | `CurrentUser` 字段限制 |

## 重要: 40900 错误码的实际触发场景

`explainError` 中映射的 `40900 RESOURCE_CONFLICT → "该用户已被审核,请刷新列表查看最新状态"`,
**只在以下并发竞态场景触发**,**常规 UI 流程不会重现**:

| 场景 | 是否能复现 |
|---|---|
| 用户在 A 标签点"通过",网络往返中,后端被另一端 admin 审核 | ✅(竞态) |
| 后端人工 SQL 直接改了 user.reviewStatus | ✅ |
| **常规 UI 流程**(approve 后立即 reload → 该行消失 → 用户找不到行) | ❌ |

之前的浏览器手测路径里我曾错误地写"对已审核用户再点通过 → toast 40900",
**这条路径在 UI 上无法触发**(按钮物理消失),撤回。
**验证 40900 真实路径**需用 curl 直接对后端发请求,不依赖 UI 操作。

## 风险与对策

| 风险 | 处理 |
|---|---|
| 并发点击同一行 | `busyId` 单值互斥 + el-button loading |
| 后端状态变更导致 reload 前 UI 状态过期 | approve 后立即 reload,后端是真实状态源 |
| 误点"拒绝" | el-popconfirm 二次确认 |
| 单行 `busyId` 限制不同行并发 | 当前实现:其他行 disabled,只允许同时操作单行 |
| TypeScript 模板 row 默认 `DefaultRow` | 4 处显式 `as CurrentUser` 断言 |

## 下一步候选

- README 加版权声明(涂山苏苏仅内部 demo)
- README 加开发文档(npm scripts、目录、OpenAPI lint、pre-commit)
- C2 路由切换 loading bar
- C1 顶栏涂山头像
- 其他 MVP-5A 业务页