# 2026-07-27 MVP-6 告警规则 Mapper 修复

**日期**: 2026-07-27
**操作人**: OpenCode

## 改动内容

### 新增

- 新增 `AlertRuleMapperMybatisTests`，直接加载生产 `AlertRuleMapper.xml`，以 H2 执行 INSERT、UPDATE 和软删除 SQL。

### 修改

- 为 `AlertRuleMapper.insertRule` 增加 `@Param("rule")`，使 Java Mapper 的参数名与 XML 中的 `#{rule.xxx}` 及 `keyProperty="rule.id"` 一致。
- 更新 MVP-6 告警规则 500 问题单，修正 `keyProperty` 结论并记录代码修复与验证状态。

### 删除

- 无。

## 涉及文件

- `server-java-SuMon/src/main/java/com/susumonitor/server/module/alert/mapper/AlertRuleMapper.java`
- `server-java-SuMon/src/test/java/com/susumonitor/server/module/alert/mapper/AlertRuleMapperMybatisTests.java`
- `docs-SuMon/Bug-fix/2026-07-27-MVP6-alert-rules-mapper-500.md`
- `docs-SuMon/Develop-log/20260727-MVP6告警规则Mapper修复.md`

## 验证结果

- `mvn -q -Dtest=AlertRuleMapperMybatisTests test`：通过，3 个真实 Mapper 用例覆盖 INSERT 主键回填、UPDATE、软删除。
- `mvn -q test`：通过。
- `mvn -q -DskipTests package`：通过。
- `GET http://127.0.0.1:18080/api/health`：返回 HTTP 200，响应中 `status` 为 `UP`。

## 未验证范围

- 未使用真实 MySQL 数据库执行 Mapper 写操作。
- 未执行真实管理员身份的 POST、PUT、DELETE HTTP 联调；管理员密码仅从运行环境安全配置读取，未在开发过程中读取或输出。
