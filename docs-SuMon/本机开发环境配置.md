# 本机开发环境配置

**适用范围**: 当前 Windows 主机本机开发和调试  
**后端目录**: `server-java-SuMon/`  
**后端端口**: `18080`

## 一、当前环境约定

| 项 | 当前值 |
|----|--------|
| 操作系统 | Windows |
| 后端语言 | Java 21 LTS |
| 后端框架 | Spring Boot 3.x |
| 构建工具 | Maven |
| 数据库 | MySQL 8.4 |
| 数据库服务名 | `MySQL84` |
| 数据库地址 | `127.0.0.1:3306` |
| 数据库名 | `susumonitor` |
| 数据库用户 | `susumonitor` |
| 本机开发数据库密码 | `732682` |
| 字符集 | `utf8mb4` |
| 排序规则 | `utf8mb4_unicode_ci` |

## 二、MySQL 初始化

本机 MySQL 初始化脚本：

```text
scripts/local-mysql-init.sql
```

脚本职责：

- 创建数据库 `susumonitor`。
- 创建本机开发用户 `susumonitor@localhost`。
- 创建本机开发用户 `susumonitor@127.0.0.1`。
- 授权 `susumonitor` 用户访问 `susumonitor` 数据库。
- 刷新 MySQL 权限。

说明：

- 该脚本只用于本机开发。
- 业务表结构后续由 Java 后端 Flyway 迁移脚本管理。
- 生产环境不得使用本机开发密码 `732682`。

## 三、后端启动

Java 后端工程骨架已初始化，`/api/health` 和 `/api/ready` 已实现，后续启动和验证以后端实际工程为准。

启动目录：

```text
server-java-SuMon/
```

基础接口验证命令：

```bash
curl http://localhost:18080/api/health
curl http://localhost:18080/api/ready
```

`/api/ready` 应检查数据库连通性，数据库正常时返回 `database=ok` 或等价成功响应。

## 四、环境变量规划

后续 Java 后端应支持以下环境变量或本机配置项。

应用配置：

| 配置项 | 示例 | 说明 |
|--------|------|------|
| `APP_ENV` | `local` | 当前运行环境 |
| `APP_NAME` | `susumonitor` | 应用名称 |
| `SERVER_PORT` | `18080` | 后端 HTTP 端口 |

数据库配置：

| 配置项 | 示例 | 说明 |
|--------|------|------|
| `DB_HOST` | `127.0.0.1` | MySQL 地址 |
| `DB_PORT` | `3306` | MySQL 端口 |
| `DB_NAME` | `susumonitor` | 数据库名 |
| `DB_USER` | `susumonitor` | 数据库用户 |
| `DB_PASSWORD` | `732682` | 本机开发数据库密码 |
| `DB_CHARSET` | `utf8mb4` | 字符集 |
| `DB_TIMEZONE` | `Asia/Shanghai` | 数据库时区 |

认证和加密配置：

| 配置项 | 示例 | 说明 |
|--------|------|------|
| `JWT_SECRET` | `CHANGE_ME_LOCAL_JWT_SECRET` | JWT 签名密钥 |
| `JWT_EXPIRE_HOURS` | `24` | JWT 有效期，单位小时 |
| `AES_GCM_KEY` | `CHANGE_ME_LOCAL_AES_GCM_KEY` | SSH 凭据 AES-GCM 加密密钥 |
| `AGENT_REGISTER_KEY` | `CHANGE_ME_LOCAL_AGENT_REGISTER_KEY` | Agent 首次注册密钥 |

SSH 和调度配置：

| 配置项 | 示例 | 说明 |
|--------|------|------|
| `SSH_CONNECT_TIMEOUT_SECONDS` | `10` | SSH 连接超时 |
| `SSH_IDLE_TIMEOUT_MINUTES` | `20` | SSH 会话无输入超时 |
| `METRICS_RETENTION_DAYS` | `10` | 指标保留天数 |
| `METRICS_CLEANUP_CRON` | `0 0 3 * * ?` | 指标清理定时任务 |

## 五、安全要求

- `.env` 不提交 Git。
- `.env.*` 不提交 Git，`.env.example` 除外。
- `JWT_SECRET`、`AES_GCM_KEY`、`AGENT_REGISTER_KEY` 不允许硬编码。
- 日志不得输出数据库密码、JWT、Token、SSH 密码、SSH 私钥、AES 密钥、Agent 注册密钥。
- 本机开发密码 `732682` 不得用于生产环境。
- Java 代码必须遵循《阿里巴巴 Java 开发手册》。

## 六、API 调试文件

API 调试模板：

```text
api-test/susumonitor.http
```

后续每实现一个 REST API，需要同步更新该调试文件和 OpenAPI 文档。

## 七、时间与时区约定（2026-07-31 补充）

**总原则：内部 UTC、展示层本地化（东八区）**。用户（前端）看到的页面时间即为北京时间。

| 层 | 口径 | 约定 |
|---|---|---|
| 应用时钟 | UTC | `ClockConfig` 返回 `Clock.systemUTC()`；所有 `LocalDateTime.now(clock)` 与业务时间字段（`collected_at`/`triggered_at`/`next_attempt_at`/`last_heartbeat_at` 等）一律 UTC |
| API 输出 | UTC ISO-8601（带 Z） | VO 的 `OffsetDateTime`；WS 帧 `timestamp`、消息契约 `occurred_at`/`collected_at` 同口径 |
| 前端展示 | 东八区（浏览器本地化） | `formatDateTime` 使用 `new Date(iso)` + 本地时间方法，页面上显示北京时间；前端不做时区转换代码 |
| 数据库默认时间戳 | MySQL 会话时区（本机为东八区） | `created_at`/`updated_at` 的 `DEFAULT CURRENT_TIMESTAMP` 为东八区——**已知混合现状**，仅作审计/排序展示，不参与业务时间比较 |
| SQL 时间函数比较 | UTC 口径 | 应用写入为 UTC，凡与 SQL 时间函数比较必须用 `UTC_TIMESTAMP()`（**禁止 `NOW()`**——会话时区东八区会造成 8 小时偏差，参见 outbox 退避失效修复 `519211d`） |

注意事项：

1. 新增时间字段必须使用应用时钟（`Clock.systemUTC()`）写入 UTC 值，不得依赖数据库默认时间戳参与业务逻辑。
2. 新增 SQL 若需与时间列比较，统一 `UTC_TIMESTAMP()`。
3. 若未来希望数据库默认时间戳也统一 UTC，需在迁移中显式 `DEFAULT (UTC_TIMESTAMP())` 或调整 MySQL 服务器 `time_zone`，并评估存量数据。
