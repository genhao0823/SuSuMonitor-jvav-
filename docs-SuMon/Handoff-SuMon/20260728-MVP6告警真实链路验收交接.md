# MVP-6 告警真实链路验收与 Bug 修复交接

**交接日期**：2026-07-28
**状态**：MVP-6 告警 HTTP/WebSocket 端到端链路真实运行时验收通过；发现并修复 3 个真实后端 Bug
**适用范围**：Java 后端告警模块（alert）Mapper、状态机和评估器修复；隔离验证库真实 MySQL + HTTP + WebSocket 验收

## 一、目的和边界

本交接记录 MVP-6 告警闭环从"代码已实现但真实链路未验证"到"真实运行时验收通过"的完整过程，包括发现并修复的 3 个真实后端 Bug。

- 所有真实运行时验证在隔离验证库 `susumonitor_metrics_validation` 上执行，不触碰开发库或生产库。
- 验证前已完成 `alert_rules` 表备份（`C:\Backup\SuSuMonitor\mvp6-alert-validation-before-v13-20260728.sql`），非空可读。
- 后端服务以临时配置在 `127.0.0.1:18081` 启动，连接隔离验证库。
- 验证结束后已停止服务并清理验证库中的测试数据。

## 二、发现并修复的 3 个真实后端 Bug

### 2.1 AlertRecordMapper.insertRecord 缺少 @Param("record")

**文件**：`server-java-SuMon/src/main/java/com/susumonitor/server/module/alert/mapper/AlertRecordMapper.java`

**根因**：`insertRecord(AlertRecordEntity record)` 方法签名未声明 `@Param("record")`，但 XML 使用 `#{record.ruleId}` 前缀引用。与已修复的 `AlertRuleMapper` 500 Bug 完全同类。真实 MySQL 执行时抛出 `ReflectionException: There is no getter for property named 'record'`。

**修复**：方法签名改为 `insertRecord(@Param("record") AlertRecordEntity record)`。

**影响**：告警评估能正确触发（规则匹配、状态查询），但记录插入失败导致评估事务回滚，`alert.push` 永远不发送。单元测试和 H2 Mapper 测试未能发现，因为 H2 MySQL mode 下 MyBatis 参数绑定行为与真实 MySQL 不同。

### 2.2 AlertStateMapper.insertState 缺少 @Param("state")

**文件**：`server-java-SuMon/src/main/java/com/susumonitor/server/module/alert/mapper/AlertStateMapper.java`

**根因**：与 2.1 完全同类的 Bug。`insertState(AlertStateEntity state)` 未声明 `@Param("state")`，XML 使用 `#{state.ruleId}` 前缀。修复 2.1 后，此 Bug 会在首次告警状态插入时暴露。

**修复**：方法签名改为 `insertState(@Param("state") AlertStateEntity state)`。

### 2.3 AlertStateMachine 恢复后再次越界不产生新告警

**文件**：
- `server-java-SuMon/src/main/java/com/susumonitor/server/module/alert/service/AlertStateMachine.java`
- `server-java-SuMon/src/main/java/com/susumonitor/server/module/alert/mapper/AlertStateMapper.java`
- `server-java-SuMon/src/main/resources/mapper/alert/AlertStateMapper.xml`
- `server-java-SuMon/src/main/java/com/susumonitor/server/module/alert/service/AlertEvaluationServiceImpl.java`

**根因**：`AlertStateMachine` 注释设计意图为"RESOLVED → IDLE 恢复后重置（state 被清除后下次评估为 null）"，但 `handleResolve` 只将 state 标记为 `active=0`（inactive），未删除 state 行。恢复后 `selectByRuleAndServer` 返回非 null 的 inactive state，`evaluate` 落入第 59 行 `NoAction` 分支，不产生新告警。

**修复**：
- `AlertStateMapper` 用 `deleteState(id, version)` 替代 `updateStateResolved`，恢复时物理删除 state 行。
- `AlertEvaluationServiceImpl.handleResolve` 改为调用 `deleteState`，使下次评估 `currentState == null`，首次越界逻辑正常工作。
- `AlertStateMachine` 注释更新为"恢复后删除 state 行，下次评估回到 IDLE"。
- 单元测试 `AlertEvaluationServiceTests.recoveryShouldMarkResolved` 的 mock 和 verify 从 `updateStateResolved` 改为 `deleteState`。

**影响**：规划要求"恢复后转为 resolved，再次越界生成新告警"，但实际恢复后再次越界被静默忽略，不产生新告警记录和推送。

## 三、新增验收资产

| 资产 | 路径 | 用途 |
| --- | --- | --- |
| MySQL 集成测试 | `server-java-SuMon/src/test/java/com/susumonitor/server/module/alert/mapper/AlertRuleMySqlValidationIT.java` | 验证 Flyway V13、生成列、唯一索引、NULL 范围、软删除后重建。仅在 `RUN_MYSQL_VALIDATION_TESTS=true` 且隔离库时执行。 |
| 告警 WebSocket E2E 脚本 | `api-test/verify-alert-ws.mjs` | 覆盖重复规则 409、首次/持续/恢复/再次越界、幂等、取消订阅。24 项检查。 |
| npm 入口 | `api-test/package.json` | `verify:alert-ws` |
| HTTP 调试用例 | `api-test/susumonitor.http` | 重复创建和更新冲突的手工排障请求 |
| 运行时验收记录 | `docs-SuMon/Develop-log/20260728-MVP6告警运行时验收资产.md` | 前置条件、命令、已验证与未验证边界 |

## 四、验证结果

### 4.1 单元测试

```text
mvnw.cmd test
Tests run: 332, Failures: 0, Errors: 0, Skipped: 0
BUILD SUCCESS
```

### 4.2 MySQL 集成测试

```text
环境变量：RUN_MYSQL_VALIDATION_TESTS=true, DB_HOST=127.0.0.1, DB_NAME=susumonitor_metrics_validation
mvnw.cmd -Pmysql-validation verify
Tests run: 12, Failures: 0, Errors: 0, Skipped: 0
BUILD SUCCESS

包含：
  AlertRuleMySqlValidationIT          1 test  PASS  （V13 唯一约束、生成列、软删除重建）
  MetricsCleanupMySqlValidationIT    2 tests PASS
  MetricsIngestionMySqlValidationIT   4 tests PASS
  ServerMapperMySqlValidationIT       5 tests PASS
```

### 4.3 真实 HTTP/WebSocket 端到端

```text
后端：127.0.0.1:18081，连接 susumonitor_metrics_validation
npm --prefix api-test run verify:alert-ws
{"status":"PASS","server_id":197,"rule_id":13,"checks":24,"token_values_logged":false}

24 项检查覆盖：
  - 管理员登录/用户注册/审批
  - 服务器和 Agent Token 创建
  - 告警规则创建
  - 重复规则返回 HTTP 409 + 业务码 40900
  - Monitor 订阅和 Agent 认证
  - 首次越界：单次 metrics.update + 单次 alert.push
  - 持续越界：metrics.update 但无第二条 alert.push
  - 恢复：record 状态转为 resolved
  - 恢复后再次越界：新 record + 新 alert.push（Bug 2.3 修复后通过）
  - 重复 message_id 幂等
  - 取消订阅后不推送
```

### 4.4 构建与契约

```text
mvnw.cmd -DskipTests package   -> BUILD SUCCESS
npm run openapi:check           -> 5/5 文件、29 端点通过
```

## 五、修改文件清单

### Java 主代码
| 文件 | 改动 |
| --- | --- |
| `module/alert/mapper/AlertRecordMapper.java` | `insertRecord` 加 `@Param("record")` |
| `module/alert/mapper/AlertStateMapper.java` | `insertState` 加 `@Param("state")`；`updateStateResolved` 替换为 `deleteState` |
| `module/alert/service/AlertEvaluationServiceImpl.java` | `handleResolve` 改为调用 `deleteState` |
| `module/alert/service/AlertStateMachine.java` | 注释更新，反映恢复后删除 state 的设计 |
| `websocket/AlertPushPublisher.java` | 无功能改动（临时诊断 log 已移除） |

### XML Mapper
| 文件 | 改动 |
| --- | --- |
| `mapper/alert/AlertStateMapper.xml` | `updateStateResolved` 替换为 `deleteState`（DELETE WHERE id=? AND version=?） |

### 测试
| 文件 | 改动 |
| --- | --- |
| `module/alert/mapper/AlertRuleMySqlValidationIT.java` | 新增：V13 真实 MySQL 验证 |
| `module/alert/service/AlertEvaluationServiceTests.java` | `recoveryShouldMarkResolved` mock/verify 改为 `deleteState` |

### 验收脚本
| 文件 | 改动 |
| --- | --- |
| `api-test/verify-alert-ws.mjs` | 新增：告警 E2E 脚本 |
| `api-test/package.json` | 新增 `verify:alert-ws` 入口 |
| `api-test/susumonitor.http` | 新增重复创建/更新冲突用例 |

### 文档
| 文件 | 改动 |
| --- | --- |
| `docs-SuMon/Develop-log/20260728-MVP6告警运行时验收资产.md` | 新增：验收资产与边界记录 |

## 六、数据影响

- 隔离验证库 `susumonitor_metrics_validation` 在验收前备份了 `alert_rules` 表。
- Flyway V13 在验证库从 V12 成功迁移至 V13（`success=1`）。
- 验收结束后清理了所有测试创建的数据（临时用户、服务器、规则、指标、告警记录和状态）。
- 未修改开发库 `susumonitor` 或任何生产数据库。

## 七、未验证范围

- 浏览器 `AlertRuleDialog.vue` 的 `40900` 中文冲突 toast 未在真实浏览器中实际观察（E2E 脚本验证了 HTTP 409/40900 响应，前端映射已在 `2026-07-28-MVP6-alert-rules-no-uniqueness-check.md` 中确认存在）。
- 真实 MySQL 上的并发相同规则 POST 未在隔离环境中专门复验（V13 唯一索引在集成测试中验证了重复写入拒绝）。
- 告警评估的 `@TransactionalEventListener(AFTER_COMMIT)` 在进程崩溃场景下的事件丢失未覆盖（属于 MVP-9/MVP-10 的 Outbox 可靠投递范围）。

## 八、后续建议

1. 提交本轮修复：3 个 Mapper/状态机 Bug 修复 + 验收资产。建议按 Bug 分开提交或合并为一个 MVP-6 收口提交。
2. 浏览器冲突 toast 可在前端联调时顺带确认，不阻塞后端收口。
3. 下一阶段后端开发为 MVP-9：模块数据所有权、`metrics.reported.v1`/`alert.triggered.v1` 契约、RabbitMQ 拓扑和性能基线，不混入本轮。
