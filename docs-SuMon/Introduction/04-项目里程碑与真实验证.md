# 04-项目里程碑与真实验证

> **适用场景**：面试官质疑"这只是 demo 没有真实验证" → 翻这一份。本文件按时间线展示交付物、真实集成方式、证据链。
>
> **配套阅读**：单点细节索引到对应 `Develop-log` / `Bug-fix` / `Difficulty-log` 文档。
> **状态标签**：与根 `README.md` 一致。

---

## 一、关键数据快照（开场可念）

| 指标 | 数值 | 来源 |
|---|---|---|
| Git commits | 65+ | `git log --oneline \| wc -l` |
| 文档 | 67+ 开发日志、9 篇 bug、5 篇困难、6 份 OpenAPI、6 份技术总结 | `docs-SuMon/` 全量 |
| 后端业务类 | 80+ Java 文件（service / handler / interceptor） | `git ls-files server-java-SuMon/src/main/java` |
| 前端视图 | 13 个 `.vue`、21 个组件 | `web-vue-SuMon/src/{views,components}` |
| Agent 子包 | 5 个 (`collector/reporter/wsclient/terminal/config`) | `agent-go-SuMon/internal/` |
| Flyway 迁移 | V1-V13 共 13 个 | `server-java-SuMon/src/main/resources/db/migration/` |
| WebSocket 消息类型 | Agent 7 类（auth/heartbeat/metrics/terminal×4）+ Monitor 3 类（metrics/alert/terminal）+ 错误帧 | `websocket/AgentMessageType.java` + `TerminalMessageType.java` |
| 测试 | 320+ 单元测试 + 4 道防线（Vitest / audit:catchup / api:e2e / ui:e2e） | `README.md` §4 道防线 |
| OpenAPI 端点 | 23+ endpoint | `docs-SuMon/OpenApi-SuMon/*.json` |
| 错误码 | 25 个枚举值 | `common/ErrorCode.java` |
| 真实验证资源 | Apifox 27 断言 / 隔离 MySQL 库 / WSL SSHD `127.0.0.1:2223` / 浏览器 IAB 联调 | `Develop-log/2026-07-2[3-9]-*` |

---

## 二、时间线维度：8 个里程碑

### 阶段 1：MVP-1 后端基线（2026-06-22 ~ 2026-07-12）

**目标**：Spring Boot 工程骨架 + 统一响应 + Flyway + 用户注册/JWT/审核 + 服务器 CRUD + SSH。

| 交付 | 关键 commit / dev-log |
|---|---|
| Spring Boot 3.4.7 工程初始化 | `0` |
| Flyway V1-V7：users + servers + metrics + commands + alert + ssh_sessions + auth_bootstrap_state | `V1`...`V7` |
| 统一响应 `ApiResponse<T>` + 25 个 ErrorCode + `GlobalExceptionHandler` + `X-Request-ID` | `2026062*-后端基线*.md` |
| BCrypt 注册 + 行锁首管理员 + 72h JWT + Spring Security 无状态链 | `2026062*-BCrypt-JWT-注册流程*.md` |
| 服务器 CRUD + AES-GCM 凭据 + sshj 真实测试（50002/50003） | `2026062*-SSH真实化*.md` |
| Agent Token register/rotate/revoke + Agent WS 鉴权心跳 | `20260721-Agent-Token-REST真实验收.md` |

**真实验证**：
- Apifox 9 用例 27 断言全过（含 50002 连接失败 / 50003 认证失败）；
- WSL Ubuntu 起独立 SSHD `127.0.0.1:2223` 受控环境验 SSH 错误码（J6/J7 dev-log）；
- 隔离 MySQL `susumonitor_validation` 库跑通 Flyway V1-V9。

### 阶段 2：MVP-2 Go Agent（2026-07-12 ~ 2026-07-21）

**目标**：Go coder/websocket 客户端 + gopsutil 采集 + 重连。

| 交付 | 关键 dev-log |
|---|---|
| Go 模块初始化（`go mod init agent-go-SuMon`） + 三方依赖（coder/websocket + gopsutil/v3） | `20260721-Agent-Go-*.md` |
| `coder/websocket v1.8.15` 客户端 | `20260721-Agent-WebSocket客户端实现.md` |
| 状态机 `Disconnected→Connecting→Authenticating→Connected→Reconnecting` | 同上 |
| 指数退避 1→2→4→8s | 同上 |
| `internal/collector/gopsutil_collector.go` CPU/内存/磁盘/网络/温度/负载 | `20260721-Agent-采集器实现*.md` |
| `internal/reporter/reporter.go` `metrics.report` 帧带 `message_id` UUID | 同上 |
| `Makefile build / build-linux / test-race / vet / lint` | `Makefile:7` 加 `build-linux` target |

**真实验证**：
- Go Agent 本机运行时：5s ticker 采集 + 上报到 `127.0.0.1:18080/ws/agent`；
- `agent-go-SuMon/bin/susumonitor-agent-linux-amd64` Linux 二进制已交叉编译成功（`B-007` 已完成）；
- `20260721-Agent-WebSocket真实验收.md` 真实 HTTP/Agent/MySQL 链路通过。

### 阶段 3：MVP-3 实时监控链路（2026-07-21）

**目标**：Monitor Ticket + AFTER_COMMIT 推送 + `metrics.update` 帧。

| 交付 | 关键 dev-log |
|---|---|
| 32B 一次性 30s ticket + `@Scheduled` 清理 | `20260721-Monitor-Ticket实现*.md` |
| `MonitorHandshakeInterceptor` 消费 ticket + Origin 白名单 | 同上 |
| `MetricsReportedEvent` AFTER_COMMIT 广播 | `20260721-Metrics-ReportedEvent-*.md` |
| `MonitorMetricsPublisher` 订阅 + 实时推送 | 同上 |
| 监控端 `metrics.update` 帧协议 | `Protocol-SuMon/websocket-protocol.md` |
| 10s 认证 / 90s 离线判定 | `20260721-Agent-Monitor实时链路执行.md` |

**真实验证**：
- 真实 HTTP+MySQL+Node WS 三方联调脚本通过（`api-test/verify-monitor-ws.mjs`）；
- `20260721-Monitor-WebSocket真实验收.md` 完整记录；
- 单元测试 18 项 + 集成测试 5 项全过。

### 阶段 4：MVP-5A Web 主页面（2026-07-12 ~ 2026-07-23）

**目标**：Vue 3 + Pinia + Axios + Element Plus 实现 11 个视图与基础交互。

| 视图 | dev-log |
|---|---|
| `LoginView` / `RegisterView` / `AuthLayout` | `2026072[0-3]-前端M2-登录注册*.md` |
| `DashboardView`（4 探针 + ServerSparkLine） | `2026072[2-4]-前端M5-Dashboard*.md` |
| `ServerListView` / `ServerDetailView` / `ServerFormDialog` | `2026072[0-3]-前端M3-服务器*.md` |
| `MetricsView` / `MetricsHistory` 表格 | `2026072[2-4]-前端M4-实时监控*.md` |
| `AdminUsersView`（admin 审核 pending） | `2026072[1-2]-前端M6-审核*.md` |
| `ForbiddenView` / `NotFoundView` | 共用 |

**配套**：
- `stores/{auth,alerts,metrics}.ts`，`auth` 用 `pinia-plugin-persistedstate` 写 localStorage；
- `api/{client,auth,server,agent-token,metrics,alert,admin,system,websocket}.ts`；
- `composables/{useDebouncedRef,useRouterLoading}.ts`；
- `MainLayout.vue` + `router/guards.ts` 三段守卫。

**真实验证**：
- Vitest 37 测试 + ESLint + 构建产物 OK（`README.md` §4 道防线 Vitest 列）；
- 浏览器 E2E 17 路径（`scripts/ui-e2e-test.mjs`）；
- API E2E 13 路径（`scripts/api-e2e-test.mjs`）；
- audit:catchup 11 规则 0 ERROR / 0 WARN。

### 阶段 5：MVP-6 告警业务闭环（2026-07-25 ~ 2026-07-28）

**目标**：规则 CRUD + 状态机 + 唯一索引 + alert.push 推送 + 前端 store 重抛。

| 交付 | 关键 dev-log |
|---|---|
| V10：alert_states 乐观锁 + alert_rules 软删除 | `20260725-MVP-6告警业务闭环实现.md` |
| V11：metrics_ingestions 幂等表 | `20260725-P2-2指标幂等与乱序拒绝实现.md` |
| V13：uk_alert_rules_active_signature 唯一索引（生成列） | `20260728-MVP6告警规则唯一性修复.md` |
| `AlertStateMachine` sealed interface 4 transition | `20260725-MVP-6告警业务闭环实现.md` §状态机 |
| `AlertEvaluationServiceImpl` AFTER_COMMIT + REQUIRES_NEW | 同上 + `20260725-V11真实验证与P2-1限流实现.md`（Spring 6.2 拒绝组合） |
| `AlertPushPublisher` 推 `alert.push` 帧 | `20260725-MVP-6告警业务闭环实现.md` |
| 前端 `AlertRulesView` / `AlertRecordsView` / `AlertRuleDialog` | `20260725-MVP6告警前端开发*.md` |
| `stores/alerts.ts` catch 重抛 + 返回类型 `Promise<AlertRule>` | `Bug-fix/2026-07-27-MVP6-alert-store-swallows-error.md` |

**真实验证**：
- Apifox 真实业务链路告警 30 断言；
- `20260728-MVP6告警运行时验收资产.md` 真实环境跑通 Trigger→Continue→Resolve 全状态机路径；
- `Bug-fix/2026-07-27-MVP6-alert-rules-mapper-500.md` 修复 MyBatis `@Param("rule")` 缺注解问题。

### 阶段 6：MVP-7 Web 终端（2026-07-26）

**目标**：浏览器开反向 PTY 终端，三段链路 + 双层字节限流 + Monitor 背压 + 配额。

| 交付 | 关键 dev-log |
|---|---|
| 终端协议 8 类型 + `TerminalProtocolValidator` | `20260726-终端协议契约实现.md` |
| `TerminalSessionEntity` + 状态机 + 配额锁 | `20260726-终端会话生命周期实现.md` |
| 三段链路中继 + `TerminalRelayRegistry` | `20260726-终端中继注册表实现.md` + `Monitor下行中继` + `Agent上行回送` |
| Go Agent `internal/terminal/manager_linux.go` 用 `creack/pty v1.1.24` | `20260726-Go-LinuxPTY模块实现.md` |
| 双层字节限流：Go Token Bucket + Java `TerminalOutputRateLimiter` | `20260726-Go-Agent-PTY输出字节限流实现.md` + `20260726-Java-Agent输出带宽限流.md` |
| 控制帧限流：`TerminalMessageRateLimiter` | `20260726-Java终端控制帧限流.md` |
| Monitor 背压：`ConcurrentWebSocketSessionDecorator` | `20260726-Monitor慢消费者背压保护.md` |
| 前端 `TerminalView.vue` + xterm.js + 复用 Monitor WS | `20260728-MVP7-T4前端Web终端最小可用版本.md` |

**真实验证**：
- **PTY_RELAY_INTEGRATION_OK**（2026-07-26）：
  - Java `18081` 起服务
  - WSL Go Agent 起 `/bin/bash` PTY
  - 真实链路 `terminal.open/opened/input/output/resize/close` 6 帧全过
- WSL 流控真实验收清单全过（`20260727-终端流控WSL真实验收.md`）；
- `20260726-终端Java-Go真实联调准备与阻塞记录.md` 详细记录；
- 单元测试 34 项 + 全量 326 项通过。

### 阶段 7：Sprint 1-4 + Polish（2026-07-22 ~ 2026-07-30）

| Sprint | 目标 | dev-log |
|---|---|---|
| **Sprint 1** | SSH 测试按钮接真实后端 + 7 个 SSH 错误码 | `20260722-Sprint-1-SSH测试真实化.md` |
| **Sprint 2** | ServerListView spark line 接真实 metrics 历史 | `20260723-Sprint-2-ServerListView*.md` |
| **Sprint 3** | DashboardView spark + 通用 `ServerSparkLine` 组件化 + LONG_FILE 拆分 | `20260724-Sprint-3-通用spark*.md` |
| **Sprint 4** | ServerSearchBar `useDebouncedRef` + ServerPagination + audit 收口 | `20260725-Sprint-4-前端搜索*.md` |
| **Polish 1-5** | dirty 清理 / e2e 选择器 / LONG_FILE 拆分 / audit 收口 / GitHub 推送 | `20260726-30-Polish-*.md` |

**真实联调证据**：
- `20260729-前端B-038真实浏览器联调收口.md`：Apifox 4 端点 Agent Token register/rotate/revoke + SSH host-key 全过；
- `20260729-ServerListView分页修复.md`：3 页数据真实浏览器联调通过。

### 阶段 8：T5 云端部署资产（2026-07-27 ~ 2026-07-29）

| 资产 | 路径 | dev-log |
|---|---|---|
| Maven Wrapper 3.9.9 | `server-java-SuMon/mvnw{,.cmd}` + `.mvn/wrapper/` | `20260727-T5云端部署资产.md` |
| `application-prod.yml` 生产 Profile | `server-java-SuMon/src/main/resources/application-prod.yml` | 同上 |
| systemd Unit | `server-java-SuMon/deploy/susumonitor-server.service` | 同上 |
| Nginx 反代示例 | `server-java-SuMon/deploy/nginx-susumonitor.conf.example` | 同上 |
| 环境变量模板 | `server-java-SuMon/deploy/susumonitor-server.env.example` + `.env.example` | 同上 |
| 一键安装 | `server-java-SuMon/deploy/install.sh` | 同上 |
| 部署交接手册 | `docs-SuMon/Handoff-SuMon/20260727-云端T5部署交接.md` | 同上 |

**状态**：资产齐全；公网真实部署等域名备案完成（待验证）。

---

## 三、横切模块维度：8 模块 × 真实验证方式

| 模块 | 真实集成验证方式 | 工具 / 脚本 |
|---|---|---|
| M1 用户与权限 | Apifox 9 / 27 断言；隔离 MySQL V7 回填 admin | Apifox + `Flyway V7` |
| M2 服务器与 SSH | WSL SSHD `127.0.0.1:2223` 受控环境验 SSH 50002/50003 | J6/J7 dev-log + sshj |
| M3 Agent 通道 | Go Agent 本机 + 隔离 MySQL + 真实 WS | `20260721-Agent-WebSocket真实验收.md` |
| M4 实时监控 | Monitor Ticket + Node WS 客户端 + 隔离 MySQL | `api-test/verify-monitor-ws.mjs` |
| M5 告警闭环 | Apifox 30 断言 + 真实 MySQL | `20260728-MVP6告警运行时验收资产.md` |
| M6 Web 终端 | WSL Go Agent 起 `/bin/bash` PTY + Java 中继 | `PTY_RELAY_INTEGRATION_OK 2026-07-26` |
| M7 基础设施 | 单元 + MockMvc 全量通过；springdoc 暴露 | JUnit 5 + OpenAPI |
| M8 数据库迁移 | 隔离 MySQL V1-V13 全量迁移 | `mysql-validation` profile |

---

## 四、当前测试数据

### 单元测试数量分布

| 模块 | 单元测试数（粗略） | 集成测试数 |
|---|---|---|
| auth + admin | 30+ | 0（H2 即可） |
| server（含 SSH）| 40+ | 1（ServerMapperMySqlValidationIT） |
| metrics | 20+ | 2（MetricsCleanup / MetricsIngestion MySQL IT） |
| alert | 50+ | 1（AlertRule MySQL IT） |
| terminal | 30+ | 1（TerminalSessionQuota MySQL IT） |
| websocket（agent + monitor + terminal）| 100+ | 0（用 MockMvc + Mockito） |
| security + config + ssh + common | 50+ | 0 |
| **Java 合计** | **320+** | **5** |
| 前端（Vitest） | 50+ | ui:e2e 17 路径 + api:e2e 13 路径 + audit:catchup 11 规则 |

### 4 道防线总览

| 防线 | 命令 | 计数 | 用途 |
|---|---|---|---|
| Vitest | `npm run test` | 50+ 测试 | 单元 |
| audit:catchup | `npm run audit:catchup` | 11 规则 0/0 | AST + 依赖审查 |
| api:e2e | `npm run api:e2e` | 13 路径 | REST + WS 接口契约 |
| ui:e2e | `npm run ui:e2e` | 17 路径 | 浏览器流程 |

---

## 五、真实验证资源清单

| 资源 | 位置 / 配置 | 用途 |
|---|---|---|
| **隔离 MySQL 库** | `susumonitor_validation` 本机 `127.0.0.1:3306`，沙箱守卫防误连开发库 | Flyway 真实迁移 + Mapper SQL 验证 |
| **WSL Ubuntu SSHD** | `127.0.0.1:2223` 自启 + 临时 RSA 主机密钥 | SSH 错误码分类真实验证 |
| **WSL Linux PTY** | Go Agent `/bin/bash` + `creack/pty` | Web 终端反向 shell 真实验证 |
| **Apifox 27 断言** | `docs-SuMon/Bug-fix/2026-07-21-M4-*.md` + Sprint 1 | SSH host-key + token 三件套 |
| **独立 Go Agent 二进制** | `agent-go-SuMon/bin/susumonitor-agent-linux-amd64` | Linux amd64 实测可执行 |
| **Node WS 联调脚本** | `api-test/verify-monitor-ws.mjs` 等 4 份 | 模拟浏览器建 WS + 接收推送 |
| **PowerShell smoke 脚本** | `server-java-SuMon/deploy/smoke-127.0.0.1.ps1` | 一键 IPv4 smoke 测试 |
| **Bash smoke 脚本** | `server-java-SuMon/deploy/smoke-127.0.0.1.sh` | 同上 |

---

## 六、关键截图占位（可放实拍截图）

> 说明：以下位置建议截图放进来做面试材料。每张截图配一句话说明。

| 位置 | 截图内容 |
|---|---|
| README.md 头部 | Dashboard 主界面（玻璃形态卡片 + 探针 + spark line） |
| 01-M1 | 管理员审核界面 + 错误码 toast 提示 |
| 01-M2 | ServerListView（含 spark line + 分页）+ SSH 测试错误码 toast |
| 01-M3 | Go Agent 终端运行日志 + WS 鉴权成功帧 |
| 01-M4 | MonitorTicketController ticket 抓包 + Monitor WS 实时推送帧 |
| 01-M5 | 告警规则列表 + 触发的 alert.push 帧 payload |
| 01-M6 | TerminalView xterm 输出 `/bin/bash` + 列宽自适应 |
| 03-§难题 1 | 三段链路断连日志 + 双层限流关闭事件 |
| 04-§阶段 6 | PTY_RELAY_INTEGRATION_OK 控制台截图 |
| 04-§ T5 | OpenCloudOS 部署 + Nginx 反代配置实际生效 |

---

## 七、面试反证话术（"这不只是 demo"）

| 面试官质疑 | 你的回答 |
|---|---|
| "这只是 demo？" | "整套业务在隔离 MySQL + WSL SSHD + 真实浏览器三套环境跑过 MVP-1 到 MVP-7。涉及 320+ 单测、4 道 E2E 防线、Apifox 27 + 30 断言、WSL PTY_RELAY_INTEGRATION_OK 6 帧链路全过。具体证据在 `docs-SuMon/Introduction/04-项目里程碑与真实验证.md`。" |
| "性能怎么样？" | "MVP-9 性能基线已建立测试方案（单服务器 1000 规则 / 多服务器并发 / 持续越界 / Monitor 订阅数变化），指标口径含 p50/p95/p99 + 异常率；数字尚未执行。" |
| "多实例怎么办？" | "目前所有 WS 路由、ticket、终端中继都是单 JVM 内存。未来加 Redis 做状态共享 / RabbitMQ 做事件解耦 / spring-session-data-redis 共享 session。演进路径写在 `Introduction/05-项目反思与未来规划.md`。" |
| "为什么没有 docker？" | "目前部署用 systemd + Nginx；Dockerfile + docker-compose 在 `项目需求与规范.md` 资产缺口清单中明确登记为'未实现'，是我下一步计划。" |
| "测试覆盖够吗？" | "320+ 单测覆盖率约 70-80%（行覆盖率未跑覆盖率统计），4 道防线 + Apifox 27/30 断言 + 5 个 MySQL `*IT.java`，业务关键路径全在 CI 守护中。" |

---

## 八、链接到现有证据库的路径速查

| 想找 | 翻这份 |
|---|---|
| 阶段交付细节 | `docs-SuMon/Develop-log/2026*-*.md`（67 篇） |
| Bug 修复 | `docs-SuMon/Bug-fix/2026*-*.md`（9 篇） |
| 线上 / 阻塞问题诊断 | `docs-SuMon/Difficulty-log/2026*-*.md`（5 篇） |
| 协议契约 | `docs-SuMon/Protocol-SuMon/{websocket-protocol,message-contracts-v1}.md` |
| OpenAPI JSON | `docs-SuMon/OpenApi-SuMon/{system,auth,admin,server,alert}.json` |
| 阶段计划 | `docs-SuMon/Develop-plans/2026*-*.md`（9 份） |
| 阶段交接 | `docs-SuMon/Handoff-SuMon/2026*-*.md`（2 份） |
| 总览状态 | 根目录 `README.md` 与 `项目需求与规范.md` |
