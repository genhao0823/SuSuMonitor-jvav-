# 前端正式功能开发规划(2026-07-24)

**目标**:在后端已 production-ready 基础上,规划前端正式功能开发路线
**规划期**:2026-07-24 -> 2026-08-31
**关联计划**:`docs-SuMon/Develop-plans/20260719-Web前端并行开发计划.md`(早期 M1-M5)
**关联后端状态**:`820ded0 fix(docs): 收口项目状态与接口契约`

---

## 1. 现状盘点

### 1.1 前端已闭环

| 维度 | 状态 | commit |
|---|---|---|
| M1 工程骨架 | ✅ | `005e655` |
| M2 鉴权 | ✅ | `566f324` |
| M3 主布局 | ✅ | `00db9db` |
| M4 CRUD | ✅ | `5b7800d` |
| M5 审核 | ✅ | `8b100b5` + `1d1b4f4` |
| M6 监控 | ✅ | `9cf8269` |
| NProgress | ✅ | `d0d80aa` |
| 3 道防线(audit/api-e2e/ui-e2e) | ✅ | `56812d2` / `38ca282` / `b1437f6` |
| 28 单元测试 | ✅ | `8454863` |
| Polish 3 LONG_FILE 拆分 | ✅ | `6b34d9a` / `0962154` / `8fbc3e5` |
| 总 commits | 60+ | - |

### 1.2 前端已知遗留

| 项 | 状态 |
|---|---|
| SSH 测试按钮 | ⚠️ 占位 `ElMessage.info('MVP-7 接入')` |
| ServerListView spark line | ⚠️ mock |
| DashboardView spark line | ⚠️ mock |
| DashboardView SSH 历史卡 | ⚠️ 占位 |
| AdminUsersView 批量审核/搜索 | ❌ 后端无对应接口 |
| Polish 5 GitHub remote | ❌ 未做 |

### 1.3 后端已就绪能力

| 后端模块 | 状态 | 前端可用 |
|---|---|---|
| auth(register/login/me/logout + JWT) | ✅ | M2 完整 |
| server CRUD + status + SSH test | ✅ | M4 + **SSH 待接** |
| metrics(latest + history) | ✅ | M6 完整 |
| admin(pending + approve + reject) | ✅ | M5 完整 |
| system(health + ready) | ✅ | Dashboard 完整 |
| WebSocket(Agent + Monitor ticket) | ✅ | M6 完整 |
| OpenAPI 契约(3 文件,77 KB) | ✅ | 稳定 |

---

## 2. 4 个 Sprint 规划

### Sprint 1:SSH 测试按钮接真实后端(本周,~40 分钟)

**目标**:消除前端最显眼 TODO,接 `/api/servers/{id}/ssh/test`

**后端依赖**:✅ `5d8e0db fix(server): 区分SSH认证失败错误码` 已就绪

**改动范围**:
- `api/server.ts` 加 `testSshConnection(id)` 函数
- `types/api.d.ts` 确认 `SshTestVo` 类型
- `ServerListView.vue` + `ServerDetailView.vue` 改 `handleTestConnection`
- 错误码映射(SSH 认证失败 / 连接超时 / 网络不可达)

**commit**:2 个(feat + docs)

### Sprint 2:ServerListView spark line 真实化(~60 分钟)

**目标**:用 `/api/servers/{id}/metrics` 历史画真实 spark line

**后端依赖**:✅ metrics API 已就绪

**改动范围**:
- 新增 `components/ServerSparkLine.vue`(~80 行,SVG)
- `ServerListView.vue` 加 spark 列

**commit**:1 个(feat)

### Sprint 3:DashboardView 完整化(~60 分钟)

**目标**:Dashboard 4 个 KPI + spark 全部接真实数据

**后端依赖**:✅ metrics + servers API

**改动范围**:
- 复用 Sprint 2 的 `ServerSparkLine.vue`
- DashboardView spark 接真实历史
- SSH 历史卡接真实数据(如后端支持)

**commit**:1 个(feat)

### Sprint 4:Polish 收口(~30 分钟)

**目标**:清理 audit 误报 + 推 GitHub(需用户提供 URL)

**改动范围**:
- `audit-catchup.mjs` LONG_FILE 改用 `wc -l`
- `git remote add origin` + push(需用户提供 URL + 认证)

**commit**:2-3 个

---

## 3. 技术栈

| 类别 | 选型 | 状态 |
|---|---|---|
| 框架 | Vue 3.5 + `<script setup>` | ✅ 已有 |
| 状态 | Pinia 3 + persistedstate | ✅ 已有 |
| HTTP | axios 1.7 + 自封装 client | ✅ 已有 |
| UI | Element Plus 2 | ✅ 已有 |
| 类型 | TypeScript 5 strict | ✅ 已有 |
| 测试 | Vitest 1 + @vue/test-utils + jsdom | ✅ Polish 4 已装 |
| E2E | puppeteer-core + 系统 Chrome | ✅ Polish 1 已装 |
| 静态扫描 | audit:catchup(11 规则) | ✅ Module 1 |

**无新增依赖**。所有 Sprint 仅复用现有栈。

---

## 4. 验证清单(每 Sprint 必跑)

| 检查 | 命令 | 预期 |
|---|---|---|
| typecheck | `npm run typecheck` | 0 错 |
| lint | `npm run lint` | 0 错 0 警 |
| test | `npm run test` | 28+ passed |
| openapi:check | `npm run openapi:check` | 3/3 |
| audit:catchup | `npm run audit:catchup` | 0/0/X |

---

## 5. 风险与对策

| 风险 | 处理 |
|---|---|
| SSH 错误码边界 | 先读后端 ErrorCode.java + SshTestVo,做映射 |
| spark 性能(每行拉历史) | 缓存 + 5min TTL |
| Edit 工具误删 | 每次 Edit 后 `git diff` 自检 |
| commit 引入回归 | 跑 4 件套 + 28 单元测试 |

---

## 6. 时间线

| 周 | Sprint | 工作量 |
|---|---|---|
| W1(7/24) | Sprint 1 SSH 真实化 | ~40 分钟 |
| W2(7/31) | Sprint 2 spark line | ~60 分钟 |
| W3(8/7) | Sprint 3 Dashboard | ~60 分钟 |
| W4(8/14) | Sprint 4 Polish 收口 | ~30 分钟 |
| W5-6 | 缓冲 / 新需求 | - |

---

## 7. 维护说明

**后续每 Sprint 开始前**:
1. 读本规划文档(Sprint N 部分)
2. 跑 4 件套确认基线干净
3. 执行步骤 -> 自检 -> commit -> push

**每 Sprint 完成后**:
- 在本规划文档末尾追加"实际进度"
- 更新 README 表格

---

## 8. 关联文档

- `docs-SuMon/Develop-log/20260722-Web前端总览.md` - M1-M6 总结
- `docs-SuMon/Develop-plans/20260720-Web前端详细开发计划.md` - M1-M5 详细
- `karpathy-guidelines` Section 13 - Edit 误删防御
