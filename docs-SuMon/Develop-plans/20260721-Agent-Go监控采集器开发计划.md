# Agent-Go 监控采集器开发计划

**计划日期**：2026-07-21
**计划状态**：规划中，尚未执行
**适用项目**：SuSuMonitor
**当前主线**：MVP-2 监控采集 Agent
**执行原则**：先调查、先备份、最小修改、分阶段验证、完整留痕

## 一、计划目标

### 1.1 总目标

在 Java 后端 WebSocket Agent 通道（`/ws/agent`）、Agent Token 生命周期、Metrics 接收/存储/查询/实时推送链路均已实现的基础上，开发正式 Go 跨平台监控采集 Agent，完成以下闭环：

```text
Go Agent 启动
→ 读取配置
→ 连接 ws://<backend>/ws/agent
→ 首帧 agent.authenticate（server_id + agent_token）
→ 收到 agent.authenticated
→ 每 5 秒采集系统指标 + metrics.report
→ 每 30 秒 heartbeat
→ 断线指数退避重连
→ 后端写入 MySQL metrics 表
→ REST /metrics/latest 与 /metrics 返回真实指标
→ Monitor WebSocket 广播 metrics.update
```

### 1.2 本计划包含

- Go Agent 工程骨架（`agent-go-SuMon/`）。
- 配置加载与校验。
- WebSocket 连接、首帧鉴权、心跳、指标上报。
- 指数退避重连。
- 跨平台系统指标采集（CPU、内存、磁盘、网络）。
- Windows 本机直接运行验证。
- 单元测试与极简链路冒烟测试。

### 1.3 本计划不包含

- Linux systemd service、安装脚本、一键部署（属阶段 4 后续）。
- 温度采集在 Windows 上的强制实现（Windows 多数机器无传感器，置 null）。
- load_avg 在 Windows 上的强制实现（Windows 无 loadavg 概念，置 null）。
- 前端 MetricsView 挂路由与 WebSocket 鉴权修复（属 MVP-3 前端收口）。
- AI 分析、AI 运维、Android 客户端（属 MVP-8 之后增强）。
- Docker、Kubernetes、远程 Linux 生产部署。

## 二、当前基线与事实

### 2.1 后端已实现能力

执行本计划前，Java 后端已实现以下能力，Go Agent 直接对接：

- WebSocket 路径 `/ws/agent`（`AgentWebSocketConfig.java:25`）。
- 首帧鉴权 `agent.authenticate`，payload 含 `server_id` 和 `token`（`AgentWebSocketHandler.java:72,105-117`）。
- 鉴权成功返回 `agent.authenticated`（`AgentMessageType.java:8`）。
- 心跳 `heartbeat` / `heartbeat.ack`（`AgentMessageType.java:9-10`）。
- 指标上报 `metrics.report`，payload 为固定宽表快照（`AgentWebSocketHandler.java:78-81`）。
- 首帧认证超时 10 秒，超时关闭连接（`AgentWebSocketHandler.java:120-130`）。
- 90 秒无心跳标记离线（`AgentHeartbeatService.java:16,37-53`）。
- 新连接替换旧连接（`AgentConnectionRegistry.java:21-28`）。
- 消息字段 `message_id`、`timestamp`（UTC ISO-8601）（`AgentMessage.java`、`AgentWebSocketHandler.java:137-138`）。
- 消息大小上限 64 KiB（`AgentWebSocketHandler.java:30`）。
- Agent Token 通过 REST `POST /api/servers/{id}/agent/register` 由 admin 预发放，明文仅一次性返回（`AgentTokenService.java:39-43,89`）。
- Metrics 写入后发 `MetricsReportedEvent`，事务提交后广播 `metrics.update`（`MetricsService.java:49`、`MonitorMetricsPublisher.java:28-40`）。

### 2.2 协议契约

正式协议文档为 `docs-SuMon/Protocol-SuMon/websocket-protocol.md`。Go Agent 必须严格按此文档实现，不得按 `项目需求与规范.md` 第 12 节的旧别名实现。

关键协议要点：

| 项 | 值 |
|----|-----|
| WebSocket 地址 | `ws://<backend>/ws/agent` |
| 通用消息字段 | `type`、`message_id`(UUID)、`timestamp`(UTC ISO-8601)、`payload` |
| 首帧类型 | `agent.authenticate` |
| 首帧 payload | `{server_id, token}` |
| 鉴权成功响应 | `agent.authenticated` |
| 心跳类型 | `heartbeat` |
| 心跳响应 | `heartbeat.ack` |
| 指标上报类型 | `metrics.report` |
| 指标 payload | 固定宽表：server_id、collected_at、cpu_percent、memory_percent、memory_used、memory_total、disk_percent、disk_used、disk_total、net_rx、net_tx、temperature、load_avg |
| 消息大小上限 | 64 KiB |
| 首帧超时 | 10 秒 |
| 离线判定 | 90 秒无心跳 |

### 2.3 执行前基线命令

```powershell
git status --short
git diff --check
```

后端健康检查：

```powershell
Invoke-WebRequest http://localhost:18080/api/health
Invoke-WebRequest http://localhost:18080/api/ready
```

## 三、技术栈选型

### 3.1 语言与版本

| 项 | 选定 | 理由 |
|----|------|------|
| 语言 | Go | 跨平台编译、单二进制部署、Linux 生产常用、Windows 能直接跑、资源占用低 |
| 最低版本 | Go 1.22 | `log/slog` 标准库成熟、泛型稳定 |
| 目标版本 | Go 1.22+ | 长期支持 |

### 3.2 依赖

| 依赖 | 版本 | 用途 |
|------|------|------|
| `github.com/coder/websocket` | 最新稳定 | WebSocket 客户端，API 简洁、支持 context 超时 |
| `github.com/shirou/gopsutil/v3` | v3 最新 | 跨平台系统指标采集（CPU、内存、磁盘、网络） |
| 标准库 `log/slog` | — | 结构化日志 |
| 标准库 `encoding/json` | — | JSON 序列化 |
| 标准库 `net/http` | — | HTTP 客户端（获取 Agent Token） |
| 标准库 `os`/`os/signal` | — | 信号处理、优雅关闭 |
| 标准库 `time` | — | 定时器、超时 |
| 标准库 `context` | — | 超时与取消传播 |

不引入第三方配置库；配置用 JSON 或简单 YAML 解析。不引入第三方日志库；用标准库 `log/slog`。

## 四、工程目录结构

```text
agent-go-SuMon/
├── go.mod
├── go.sum
├── README.md
├── .env.example
├── Makefile
├── config/
│   └── agent.example.yml
├── cmd/
│   └── susumonitor-agent/
│       └── main.go
├── internal/
│   ├── config/
│   │   ├── config.go          配置结构与加载
│   │   └── config_test.go
│   ├── wsclient/
│   │   ├── client.go          WebSocket 连接、鉴权、重连
│   │   ├── message.go         消息结构定义
│   │   └── client_test.go
│   ├── collector/
│   │   ├── collector.go       系统指标采集接口
│   │   ├── gopsutil_collector.go  gopsutil 实现
│   │   └── collector_test.go
│   └── reporter/
│       ├── reporter.go        metrics.report 消息构造
│       └── reporter_test.go
└── scripts/
    └── smoke-test-ws-agent.ps1  极简链路冒烟（放项目 scripts/ 目录）
```

目录设计原则：

- `cmd/` 只放启动入口，不放业务逻辑。
- `internal/` 放内部实现，不对外暴露。
- 每个 package 单一职责：config 管配置、wsclient 管连接、collector 管采集、reporter 管上报。
- 测试文件与源码同目录，`_test.go` 后缀。
- 不过度抽象；不为单次使用的代码创建接口。

## 五、配置模型

### 5.1 配置文件

`config/agent.yml`（本机测试用，不提交 Git）：

```yaml
backend_url: "ws://localhost:18080"
server_id: 1
agent_token: "REPLACE_WITH_AGENT_TOKEN"
collect_interval_seconds: 5
heartbeat_interval_seconds: 30
reconnect_initial_seconds: 5
reconnect_max_seconds: 60
log_level: "info"
```

`.env.example` 列出环境变量：

```text
SUSUMONITOR_BACKEND_URL=ws://localhost:18080
SUSUMONITOR_SERVER_ID=1
SUSUMONITOR_AGENT_TOKEN=REPLACE_WITH_AGENT_TOKEN
SUSUMONITOR_COLLECT_INTERVAL_SECONDS=5
SUSUMONITOR_HEARTBEAT_INTERVAL_SECONDS=30
SUSUMONITOR_RECONNECT_INITIAL_SECONDS=5
SUSUMONITOR_RECONNECT_MAX_SECONDS=60
SUSUMONITOR_LOG_LEVEL=info
```

### 5.2 优先级

环境变量 > 配置文件 > 默认值。

### 5.3 敏感信息处理

- `agent_token` 优先从环境变量 `SUSUMONITOR_AGENT_TOKEN` 读取。
- 本机测试可从配置文件读，但配置文件必须加入 `.gitignore`。
- 日志中不得输出 `agent_token` 明文；只输出前 8 字符用于排障。
- 日志中不得输出完整 `agent_token`、密钥或完整 WebSocket URL（含 token 参数）。

### 5.4 配置校验

启动时校验：

- `backend_url` 非空，且以 `ws://` 或 `wss://` 开头。
- `server_id` 为正整数。
- `agent_token` 非空。
- `collect_interval_seconds` >= 1。
- `heartbeat_interval_seconds` >= 1。
- `reconnect_initial_seconds` >= 1。
- `reconnect_max_seconds` >= `reconnect_initial_seconds`。

非法配置启动失败，不退化为默认值。

## 六、WebSocket 连接与状态机

### 6.1 状态定义

```text
Disconnected → Connecting → Authenticating → Connected → Reconnecting
```

| 状态 | 说明 |
|------|------|
| Disconnected | 初始状态或关闭后 |
| Connecting | WebSocket 握手中 |
| Authenticating | 已发 agent.authenticate，等待 agent.authenticated |
| Connected | 鉴权成功，可发 heartbeat 和 metrics.report |
| Reconnecting | 连接断开，指数退避等待重连 |

### 6.2 首帧鉴权流程

1. 连接 `ws://<backend_url>/ws/agent`。
2. 构造 `agent.authenticate` 消息：

```json
{
  "type": "agent.authenticate",
  "message_id": "<UUID>",
  "timestamp": "<UTC ISO-8601>",
  "payload": {
    "server_id": <配置的 server_id>,
    "token": "<配置的 agent_token>"
  }
}
```

3. 发送首帧，启动 10 秒超时。
4. 收到 `agent.authenticated` → 进入 Connected 状态。
5. 10 秒内未收到响应 → 关闭连接，进入 Reconnecting。
6. 收到 `error` 或被关闭 → 进入 Reconnecting。

### 6.3 心跳

Connected 状态下每 `heartbeat_interval_seconds`（默认 30 秒）发送：

```json
{
  "type": "heartbeat",
  "message_id": "<UUID>",
  "timestamp": "<UTC ISO-8601>",
  "payload": {}
}
```

等待 `heartbeat.ack`。未收到 ack 不阻塞采集，只记日志。

### 6.4 指标上报

Connected 状态下每 `collect_interval_seconds`（默认 5 秒）采集并上报：

```json
{
  "type": "metrics.report",
  "message_id": "<UUID>",
  "timestamp": "<UTC ISO-8601>",
  "payload": {
    "server_id": <id>,
    "collected_at": "<UTC ISO-8601>",
    "cpu_percent": <0-100>,
    "memory_percent": <0-100>,
    "memory_used": <bytes>,
    "memory_total": <bytes>,
    "disk_percent": <0-100>,
    "disk_used": <bytes>,
    "disk_total": <bytes>,
    "net_rx": <累计字节>,
    "net_tx": <累计字节>,
    "temperature": null,
    "load_avg": null
  }
}
```

### 6.5 重连

连接断开后：

1. 进入 Reconnecting。
2. 等待 `reconnect_initial_seconds`（默认 5 秒）。
3. 尝试重连。
4. 失败则等待时间翻倍，上限 `reconnect_max_seconds`（默认 60 秒）。
5. 成功后重置等待时间为初始值。

### 6.6 优雅关闭

收到 `SIGINT` 或 `SIGTERM`：

1. 停止采集和心跳 ticker。
2. 发送 WebSocket Close 帧。
3. 等待 5 秒强制退出。

## 七、系统指标采集策略

### 7.1 采集库

统一使用 `github.com/shirou/gopsutil/v3`，跨平台支持 Linux/Windows/macOS。

### 7.2 各指标采集方式

| 指标 | gopsutil 调用 | 说明 |
|------|--------------|------|
| cpu_percent | `cpu.Percent(time.Second, false)` | 采样 1 秒，返回使用率 |
| memory_percent | `mem.VirtualMemory()` 取 `UsedPercent` | 已用/总 |
| memory_used | `mem.VirtualMemory()` 取 `Used` | 字节 |
| memory_total | `mem.VirtualMemory()` 取 `Total` | 字节 |
| disk_percent | `disk.Usage("/")` 取 `UsedPercent` | 系统盘；Linux 用 `/`，Windows 用 `C:` |
| disk_used | `disk.Usage(mount)` 取 `Used` | 字节 |
| disk_total | `disk.Usage(mount)` 取 `Total` | 字节 |
| net_rx | `net.IOCounters(false)` 取 `BytesRecv` | 累计字节，非速率 |
| net_tx | `net.IOCounters(false)` 取 `BytesSent` | 累计字节，非速率 |
| temperature | `host.SensorsTemperatures()` | Windows 多数无传感器 → null |
| load_avg | `load.Avg()` 取 `Load1` | Windows 无 loadavg → null |

### 7.3 net_rx/net_tx 语义

上报**累计字节数**（从操作系统启动开始计），不是速率。后端存储累计值，查询时由前端或查询接口计算增量。

### 7.4 磁盘挂载点

- Linux：`/`
- Windows：`C:`

通过 `disk.Partitions(false)` 获取第一个物理分区，或配置指定。

### 7.5 temperature 和 load_avg 的 null 处理

- Windows 上 `host.SensorsTemperatures()` 通常返回空或错误 → payload 中 `temperature` 设为 `null`。
- Windows 上 `load.Avg()` 通常返回错误 → payload 中 `load_avg` 设为 `null`。
- 后端 `MetricsService.validatePayload` 对 null 跳过校验（`MetricsService.java:110-120`），允许 null。

### 7.6 精度

- 百分比：保留 2 位小数（如 `35.23`）。
- 字节数：整数。
- Go 用 `float64`，JSON 序列化时 `encoding/json` 自动处理。
- 后端用 `BigDecimal` 接收，不丢失精度。

## 八、时间与 JSON 规范

| 项 | 规范 |
|----|------|
| 时间格式 | UTC ISO-8601 字符串，如 `2026-07-21T12:00:00Z` |
| message_id | UUID v4（`crypto/rand` 或 `google/uuid`，优先用标准库） |
| JSON 字段名 | snake_case（`server_id`、`cpu_percent` 等） |
| timestamp | UTC ISO-8601 |
| collected_at | UTC ISO-8601 |
| 消息大小 | 不超过 64 KiB |

Go 结构体用 `json` tag 控制字段名：

```go
type MetricsPayload struct {
    ServerID       int64    `json:"server_id"`
    CollectedAt    string   `json:"collected_at"`
    CPUPercent     *float64 `json:"cpu_percent"`
    MemoryPercent  *float64 `json:"memory_percent"`
    MemoryUsed     *uint64  `json:"memory_used"`
    MemoryTotal    *uint64  `json:"memory_total"`
    DiskPercent    *float64 `json:"disk_percent"`
    DiskUsed       *uint64  `json:"disk_used"`
    DiskTotal      *uint64  `json:"disk_total"`
    NetRx          *uint64  `json:"net_rx"`
    NetTx          *uint64  `json:"net_tx"`
    Temperature    *float64 `json:"temperature"`
    LoadAvg        *float64 `json:"load_avg"`
}
```

用指针类型表示可空字段，`nil` 序列化为 `null`。

## 九、超时与资源限制

| 项 | 值 | 说明 |
|----|-----|------|
| 首帧认证超时 | 10 秒 | 与后端 `AgentWebSocketHandler:122` 一致 |
| WebSocket 写超时 | 5 秒 | 防止发送阻塞 |
| 采集超时 | 3 秒 | gopsutil 调用超时 |
| 重连初始间隔 | 5 秒 | |
| 重连最大间隔 | 60 秒 | |
| 消息大小上限 | 64 KiB | 与后端一致 |

## 十、并发与背压

### 10.1 goroutine 模型

```text
main goroutine
├── wsclient goroutine    WebSocket 收发
├── collector ticker      每 5 秒采集
└── heartbeat ticker      每 30 秒心跳
```

### 10.2 背压策略

- 采集 ticker 和心跳 ticker 通过 channel 向 wsclient 发送消息。
- channel 容量为 1。
- 如果 channel 满（wsclient 阻塞），采集/心跳跳过当前批次，记日志。
- 不排队积压；优先保证最新指标。

### 10.3 context 取消

- 启动时创建根 `context.Context`。
- 收到信号后 `cancel()`，所有 goroutine 退出。
- wsclient 的 `context` 超时控制首帧认证和写操作。

## 十一、日志规范

### 11.1 日志库

标准库 `log/slog`，JSON handler。

### 11.2 日志级别

| 级别 | 用途 |
|------|------|
| INFO | 连接、鉴权、重连、关闭 |
| DEBUG | 指标摘要、心跳 ack |
| WARN | 背压跳过、采集失败 |
| ERROR | 鉴权失败、连接断开 |

### 11.3 日志脱敏

禁止输出：

- 完整 `agent_token`（只输出前 8 字符）
- 完整 WebSocket URL（如果含 token 参数）
- 密钥、密码、私钥

允许输出：

- `server_id`
- 连接状态
- 指标摘要（cpu_percent、memory_percent 数值）
- 重连次数和等待时间

## 十二、测试矩阵

### 12.1 单元测试

| 模块 | 测试文件 | 场景 |
|------|---------|------|
| config | `config_test.go` | 配置加载、优先级、校验失败 |
| wsclient | `message_test.go` | 消息序列化、字段名、null 处理 |
| collector | `collector_test.go` | mock gopsultil，验证字段映射 |
| reporter | `reporter_test.go` | payload 构造、UUID、时间格式 |

### 12.2 链路冒烟测试

`scripts/smoke-test-ws-agent.ps1`（PowerShell，50 行内）：

1. admin 登录拿 JWT。
2. `POST /api/servers` 创建 server（host=127.0.0.1）。
3. `POST /api/servers/{id}/agent/register` 拿 agent_token。
4. 连接 `ws://localhost:18080/ws/agent`。
5. 首帧 `agent.authenticate`。
6. 收到 `agent.authenticated`。
7. 发一条 `metrics.report`（固定假数据）。
8. 查 MySQL `metrics` 表确认有数据。

### 12.3 本机集成验证

1. 启动后端（test profile + 测试库）。
2. admin 创建 server + 预发放 agent_token。
3. `go run cmd/susumonitor-agent/main.go` 启动 Agent。
4. 观察 Agent 日志：连接成功、鉴权成功、每 5 秒上报。
5. `GET /api/servers/{id}/metrics/latest` 返回真实指标。
6. `GET /api/servers/{id}/metrics` 返回历史记录。
7. 观察 Monitor WebSocket（前端或调试工具）收到 `metrics.update`。

### 12.4 验证标准

| 验证项 | 标准 |
|--------|------|
| Go 编译 | `go build ./...` 无错误 |
| 单元测试 | `go test ./...` 全通过 |
| 链路冒烟 | metrics 表有数据 |
| 本机集成 | `/metrics/latest` 返回真实 CPU/内存/磁盘数据 |
| 日志脱敏 | grep 日志不含完整 token |

## 十三、构建与发布

### 13.1 当前阶段（本机验证）

```powershell
# Windows 本机直接运行
cd agent-go-SuMon
go run cmd/susumonitor-agent/main.go

# 或编译后运行
go build -o bin/susumonitor-agent.exe cmd/susumonitor-agent/main.go
.\bin\susumonitor-agent.exe
```

### 13.2 后续阶段（Linux 生产化）

```bash
# 交叉编译 Linux 二进制
GOOS=linux GOARCH=amd64 go build -o bin/susumonitor-agent cmd/susumonitor-agent/main.go

# 部署到目标 Linux 服务器
scp bin/susumonitor-agent user@server:/usr/local/bin/
# systemd service 配置（后续阶段）
```

### 13.3 不做

- 不做 Docker 镜像（后续阶段）。
- 不做自动升级（后续阶段）。
- 不做多架构交叉编译（后续阶段）。

## 十四、实施阶段划分

### 阶段 0：工程骨架

- 创建 `agent-go-SuMon/` 目录。
- `go mod init`。
- 创建目录结构和空文件。
- 写 `README.md` 和 `.env.example`。

验证：`go build ./...` 通过。

### 阶段 1：配置加载

- 实现 `internal/config/config.go`。
- 配置文件解析 + 环境变量覆盖 + 校验。
- 单元测试。

验证：`go test ./internal/config/` 通过。

### 阶段 2：WebSocket 客户端

- 实现 `internal/wsclient/client.go` 和 `message.go`。
- 连接、首帧鉴权、心跳、重连。
- 消息序列化。
- 单元测试（消息序列化）。

验证：单元测试通过；手动连接后端验证鉴权。

### 阶段 3：指标采集

- 实现 `internal/collector/gopsutil_collector.go`。
- CPU、内存、磁盘、网络采集。
- temperature 和 load_avg 在 Windows 上置 null。
- 单元测试（mock）。

验证：本机采集到真实 CPU/内存/磁盘数据。

### 阶段 4：指标上报

- 实现 `internal/reporter/reporter.go`。
- 构造 `metrics.report` 消息。
- 通过 wsclient 发送。
- 单元测试。

验证：本机跑通完整闭环，`/metrics/latest` 返回真实数据。

### 阶段 5：链路冒烟测试

- 编写 `scripts/smoke-test-ws-agent.ps1`。
- 验证后端链路通。

验证：metrics 表有数据。

## 十五、依赖关系

```text
阶段 0（骨架）
  → 阶段 1（配置）
    → 阶段 2（WebSocket 客户端）
      → 阶段 3（采集）
        → 阶段 4（上报）
          → 阶段 5（冒烟）
```

每个阶段必须通过验证才能进入下一阶段。

## 十六、风险与处理

| 风险 | 处理 |
|------|------|
| gopsultil 在 Windows 上部分指标不可用 | temperature/load_avg 置 null，后端已支持 |
| WebSocket 连接不稳定 | 指数退避重连，不丢数据（跳过积压） |
| 首帧超时 10 秒 | Agent 设 10 秒超时，超时后重连 |
| 消息超过 64 KiB | metrics.report 单条固定宽表，不会超限 |
| agent_token 泄露 | 环境变量读取，日志脱敏，配置文件不入 Git |

## 十七、不纳入本计划的内容

- 前端 `MetricsView.vue` 挂路由与 WebSocket 注入 JWT。
- 后端 `MonitorWebSocketHandler` 推送链路修复（已实现）。
- Bug-fix 4 个 M4 问题修复。
- SSH 测试闭环（Phase A，延后）。
- AI 运维、Android、Docker、Kubernetes、微服务。
