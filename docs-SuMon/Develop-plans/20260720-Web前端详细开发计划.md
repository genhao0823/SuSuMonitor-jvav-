# Web 前端详细开发计划

**日期**: 2026-07-20
**状态**: 主体已执行；M2-M6、Vitest、HTTP/UI E2E 和实时监控已实现，告警与 Web SSH 待对应阶段
**执行范围**: 分阶段完整路线；当前优先完成已具备真实 REST API 的前端闭环
**测试策略**: 单元与组件测试随里程碑同步实施，REST 闭环后执行 Playwright E2E
**前置计划**: [`20260719-Web前端并行开发计划.md`](./20260719-Web前端并行开发计划.md)
**关联后端计划**: [`20260719-MVP-1收口与安全SSH连接测试计划.md`](./20260719-MVP-1收口与安全SSH连接测试计划.md)

## 一、计划目的

本计划以当前仓库、最新 OpenAPI 正式契约和已有运行时验证为基线，接替建立于
`web-vue-SuMon/` 空目录时期的初始前端计划。目标不是一次性堆完所有页面，而是按可验证
的业务闭环推进：先收口认证、路由、布局和类型基线，再完成服务器管理、SSH 安全操作、
管理员审核和自动化验收，最后在对应后端能力及协议正式就绪后扩展实时指标、告警和 Web
SSH 终端。

本计划必须遵循以下原则：

1. `docs-SuMon/OpenApi-SuMon/*.json` 是 REST API 唯一正式契约。
2. 只渲染后端已实现且已形成正式契约的入口，未就绪能力不显示空菜单。
3. 每个里程碑独立完成代码、类型检查、Lint、单元测试、构建和真实浏览器联调。
4. 编译通过、测试通过、浏览器运行时验证和真实外部服务验证分别记录，不互相替代。
5. JWT、密码、SSH 密码、私钥、私钥口令和其他密钥材料不得进入 URL、普通日志、
   控制台、测试输出、开发文档或非必要的持久化存储。
6. 前端权限控制只负责交互和导航，后端鉴权始终是最终安全边界。
7. 优先使用最小、清晰的页面和组件，不提前建立通用 CRUD 框架或推测性抽象。

## 二、当前事实基线

### 2.1 已有前端代码

当前 `web-vue-SuMon/` 已不再是空目录，已有以下能力：

| 能力 | 代码状态 | 已知验证状态 | 本计划结论 |
|---|---|---|---|
| Vue 3 + Vite + TypeScript 工程 | 已实现 | `typecheck`、`lint`、`build` 历史记录通过 | 保留并复验 |
| Vite `/api` 本机代理 | 已实现 | health、ready 代理已有运行时验证 | 保留并复验 |
| axios 统一客户端 | 已实现 | 有代码检查，业务导航边界需修正 | F1-F2 收口 |
| 登录、注册、退出、当前用户 Store | 已实现代码 | 尚缺完整浏览器业务验收 | F1 收口 |
| 路由守卫 | 已实现代码 | 守卫顺序和认证初始化存在缺口 | F1 修正 |
| `MainLayout.vue` | 已创建 | 尚未真正挂入路由树 | F1 修正 |
| Dashboard 基础探针 | 已实现代码 | 真实登录态页面仍需验收 | F1-F2 收口 |
| 服务器 API | 仅列表最小封装 | 完整 CRUD 未实现 | F2-F5 实现 |
| 管理员 API | 已封装 | 审核页面未实现 | F6 实现 |
| 前端自动化测试 | 未建立 | 未验证 | F1 起同步补齐 |

历史验证记录见：

- `docs-SuMon/Develop-log/20260719-Web前端工程初始化.md`
- `docs-SuMon/Develop-log/20260720-Web前端M1dev运行时验证.md`

历史记录只说明对应日期、对应提交状态下的结果。本计划开始执行时仍需重新运行当前代码的
验证命令，不能直接把历史结果记为当前通过。

截至 2026-07-23，以下第 2.1 节和第 2.3 节保留为计划启动时的历史基线；当前实现状态以
`web-vue-SuMon/README.md` 和 `20260722-Web前端总览.md` 为准，不再把其中的“未建立”描述视为现状。

### 2.2 当前可对接的后端接口

| 模块 | 接口 | 前端用途 |
|---|---|---|
| 系统 | `GET /api/health` | 应用健康状态 |
| 系统 | `GET /api/ready` | 数据库就绪状态 |
| 认证 | `POST /api/auth/register` | 注册 |
| 认证 | `POST /api/auth/login` | 登录并获取 Bearer JWT |
| 认证 | `POST /api/auth/logout` | 无状态退出通知 |
| 认证 | `GET /api/auth/me` | 恢复和刷新当前用户状态 |
| 管理员 | `GET /api/admin/users/pending` | 待审核用户列表 |
| 管理员 | `PUT /api/admin/users/{id}/approve` | 审核通过 |
| 管理员 | `PUT /api/admin/users/{id}/reject` | 审核拒绝 |
| 服务器 | `GET /api/servers` | 分页、搜索、排序列表 |
| 服务器 | `POST /api/servers` | 管理员创建服务器 |
| 服务器 | `GET /api/servers/{id}` | 服务器详情 |
| 服务器 | `PUT /api/servers/{id}` | 管理员编辑服务器 |
| 服务器 | `DELETE /api/servers/{id}` | 管理员软删除服务器 |
| 服务器 | `GET /api/servers/{id}/status` | 数据库状态快照 |
| SSH | `PUT /api/servers/{id}/ssh/host-key` | 管理员确认或显式轮换主机指纹 |
| SSH | `POST /api/servers/{id}/ssh/test` | 管理员测试已存凭据和主机身份 |

正式契约来源：

- `docs-SuMon/OpenApi-SuMon/openapi-system.json`
- `docs-SuMon/OpenApi-SuMon/openapi-auth.json`
- `docs-SuMon/OpenApi-SuMon/openapi-server.json`

### 2.3 进入开发前必须修正的缺口

| 优先级 | 缺口 | 影响 | 处理阶段 |
|---|---|---|---|
| P0 | `MainLayout.vue` 未作为受保护路由父布局 | 登录后没有计划中的侧栏和顶栏 | F1 |
| P0 | 管理员判定早于登录判定 | 未登录访问管理员页可能错误进入 403 页 | F1 |
| P0 | 全局把所有 `40300` 跳转为权限页 | pending/rejected 登录错误可能被误当页面权限错误 | F1 |
| P0 | 登录和注册同时绑定 form submit 与 button click | 单击可能造成重复请求 | F1 |
| P1 | 启动时 `/auth/me` 与首个路由导航存在竞态 | 可能短暂使用过期角色或用户信息 | F1 |
| P1 | `openapi-typescript` 已安装但没有生成流程 | 手写类型容易与正式契约漂移 | F2 |
| P1 | SSH 两个后端接口已就绪但旧计划仍排除 | 会遗漏当前已经可交付的管理员能力 | F5 |
| P1 | 没有前端测试框架和测试文件 | 权限、凭据表单和 SSH 流程回归风险高 | F1-F7 |
| P2 | README 仍把项目描述为仅 M1 | 文档状态与代码不一致 | F0、各阶段收尾 |
| P2 | Vite 5.4.21 漏洞风险已有历史记录 | 开发服务器依赖风险未收口 | F0 独立评估 |

## 三、范围与非范围

### 3.1 当前实施范围

- 工程依赖、脚本、类型生成、错误处理和测试基线。
- 登录、注册、退出、当前用户恢复、审核状态提示和路由权限。
- 桌面与移动端主布局。
- Dashboard 健康、就绪、服务器总数和管理员待审核数。
- 服务器列表、搜索、排序、分页、详情、创建、编辑和软删除。
- SSH 主机指纹首次确认、幂等复核、显式轮换和连接测试。
- 管理员待审核用户列表、审核通过和审核拒绝。
- Vitest 单元/组件测试和 Playwright REST 关键路径 E2E。
- README、开发计划状态、开发日志和验证记录同步。

### 3.2 当前明确不实现

- WebSocket 实时指标和 ECharts 图表。
- 专用 `/api/dashboard/summary`；当前 Dashboard 使用既有接口前端聚合。
- 告警规则、告警记录和 `alert.push`。
- xterm.js Web SSH 终端、PTY 和多会话。
- 命令历史。
- 国际化和暗色主题。
- Android、Docker、Kubernetes 和生产部署。
- HttpOnly Cookie 认证改造。

以上能力必须等待第十节所列入口条件满足后另立计划。当前不安装 ECharts、xterm.js 或与
未实现能力相关的状态库。

## 四、技术与安全决策

| 项目 | 当前决策 |
|---|---|
| 框架 | Vue 3.5、`<script setup lang="ts">`、Composition API |
| 构建 | Vite；版本安全升级在 F0 独立验证，不与业务页面混改 |
| 组件库 | Element Plus，保持按需组件注册方向 |
| 路由 | Vue Router 4，受保护页面统一使用 `MainLayout` 父路由 |
| 状态 | Pinia 3；异步认证操作放在 Store action |
| HTTP | axios 单一实例；组件内禁止裸用 axios |
| 类型 | `openapi-typescript` 从三份正式 OpenAPI 生成，生成文件禁止手改 |
| Token | 保留“记住我”：勾选时将 token 和最小用户快照持久化到 `localStorage`，未勾选时仅保留于当前页面内存 |
| 单元测试 | Vitest + `@vue/test-utils`，测试就近放置 `*.spec.ts` |
| E2E | Playwright，连接真实本机后端和隔离测试数据 |
| 响应式 | 至少验证 360px、768px、1280px 三档宽度 |
| 可观测性 | 从响应头读取 `X-Request-ID`；错误详情可显示 request ID |
| 敏感数据 | 不持久化、不记录、不回显密码、私钥和口令；Token 仅按上述白名单策略存储 |

Token 决策说明：后端当前只接受 `Authorization: Bearer <token>`，尚无 HttpOnly Cookie
契约。产品决策保留“记住我”：勾选后使用 `localStorage` 跨刷新和浏览器重启恢复登录；
未勾选时 Token 只保留在当前 Pinia 内存中，刷新或关闭页面后不恢复。axios 必须同时支持
读取当前内存 Token 和已持久化 Token，不能因未勾选“记住我”而使当前页面后续请求丢失
Authorization。`localStorage` 不能抵御 XSS，已接受其较长暴露周期，必须通过依赖审计、
禁止未审计脚本、后端 JWT 过期和权限校验降低风险。若后续采用 HttpOnly Cookie，必须先
修改后端认证、CSRF、CORS 和 OpenAPI 契约，再单独迁移前端，不兼容两套认证机制。

## 五、目标工程结构

```text
web-vue-SuMon/
├── package.json
├── vite.config.ts
├── vitest.config.ts
├── playwright.config.ts
├── e2e/
│   ├── auth.spec.ts
│   ├── server.spec.ts
│   └── admin-review.spec.ts
└── src/
    ├── api/
    │   ├── client.ts
    │   ├── system.ts
    │   ├── auth.ts
    │   ├── admin.ts
    │   └── server.ts
    ├── components/
    │   ├── PageHeader.vue
    │   ├── ServerStatusTag.vue
    │   └── RequestErrorAlert.vue
    ├── features/
    │   └── server/
    │       ├── ServerFormDialog.vue
    │       └── SshSecurityPanel.vue
    ├── layouts/
    │   └── MainLayout.vue
    ├── router/
    │   ├── index.ts
    │   └── guards.ts
    ├── stores/
    │   └── auth.ts
    ├── types/
    │   ├── generated-system.d.ts
    │   ├── generated-auth.d.ts
    │   ├── generated-server.d.ts
    │   └── error-code.ts
    ├── utils/
    │   └── format.ts
    └── views/
        ├── LoginView.vue
        ├── RegisterView.vue
        ├── DashboardView.vue
        ├── ServerListView.vue
        ├── ServerDetailView.vue
        ├── AdminUsersView.vue
        ├── ForbiddenView.vue
        └── NotFoundView.vue
```

目录是目标方向，不要求在 F0 一次性创建空文件。只有对应里程碑确实使用时才创建组件。
不建立通用 Repository、BaseService、通用 CRUD 页面生成器或与当前需求无关的抽象。

## 六、里程碑详细计划

### F0：当前基线与工程风险收口

**预计工期**: 1-2 人日
**目标**: 让文档、依赖、现有代码和验证状态形成可信起点。

任务：

- [ ] 重新执行当前前端 `npm ci`、类型检查、Lint、构建和依赖审计。
- [ ] 启动 Vite，重新验证首页、health、ready 和代理链路。
- [ ] 更新 README 当前功能矩阵，去除“仅 M1”的过时描述。
- [ ] 明确 M1 为历史已验证；M2/M3 只能标为“有代码，待完整运行时验收”。
- [ ] 评估 Vite 当前安全公告和升级路径；如升级，独立修改并重新验证插件兼容性。
- [ ] 确认 `package-lock.json` 与 `package.json` 一致且使用 npm 管理。
- [ ] 检查生成物、环境文件和敏感文件不会进入版本控制。

验证：

```powershell
npm ci
npm run typecheck
npm run lint
npm run build
npm audit
```

运行时验收：

- [ ] `http://127.0.0.1:5173/` 返回 HTTP 200。
- [ ] `/api/health` 返回 HTTP 200、`code=0`。
- [ ] `/api/ready` 返回 HTTP 200、`code=0`、`database=ok`。
- [ ] 验证日志记录实际命令、结果、未覆盖项和后端依赖边界。

出口条件：当前代码的静态检查和运行时基线有新记录；依赖风险要么修复，要么明确记录影响、
临时接受原因和后续处理阶段。

### F1：认证、路由、错误处理与响应式布局

**预计工期**: 2 人日
**目标**: 建立可运行、可刷新、不会错误越权导航的认证闭环。

任务：

- [ ] 将 Dashboard、服务器和管理员页面挂到 `MainLayout` 父路由下。
- [ ] 路由守卫按“认证初始化 → 是否登录 → 是否具备角色 → publicOnly”顺序执行。
- [ ] Store 增加一次性认证初始化状态，首次导航等待 `/api/auth/me` 结束。
- [ ] 保留“记住我”：勾选时只向 `localStorage` 保存 token 和最小用户快照，未勾选时仅保留当前 Pinia 内存会话。
- [ ] axios 在未勾选“记住我”时仍从当前内存认证状态取得 Token，保证当前页面后续请求携带 Authorization。
- [ ] 401 清空本地认证状态并返回登录页，同时保留合法站内 redirect。
- [ ] 不再把所有 403 自动跳转到 `/forbidden`；登录审核状态由登录页解释。
- [ ] 删除登录和注册按钮上的重复 click 提交，只保留 form submit。
- [ ] 防止提交期间重复请求，成功或失败后正确恢复按钮状态。
- [ ] 桌面端使用侧栏，移动端改为抽屉菜单，不固定占用 220px。
- [ ] 完成 admin、approved user、pending、rejected 四种状态的导航和文案。
- [ ] 保持退出接口失败时仍清除本地 token，但准确提示本地退出结果。

Vitest/组件测试：

- [ ] 未登录访问 `/servers` 跳到 `/login?redirect=/servers`。
- [ ] 未登录访问 `/admin/users` 先跳登录而不是 403。
- [ ] approved user 访问管理员路由跳 `/forbidden`。
- [ ] 已登录用户访问 `/login` 或 `/register` 跳 `/dashboard`。
- [ ] 过期或失效 token 返回 401 后 Store 和 `localStorage` 认证项被清空。
- [ ] pending/rejected 登录失败停留登录页并显示审核状态提示。
- [ ] 单击登录或注册按钮只发送一个请求。
- [ ] redirect 只接受站内绝对路径，拒绝外部地址和协议相对地址。

浏览器验收：

- [ ] admin 登录、刷新恢复、菜单展示和退出闭环。
- [ ] approved user 登录后看不到管理员菜单。
- [ ] pending/rejected 用户不能进入受保护页面。
- [ ] 勾选“记住我”后刷新和重新打开浏览器可恢复登录；未勾选时刷新或重新打开页面不恢复登录。
- [ ] 360px、768px、1280px 下菜单、表单和内容区域可用。

出口条件：认证流程和 MainLayout 完成真实浏览器验收，关键守卫分支有自动化测试。

### F2：OpenAPI 类型、API 客户端与 Dashboard 收口

**预计工期**: 1-2 人日
**目标**: 建立可重复生成的契约类型和单一、可追踪的 API 错误边界。

任务：

- [ ] 为 system、auth、server 三份 OpenAPI 增加确定性的类型生成脚本。
- [ ] 生成文件标注“禁止手工编辑”，业务 API 只做最小类型别名和请求封装。
- [ ] 扩展 `server.ts`，覆盖 CRUD、状态、主机指纹和 SSH 测试全部接口。
- [ ] 统一 `ApiBusinessError`，保留业务码、消息、HTTP 状态和 `X-Request-ID`。
- [ ] 网络错误、业务错误和页面业务提示分层，避免拦截器与页面重复弹窗。
- [ ] `X-Correlation-ID` 继续由前端生成，符合后端 1-64 位字符约束。
- [ ] Dashboard 并行加载 health、ready、服务器总数和 admin 待审核数。
- [ ] Dashboard 明确服务器状态是数据库快照，不描述为实时探测。

验证：

```powershell
npm run api:generate
npm run typecheck
npm run lint
npm run test
npm run build
```

自动化验收：

- [ ] 连续生成两次类型文件结果一致。
- [ ] 契约字段变化能通过类型检查暴露前端引用错误。
- [ ] 401、403、40002 和网络错误不会重复提示。
- [ ] 错误详情能显示 request ID，但不包含请求凭据和 Token。
- [ ] Dashboard 对部分探针失败进行局部降级，不导致整页崩溃。

出口条件：手写 API 类型不再是主要契约来源，Dashboard 在 admin 和 approved user 下均完成
真实 API 验收。

### F3：服务器列表

**预计工期**: 2 人日
**目标**: 完成普通用户和管理员都可使用的服务器资产查询入口。

任务：

- [ ] 新增 `ServerListView.vue` 并替换占位路由。
- [ ] 支持 `page`、`page_size`、`keyword`、`sort_by`、`sort_order`。
- [ ] 排序字段限制为 OpenAPI 白名单：`id`、`name`、`host`、`status`、
  `created_at`、`updated_at`。
- [ ] 搜索输入适度防抖，搜索条件变化时页码重置为 1。
- [ ] 查询条件同步到 URL，刷新、前进和后退可恢复列表状态。
- [ ] 展示名称、host、SSH 地址、状态、Agent 状态、最后心跳和更新时间。
- [ ] 区分加载、空列表、请求失败和正常数据四种状态。
- [ ] admin 显示新增、编辑入口，approved user 只显示查看。
- [ ] 桌面使用表格；移动端使用卡片或受控横向滚动的最小列集合。

Vitest/组件测试：

- [ ] 查询条件正确转换为 snake_case 参数。
- [ ] 非法排序字段不会发送到后端。
- [ ] 搜索条件变化后页码回到 1。
- [ ] approved user 不渲染新增、编辑和删除入口。
- [ ] online、offline、unknown 状态标签和空时间格式正确。
- [ ] API 返回中即使意外出现未知字段，页面也不会展示凭据相关内容。

浏览器验收：

- [ ] admin 和 approved user 都能分页、搜索和排序。
- [ ] 非法分页或排序返回 `40002` 时显示统一参数错误。
- [ ] 浏览器前进、后退和刷新能恢复筛选状态。
- [ ] Network 响应和页面 DOM 不包含 password、private_key、passphrase 或密文。

出口条件：服务器列表真实 API 验收通过，角色可见性和查询参数有自动化覆盖。

### F4：服务器创建与编辑

**预计工期**: 2-3 人日
**目标**: 完成管理员安全维护服务器及 SSH 凭据的表单闭环。

任务：

- [ ] 创建 `ServerFormDialog.vue`，在创建和编辑场景复用。
- [ ] 基础字段长度、必填项和 SSH 端口范围严格对齐 OpenAPI。
- [ ] password 模式只提交 `ssh_password`。
- [ ] private_key 模式只提交 `ssh_private_key` 和可选 passphrase。
- [ ] 切换认证类型时清空另一类型凭据，禁止同时提交两种主凭据。
- [ ] 编辑且认证类型不变时允许省略凭据，表示后端保留原密文。
- [ ] 编辑切换认证类型时强制输入新的匹配主凭据。
- [ ] 修改 SSH host 或 port 时提示后端会清除旧主机指纹，需要重新确认。
- [ ] 关闭表单、取消或提交成功后立即清空所有敏感输入。
- [ ] 不把表单内容写入 Store、URL、console、localStorage 或 sessionStorage。

Vitest/组件测试：

- [ ] 创建 password 和 private_key 两种合法组合。
- [ ] 两种主凭据同时存在、空白凭据和越界端口均阻止提交。
- [ ] 编辑不切换认证类型时可省略凭据。
- [ ] 切换认证类型时新主凭据必填。
- [ ] 关闭对话框后密码、私钥和口令字段清空。
- [ ] approved user 不挂载服务器表单组件。

浏览器验收：

- [ ] 创建后列表显示新服务器。
- [ ] 重复 host 返回 `40900` 并显示冲突提示。
- [ ] 编辑基础字段成功，列表和详情同步更新。
- [ ] 两种认证方式切换成功。
- [ ] DevTools Network 中请求只包含当前模式需要的凭据字段。

出口条件：创建和编辑两种认证方式均完成真实 API 验收，敏感字段生命周期有组件测试。

### F5：服务器详情、软删除与 SSH 安全操作

**预计工期**: 3 人日
**目标**: 完成服务器详情和已由后端正式支持的 SSH 安全管理闭环。

任务：

- [ ] 新增 `/servers/:id` 详情路由和 `ServerDetailView.vue`。
- [ ] 并行读取服务器详情与数据库状态快照，分区展示基本信息和状态。
- [ ] admin 显示编辑、软删除、主机指纹和 SSH 测试；approved user 只读。
- [ ] 软删除执行明确二次确认，成功后返回保留查询条件的列表页。
- [ ] 删除后访问详情进入 404 页面，不把软删除误报为权限错误。
- [ ] 新增 `SshSecurityPanel.vue`，明确区分“主机身份确认”和“凭据测试”。
- [ ] 指纹确认要求管理员输入通过可信带外渠道获得的完整 SHA-256 指纹。
- [ ] 禁止提供“扫描后自动信任”或从错误响应一键复制并确认的入口。
- [ ] 主机指纹轮换必须再次确认，并显式发送 `replace=true`。
- [ ] SSH test 不发送请求体，只展示安全响应中的算法、指纹、认证类型、耗时和时间。
- [ ] SSH 失败不得显示后端底层异常、密码、私钥、口令或密文。

SSH 错误交互：

| 错误码 | 前端行为 |
|---|---|
| `40901` | 提示先通过可信渠道核对并登记主机指纹 |
| `40902` | 显示高风险阻断提示，说明可能是主机更换或中间人攻击 |
| `40301` | 提示目标不在后端允许的 SSH 出站范围 |
| `42900` | 提示达到连接并发限制，稍后重试 |
| `50002` | 提示握手或凭据认证失败，不显示底层异常 |
| `50400` | 提示 SSH 操作超时 |
| `40100` | 清除登录态并返回登录页 |
| `40300` | 提示该操作仅管理员可执行 |

Vitest/组件测试：

- [ ] approved user 详情页没有编辑、删除和 SSH 操作入口。
- [ ] 删除二次确认、取消和成功跳转行为正确。
- [ ] 指纹格式必须满足 `SHA256:` 加 43 位 Base64 内容。
- [ ] 首次确认、幂等复核和显式轮换使用正确请求体。
- [ ] 轮换必须经过独立确认，不能默认 `replace=true`。
- [ ] SSH test 请求没有 body。
- [ ] 每个 SSH 专用错误码有独立、准确且脱敏的提示。

真实运行时验收：

- [ ] admin 完成首次指纹确认、幂等复核和显式轮换。
- [ ] password、private_key 和带口令私钥场景按可用测试环境分别验证。
- [ ] 指纹不匹配时 UI 阻止继续测试并显示高风险提示。
- [ ] 出站拒绝、认证失败、超时和并发限制按可控环境验证；无法复现项明确记录。
- [ ] 删除后列表不再显示记录，详情返回 `40400`。

出口条件：服务器详情、软删除和 SSH 管理通过真实后端验收；真实 SSH 验收依赖受控 OpenSSH
环境，组件 Mock 不得替代该层验证。

### F6：管理员用户审核

**预计工期**: 1-2 人日
**目标**: 完成注册用户从 pending 到 approved/rejected 的管理员操作闭环。

任务：

- [ ] 新增 `AdminUsersView.vue` 并替换占位路由。
- [ ] 展示用户名、角色、注册时间和审核状态。
- [ ] approve 和 reject 均需明确确认。
- [ ] 操作期间只禁用当前行，防止重复提交但不阻塞其他行。
- [ ] 成功后从待审核列表局部移除，并更新 Dashboard 待审核数量。
- [ ] `40900` 表示可能已被其他管理员处理，提示后重新加载列表。
- [ ] approved user 不显示菜单，直接访问仍由路由守卫拦截。

Vitest/组件测试：

- [ ] admin 正常加载、空列表和失败状态。
- [ ] approved user 无法进入管理员页面。
- [ ] approve/reject 单击只发送一次请求。
- [ ] 当前行操作期间按钮禁用，其他行仍可用。
- [ ] `40900` 后重新加载列表。

真实业务验收：

- [ ] 第二用户注册后出现在待审核列表。
- [ ] approve 后该用户可以登录。
- [ ] reject 后该用户仍不能登录。
- [ ] 重复审核或并发审核显示冲突提示并刷新。

出口条件：审核通过和拒绝均形成真实注册到登录结果的端到端人工验收。

### F7：REST 闭环 E2E、质量与文档收口

**预计工期**: 2-3 人日
**目标**: 对当前可用 REST 功能建立可重复的关键路径回归基线。

任务：

- [ ] 引入 Playwright 和最小浏览器配置。
- [ ] 使用隔离测试账号和可识别、可清理的测试服务器数据。
- [ ] 测试数据通过预定义 API 辅助动作建立，不直接执行模型生成的数据库命令。
- [ ] 覆盖键盘导航、表单 label、焦点、错误提示和确认对话框可访问性。
- [ ] 检查 360px、768px、1280px 布局。
- [ ] 检查构建产物体积和 Element Plus 样式加载方式，按实际数据决定是否优化。
- [ ] 更新 README、总需求前端状态、当前计划状态和里程碑开发日志。

Playwright 最小关键路径：

1. admin 登录、刷新恢复和退出。
2. pending 用户登录失败并看到审核提示。
3. admin 创建、编辑、查看和软删除服务器。
4. approved user 可查看服务器但没有写操作入口。
5. admin 审核用户，用户随后可以登录。
6. admin 确认 SSH 指纹并执行连接测试。
7. 失效 token 返回登录页并清除本地状态。
8. 未登录深链访问在登录后返回原站内目标。

总验收命令：

```powershell
npm ci
npm run api:generate
npm run typecheck
npm run lint
npm run test
npm run build
npm run e2e
npm audit
```

出口条件：所有命令记录实际退出码和结果；E2E 失败不能以人工手测替代后标记通过；依赖真实
SSH 或特殊故障环境而未覆盖的场景必须单独列为未验证。

## 七、测试分层和数据策略

| 层级 | 验证对象 | 是否依赖后端 | 不能替代的层级 |
|---|---|---|---|
| TypeScript | 类型、OpenAPI 引用、组件编译 | 否 | 单元、浏览器、真实 API |
| ESLint | 代码规范和部分静态缺陷 | 否 | 类型和运行时 |
| Vitest 单元测试 | Store、守卫、格式化、错误映射 | 否 | 组件交互、真实 API |
| Vue 组件测试 | 表单、角色可见性、按钮状态 | 使用 Mock | 真实后端和浏览器 E2E |
| Playwright E2E | 浏览器关键路径 | 是 | 真实 SSH 故障场景、部署验证 |
| 真实 SSH 验收 | 主机身份、凭据、网络和超时 | 是 | 生产部署验证 |

测试数据约束：

- 使用专用前缀标识测试用户和服务器，避免误操作开发数据。
- Token、密码和 SSH 凭据只通过环境变量、交互式输入或测试运行时内存注入。
- Playwright trace、截图和视频不得录入密码或私钥输入过程；必要时关闭对应失败附件。
- 服务器软删除属于业务验证，执行前确认目标是测试数据；不得清理非测试记录。
- 任何数据库批量删除、TRUNCATE、DROP 或迁移历史重置均不属于前端测试动作。

## 八、文档和交付规则

每个里程碑遵循固定闭环：

```text
核对 OpenAPI
→ 编写或生成类型
→ 编写关键失败测试
→ 实现功能
→ typecheck
→ lint
→ Vitest
→ build
→ 浏览器真实 API 验收
→ 更新开发日志和计划状态
```

每个里程碑建立独立开发日志：

```text
docs-SuMon/Develop-log/YYYYMMDD-Web前端F{N}主题.md
```

日志至少包含：

- 日期、操作人、关联计划。
- 已创建、已修改和已删除文件；如无删除明确写“无”。
- 已实现能力和未实现能力。
- 类型检查、Lint、测试、构建和运行时命令及实际结果。
- 真实后端、真实 SSH、部署等依赖边界。
- 已知问题、风险和下一阶段入口条件。

修改 REST 字段时必须先更新 OpenAPI，再更新生成类型、API 封装、页面、测试和调试样例。
修改 WebSocket 消息时必须先建立 `docs-SuMon/Protocol-SuMon/` 正式协议文档。

## 九、工期、依赖与执行顺序

| 阶段 | 预计人日 | 主要依赖 |
|---|---:|---|
| F0 基线收口 | 1-2 | 当前工作区、npm registry、运行中的本机后端 |
| F1 认证与布局 | 2 | auth OpenAPI、真实认证接口 |
| F2 类型与客户端 | 1-2 | 三份正式 OpenAPI |
| F3 服务器列表 | 2 | server list API |
| F4 创建与编辑 | 2-3 | server write API、管理员账号 |
| F5 详情、删除与 SSH | 3 | server/SSH API、受控 OpenSSH 环境 |
| F6 用户审核 | 1-2 | admin API、可创建的测试用户 |
| F7 E2E 收口 | 2-3 | 稳定后端、隔离测试数据、浏览器环境 |

预计总工期为 **14-19 人日**。工期是规划估算，不代表已执行时间。必须按 F0 → F1 → F2
→ F3 → F4 → F5 → F6 → F7 顺序推进；同一阶段内测试与实现同步，不把测试整体推迟到
最后。如果 Vite 主版本升级出现插件兼容问题，应独立处理并重新估算，不挤占业务里程碑验收。

## 十、后续完整路线和入口条件

| 后续阶段 | 必须满足的入口条件 | 计划中的前端交付 |
|---|---|---|
| 实时指标 | 最新/历史指标 OpenAPI、WebSocket 正式协议和真实后端均就绪 | ECharts、实时订阅、断线恢复、历史区间查询 |
| Dashboard 汇总 | `/api/dashboard/summary` 正式契约和实现通过验证 | 汇总卡片、状态分布、最近告警 |
| 告警 | 告警 OpenAPI、`alert.push` 协议和权限矩阵冻结 | 告警规则、记录、筛选、已读和实时推送 |
| Web SSH | PTY、多会话、resize、close、超时协议和后端真实 SSH 验证完成 | xterm.js、多标签会话、关闭确认和超时处理 |
| 国际化 | MVP 核心功能稳定且有明确多语言需求 | vue-i18n 和模块化语言文件 |
| 生产部署 | 发布路径、反向代理、API 域名和部署平台确定 | CSP、缓存、压缩、history fallback 和部署验证 |

入口条件未满足前，前端不得展示对应菜单，不用 Mock 页面伪装为已实现，也不得把静态演示记为
运行时验证通过。

## 十一、主要风险与处理

| 风险 | 影响 | 处理 |
|---|---|---|
| OpenAPI 与实现漂移 | 类型正确但运行时失败 | 生成类型、真实 API 验收和后端漂移检查共同约束 |
| localStorage 受 XSS 且跨浏览器会话保留 | JWT 泄漏与暴露周期延长 | 仅白名单保存 token/最小用户快照，不引入未审计脚本，401/退出立即清理，后端控制 JWT 有效期，部署阶段启用严格 CSP |
| 全局错误处理过度导航 | 登录或局部业务错误被错误重定向 | 仅 401 全局清理，403 由路由和页面上下文处理 |
| 敏感表单残留 | 密码或私钥进入内存快照、日志或附件 | 关闭即清空，不持久化，不记录，E2E 限制 trace |
| SSH 指纹交互误导 | 管理员错误信任攻击者主机 | 强制带外核对，禁止自动信任，轮换二次确认 |
| 固定侧栏不适配移动端 | 手机无法使用 | F1 引入抽屉菜单并验证三档宽度 |
| 测试数据误删 | 影响开发资产 | 测试前缀、目标确认、只通过业务软删除测试记录 |
| Vite 依赖漏洞 | 开发服务暴露风险 | F0 独立评估升级并记录 `npm audit` 实际结果 |
| E2E 依赖真实环境 | 用例不稳定 | 固定本机后端、隔离数据、明确重试边界，不掩盖真实失败 |
| 后端后续字段变更 | 重复返工 | 先冻结并更新 OpenAPI，再开发对应页面 |

## 十二、总完成标准

只有同时满足以下条件，当前 REST 前端闭环才能标记完成：

- [x] F0-F7 的主体任务和各阶段结果已有开发记录。
- [x] 类型检查、Lint、Vitest 和生产构建通过；HTTP/UI E2E 有历史运行记录。
- [x] admin、approved user、pending、rejected 四类认证状态有真实 HTTP/UI 验收记录。
- [x] 服务器列表、详情、创建、编辑和软删除完成真实 API 验收。
- [ ] 主机指纹确认、显式轮换和 SSH test 的当前受控 WSL `50003` 分类仍未完成。
- [x] 管理员审核通过和拒绝完成注册到登录结果的业务闭环验收。
- [x] 页面和响应不暴露 JWT、密码、私钥、口令或凭据密文。
- [x] 关键页面已有桌面和移动视口 UI E2E 记录；具体尺寸结果以对应日志为准。
- [x] README、当前计划、总需求状态和开发日志已于 2026-07-23 收口。
- [x] 实时指标已实现；未实现的告警和 Web SSH 终端没有入口，也没有被误报为完成。
- [x] 所有未验证项、外部依赖和残余风险均明确列出。

本计划只定义后续执行目标和验收方法。文档创建不代表上述功能已实现、已测试或已通过运行时
验证；实际状态必须以对应阶段的开发日志和直接验证输出为准。
