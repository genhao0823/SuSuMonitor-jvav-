<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.susumonitor.server.module.metrics.mapper.MetricsCleanupMapper">

    <delete id="deleteExpiredBatch">
        DELETE FROM metrics
        WHERE id IN (
            SELECT expired_metrics.id
            FROM (
                SELECT id
                FROM metrics
                WHERE collected_at &lt; #{cutoffTime}
                ORDER BY id
                LIMIT #{batchSize}
            ) expired_metrics
        )
    </delete>
</mapper>
