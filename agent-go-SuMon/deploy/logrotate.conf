# SuSuMonitor Agent 日志轮转配置。
#
# 部署路径：/etc/logrotate.d/susumonitor-agent
# 由 logrotate 守护进程自动加载，不需要重启 Agent。

/var/log/susumonitor/agent.log {
    daily
    rotate 7
    compress
    delaycompress
    missingok
    notifempty
    copytruncate
}
