# SuSuMonitor 项目需求与规范汇总

**整理日期**: 2026-07-12  
**当前状态**: 当前阶段需求与规范汇总  
**适用范围**: 本文档只整理当前已确定、已在项目文档或用户补充中明确的内容；未确认内容不作为当前开发目标。

## 一、项目定位

SuSuMonitor 是一个 Linux 服务器性能监控平台，目标是通过 Web 页面和后续 Android App 实现服务器监控、告警、命令执行和 SSH 终端能力。

当前开发策略采用 MVP 优先：先完成可运行、可验证的核心闭环，再逐步加入增强能力。

## 二、当前开发与调试范围

### 2.1 当前执行范围

- 项目完整需求仍包含多主机监控、Agent、Web、Android、告警、SSH 终端、Docker/k8s/Helm、Prometheus/Grafana、GitHub Actions 等规划内容。
- 当前仅限定开发和调试在当前主机上进行。
- 当前调试对象只有当前主机，不代表项目最终只支持单主机。
- 当前阶段可使用 `localhost` 或 `127.0.0.1` 完成本机联调。
- 文档或代码中涉及通用服务器地址示例时，继续统一使用 `SERVER_IP_OR_DOMAIN`，不写真实公网 IP 或内网 IP。

### 2.2 当前开发环境

| 项 | 当前约定 |
|----|---------|
| 开发机器 | 当前 Windows 主机 |
| 后端运行 | 当前主机本地运行 |
| 数据库 | 当前主机 MySQL 8.4，服务名 `MySQL84` |
| 数据库地址 | `127.0.0.1:3306` |
| 数据库名 | `susumonitor` |
| 数据库用户 | `susumonitor` |
| 后端端口 | `18080` |
| Docker | 当前未检测到，MVP-1 不依赖 Docker |

### 2.3 当前不作为本机调试前置条件

- 远程主机部署不是当前本机开发和调试的前置条件，但仍属于项目后续部署和验证需求。
- 多 Agent、多服务器真实部署验证不是当前本机调试的前置条件，但项目设计仍按多服务器监控平台保留。
- Docker、Docker Compose、Kubernetes、Helm 当前不作为本机 MVP-1 调试依赖，但仍属于增强阶段需求。
- Redis 当前不作为 MVP-1 调试依赖，但仍属于增强阶段需求。
- Android App 当前不作为本机后端开发前置条件，但仍属于增强阶段需求。
- Prometheus、Grafana、GitHub Actions 当前不作为本机调试前置条件，但仍属于增强阶段需求。
- 命令增强、审计增强、复杂权限体系当前不作为 MVP-1 前置条件，但仍属于后续增强方向。

## 三、MVP 总体需求

MVP 目标是完成核心闭环：用户登录、服务器管理、Agent 指标采集、实时监控、告警、Web SSH 终端。

当前文档中的 MVP 阶段划分如下：

| 阶段 | 内容 |
|------|------|
| MVP-1 | 后端基础：Java 后端新建、MySQL 建表、JWT 登录注册、服务器 CRUD、SSH 凭据加密存储 |
| MVP-2 | Agent 开发：指标采集、5 秒采集频率、WebSocket 连接、心跳、注册校验 |
| MVP-3 | 后端实时层：指标接收、MySQL 存储、WebSocket 推送、历史指标查询、10 天数据清理 |
| MVP-4 | 接口文档校验：补全、校验和整理 OpenAPI 文档 |
| MVP-5 | Web 前端：登录、仪表盘、服务器列表、服务器详情、告警页面、SSH 终端页面，直接对接真实 API |
| MVP-6 | 告警系统：阈值规则、告警记录、WebSocket 告警推送 |
| MVP-7 | Web SSH 终端：中心服务器 SSH 代理、PTY、多会话、20 分钟超时、xterm.js 对接 |
| MVP-8 | 基础部署文档：后端启动、Agent 安装、Web 启动说明 |

## 四、当前已明确的后端需求

后端计划使用 Java 重新实现。以下内容作为新后端需要满足的业务和接口需求，不绑定旧实现。

| 模块 | 需求 |
|------|------|
| 后端启动 | 支持本机启动和本机调试 |
| 配置加载 | 支持本机环境配置、数据库配置、JWT 配置、SSH 加密配置、Agent 注册密钥配置 |
| 日志 | 支持统一请求日志，并记录 `request_id` |
| request_id | HTTP 请求生成 `X-Request-ID` 并写入响应 header |
| 统一响应 | 支持成功响应、错误响应和分页响应 |
| 错误码 | 覆盖认证、参数、资源、数据库、SSH、Agent 等错误场景 |
| 健康检查 | 提供 `GET /api/health` |
| 就绪检查 | 提供 `GET /api/ready`，检查数据库连通性 |
| 数据库初始化 | 连接本机 MySQL，并维护基础表结构 |
| 用户认证 | 支持注册、登录、退出、当前用户信息 |
| 密码哈希 | 密码只存储哈希，不明文存储 |
| JWT | 支持签发、校验、过期时间配置、认证中间件 |
| 用户审核 | 首个用户自动管理员并通过，后续用户需管理员审核 |
| 管理员审核接口 | 支持待审核列表、审核通过、审核拒绝 |

MVP-1 新 Java 后端需要完成：

- 服务器 CRUD。
- SSH 凭据字段校验。
- AES-256-GCM 工具，用于加密 SSH 密码、私钥和私钥口令。
- SSH 连接测试接口 `POST /api/servers/:id/ssh/test`。
- 定时任务框架。
- metrics 清理任务骨架和配置项。
- API 调试文件与 OpenAPI 文档同步维护。

## 五、后端 API 需求

### 5.1 通用响应格式

所有 REST API 使用统一 JSON 响应结构。

成功响应：

```json
{
  "code": 0,
  "message": "success",
  "data": {}
}
```

分页响应：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "items": [],
    "total": 100,
    "page": 1,
    "page_size": 20
  }
}
```

错误响应：

```json
{
  "code": 40000,
  "message": "bad request"
}
```

### 5.2 统一错误码

| 错误码 | 含义 |
|--------|------|
| 0 | success |
| 40000 | bad request |
| 40001 | invalid username or password |
| 40002 | invalid request parameter |
| 40100 | unauthorized |
| 40300 | forbidden |
| 40400 | resource not found |
| 40900 | resource conflict |
| 50000 | internal server error |
| 50001 | database error |
| 50002 | ssh connection failed |
| 50003 | agent offline |

### 5.3 request_id 规范

- 每个 HTTP 请求由后端生成 `request_id`。
- 响应 header 返回 `X-Request-ID`。
- 应用日志记录 `request_id`，用于联调和排障。
- WebSocket 消息使用消息体中的 `request_id` 字段。

### 5.4 列表查询规范

列表接口统一支持以下查询参数：

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| page | integer | 否 | 页码，默认 1 |
| page_size | integer | 否 | 每页数量，默认 20，最大 100 |
| keyword | string | 否 | 搜索关键词 |
| sort_by | string | 否 | 排序字段 |
| sort_order | string | 否 | 排序方向：`asc`、`desc` |

### 5.5 当前已明确接口

| 模块 | 接口 | 说明 | 当前阶段 |
|------|------|------|---------|
| 系统 | `GET /api/health` | 健康检查 | 已实现 |
| 系统 | `GET /api/ready` | 就绪检查，检查数据库 | 已实现 |
| 认证 | `POST /api/auth/register` | 用户注册 | 已实现 |
| 认证 | `POST /api/auth/login` | 用户登录，返回 JWT | 已实现 |
| 认证 | `POST /api/auth/logout` | 用户退出登录 | 已实现 |
| 认证 | `GET /api/auth/me` | 当前用户信息 | 已实现 |
| 管理员 | `GET /api/admin/users/pending` | 待审核用户列表 | 已实现 |
| 管理员 | `PUT /api/admin/users/{id}/approve` | 审核通过用户 | 已实现 |
| 管理员 | `PUT /api/admin/users/{id}/reject` | 审核拒绝用户 | 已实现 |
| 服务器 | `GET /api/servers` | 查询服务器列表 | MVP-1 后续 |
| 服务器 | `POST /api/servers` | 新增服务器 | MVP-1 后续 |
| 服务器 | `GET /api/servers/:id` | 查询服务器详情 | MVP-1 后续 |
| 服务器 | `PUT /api/servers/:id` | 更新服务器 | MVP-1 后续 |
| 服务器 | `DELETE /api/servers/:id` | 删除服务器 | MVP-1 后续 |
| 服务器 | `GET /api/servers/:id/status` | 查询服务器在线状态 | MVP-1 后续 |
| SSH | `POST /api/servers/:id/ssh/test` | 测试 SSH 连接 | MVP-1 后续 |
| 指标 | `GET /api/servers/:id/metrics/latest` | 查询最新指标 | MVP-3 |
| 指标 | `GET /api/servers/:id/metrics` | 查询历史指标 | MVP-3 |
| 仪表盘 | `GET /api/dashboard/summary` | 查询仪表盘汇总数据 | MVP-3/MVP-5 |
| 告警 | `/api/alerts/rules` | 告警规则管理 | MVP-6 |
| 告警 | `/api/alerts/records` | 告警记录查询和已读 | MVP-6 |

## 六、认证与用户需求

| 项 | 规则 |
|----|------|
| username | 3-50 位，允许字母、数字、下划线 |
| password | 8-64 位 |
| password_hash | 使用 bcrypt 存储 |
| JWT 有效期 | 默认 24 小时，通过配置项调整 |
| 首个用户 | 自动成为 `admin/approved` |
| 后续注册用户 | 默认为 `user/pending`，需要管理员审核 |
| 登录限制 | 只有 `approved` 用户可以登录 |
| pending/rejected 用户 | 登录返回 403 |
| logout | MVP 阶段服务端无状态退出，前端删除 Token |

## 七、服务器与 SSH 需求

### 7.1 服务器管理字段

新增服务器时已明确字段如下：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| name | string | 是 | 服务器名称 |
| host | string | 是 | 服务器地址；当前阶段表示当前主机 |
| description | string | 否 | 备注说明 |
| ssh_host | string | 是 | SSH 地址 |
| ssh_port | integer | 是 | SSH 端口，默认 22 |
| ssh_user | string | 是 | SSH 用户名 |
| ssh_auth_type | string | 是 | SSH 认证方式：`password` 或 `private_key` |
| ssh_password | string | 否 | SSH 密码，提交后加密存储 |
| ssh_private_key | string | 否 | SSH 私钥，提交后加密存储 |
| ssh_private_key_passphrase | string | 否 | SSH 私钥口令，提交后加密存储 |

### 7.2 SSH 凭据存储

| 字段 | 存储方式 |
|------|---------|
| ssh_host | 明文 |
| ssh_port | 明文 |
| ssh_user | 明文 |
| ssh_auth_type | 明文 |
| ssh_password_encrypted | AES-256-GCM 加密 |
| ssh_private_key_encrypted | AES-256-GCM 加密 |
| ssh_private_key_passphrase_encrypted | AES-256-GCM 加密 |

### 7.3 SSH 会话规则

- Web 通过 WebSocket 发送 `ssh.open` 创建 SSH 会话。
- 中心服务器生成 `session_id`。
- 同一用户可同时打开多个 SSH 会话。
- 输入输出通过 WebSocket `ssh.input` 和 `ssh.output` 转发。
- xterm.js resize 时发送 `ssh.resize`。
- 20 分钟无输入自动断开。
- Web 可发送 `ssh.close` 或调用 REST 删除会话主动关闭。
- MVP 不记录终端输出内容。
- MVP 只记录会话打开、关闭、超时和错误状态。

## 八、Agent 与指标需求

当前 Agent 是 MVP-2 目标。项目仍保留多主机 Agent 监控需求；当前只是先在本机完成开发和调试。

已明确的 Agent 规则如下：

| 项 | 规则 |
|----|------|
| Agent 权限 | 需要 root 权限，用于读取温度等指标 |
| 注册密钥 | 全局密钥，配置在 `AGENT_REGISTER_KEY` |
| 注册方式 | 首次连接发送 `agent.register`，携带 `register_key` 和 `host` |
| 服务器关联 | 中心服务器通过 `host` 匹配 servers 表预建记录 |
| 后续连接 | 使用 `agent_id + agent_token` |
| Token 存储 | 中心服务器只存 `agent_token_hash` |
| 指标频率 | 每 5 秒上报一次 |
| 心跳频率 | 每 30 秒发送一次 |
| 在线判定 | WebSocket 鉴权成功后立即 online |
| 离线判定 | 超过 90 秒未收到心跳或指标置为 offline |
| 离线扫描 | 后端每 30 秒扫描 |
| 初始重连间隔 | 5 秒 |
| 最大重连间隔 | 60 秒 |
| 重连策略 | 指数退避，成功连接后重置 |

指标字段：

| 字段 | 说明 |
|------|------|
| cpu_percent | CPU 使用率 |
| memory_percent | 内存使用率 |
| memory_used | 已用内存字节数 |
| memory_total | 总内存字节数 |
| disk_percent | 磁盘使用率 |
| disk_used | 已用磁盘字节数 |
| disk_total | 总磁盘字节数 |
| net_rx | 网络接收字节数 |
| net_tx | 网络发送字节数 |
| temperature | 温度 |
| load_avg | 系统负载 |
| collected_at | 采集时间 |

## 九、告警需求

告警系统属于 MVP-6，已明确需求如下：

| 项 | 规则 |
|----|------|
| 告警规则指标 | `cpu`、`memory`、`disk`、`temperature`、`load` |
| 操作符 | `>`、`>=`、`<`、`<=` |
| 告警等级 | `warning`、`critical` |
| 是否启用 | `enabled` 控制 |
| 告警检测频率 | 每 10 秒 |
| 告警推送 | 通过 WebSocket `alert.push` 推送 |
| 告警记录 | 支持查询和标记已读 |

## 十、数据表与数据保留

### 10.1 当前基础表

后端需要维护以下基础表：

```text
users
servers
metrics
commands
alert_rules
alert_records
ssh_sessions
```

### 10.2 核心表用途

| 表名 | 用途 |
|------|------|
| users | 用户表 |
| servers | 服务器表，包含 Agent 信息和 SSH 凭据密文 |
| metrics | 指标数据 |
| commands | 命令记录，增强阶段使用 |
| alert_rules | 告警规则 |
| alert_records | 告警记录 |
| ssh_sessions | SSH 会话生命周期记录 |

### 10.3 数据保留策略

| 清理对象 | 保留时间 | 执行频率 | 当前阶段 |
|----------|----------|---------|---------|
| metrics | 10 天 | 每天 03:00 | MVP-3 启用，MVP-1 先做骨架 |
| commands | 10 天 | 增强阶段 E2 | 后续 |
| closed ssh_sessions | 30 天 | 每天 03:20 | 可后置 |
| read alert_records | 30 天 | 每天 03:30 | 可后置 |

## 十一、Web 前端需求

Web 前端属于 MVP-5，技术栈和页面已明确。

### 11.1 技术栈

- Vue 3。
- Vite。
- Element Plus。
- ECharts。
- xterm.js。
- 前端直接对接真实 API，不使用 mock。

### 11.2 页面清单

| 页面 | 路由 | 核心内容 |
|------|------|---------|
| 登录 | `/login` | 用户名、密码 |
| 注册 | `/register` | 用户名、密码、确认密码 |
| 仪表盘 | `/dashboard` | 统计卡片、服务器状态列表、最近告警 |
| 服务器列表 | `/servers` | 名称/IP/CPU/内存/温度、搜索、添加 |
| 服务器详情 | `/servers/:id` | 基本信息、实时图表、终端入口 |
| 告警规则 | `/alerts/rules` | 规则表格、添加弹窗 |
| 告警记录 | `/alerts/records` | 记录表格、筛选 |
| SSH 终端 | `/servers/:id/terminal` | xterm.js 多会话终端 |
| 命令历史 | `/commands` | 增强阶段页面 |

## 十二、WebSocket 协议规范

### 12.1 通用消息结构

所有 WebSocket 消息统一使用 JSON 格式。

```json
{
  "type": "metrics.report",
  "request_id": "uuid",
  "timestamp": 1710000000000,
  "payload": {}
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| type | string | 是 | 消息类型 |
| request_id | string | 是 | 请求 ID，用于请求响应关联 |
| timestamp | integer | 是 | 毫秒时间戳 |
| payload | object | 是 | 业务数据 |

### 12.2 WebSocket 地址

| 地址 | 用途 | 鉴权 |
|------|------|------|
| `/api/ws/agent` | Agent 注册、心跳、指标上报、命令结果回传 | Agent 注册密钥或 Agent Token |
| `/api/ws/client` | Web 实时指标订阅、告警推送、SSH 终端 | JWT |

### 12.3 MVP 消息类型

| 消息类型 | 方向 | 说明 |
|---------|------|------|
| `agent.register` / `agent.registered` | Agent 与中心服务器 | Agent 注册 |
| `agent.heartbeat` | Agent 到中心服务器 | Agent 心跳 |
| `metrics.report` | Agent 到中心服务器 | 指标上报 |
| `metrics.subscribe` / `metrics.unsubscribe` | Web 到中心服务器 | 订阅/取消订阅指标 |
| `metrics.update` | 中心服务器到 Web | 实时指标推送 |
| `alert.push` | 中心服务器到 Web | 告警推送 |
| `ssh.open` / `ssh.opened` | Web 与中心服务器 | SSH 会话创建 |
| `ssh.input` / `ssh.output` | Web 与中心服务器 | SSH 输入输出 |
| `ssh.resize` / `ssh.close` | Web 到中心服务器 | SSH 窗口调整/关闭 |
| `ssh.error` | 中心服务器到 Web | SSH 错误 |
| `error` | 双向 | 通用错误 |

增强阶段消息：`command.execute`、`command.result`。

## 十三、后端实现方向

- 后端计划使用 Java 新建实现。
- 新 Java 后端需要满足本文档中的业务需求、API 需求、数据库需求和 WebSocket 协议需求。
- 旧后端实现细节不作为新后端约束。
- 当前只在本机开发和调试新后端。

## 十四、Java 后端技术栈

新 Java 后端计划使用以下技术栈。当前优先满足本机开发、本机调试和 MVP 后端功能闭环。

| 层级 | 技术 | 用途 |
|------|------|------|
| 开发语言 | Java 21 LTS | 后端主语言 |
| 后端框架 | Spring Boot 3.x | 应用启动、依赖注入、配置管理 |
| Web 框架 | Spring Web MVC | REST API、健康检查、就绪检查 |
| 参数校验 | Hibernate Validator | 请求参数校验、字段长度校验、必填校验 |
| 数据访问 | MyBatis-Plus | MySQL CRUD、分页查询、条件查询 |
| 数据库 | MySQL 8.4 | 当前本机开发数据库和后续主数据库 |
| 数据库连接池 | HikariCP | 数据库连接池 |
| 数据库迁移 | Flyway | 表结构初始化和版本化变更管理 |
| 认证鉴权 | Spring Security | 登录态校验、接口鉴权、管理员权限控制 |
| Token | JWT | 登录后签发 Token，接口使用 Bearer Token 鉴权 |
| 密码哈希 | BCrypt | 用户密码哈希存储 |
| SSH 凭据加密 | JDK JCA AES-GCM | SSH 密码、私钥、私钥口令加密存储 |
| WebSocket | Spring WebSocket | Agent 连接、指标推送、告警推送、SSH 终端消息 |
| SSH 客户端 | sshj | 中心服务器发起 SSH 连接、创建 PTY 会话 |
| 定时任务 | Spring Scheduling | Agent 离线检测、指标清理、告警检测 |
| JSON 序列化 | Jackson | REST API 和 WebSocket JSON 消息处理 |
| API 文档 | springdoc-openapi | 生成和维护 OpenAPI 3 文档 |
| 日志门面 | SLF4J | 统一日志接口 |
| 日志实现 | Logback | 本机开发和运行日志输出 |
| 构建工具 | Maven | 依赖管理、构建、测试 |
| 单元测试 | JUnit 5 | 单元测试和基础集成测试 |
| Mock 测试 | Mockito | Service、组件隔离测试 |

增强阶段可加入以下技术：

| 技术 | 用途 |
|------|------|
| Spring Data Redis | 指标缓存、JWT 黑名单、API 限流、Agent 在线状态缓存 |
| Micrometer | 应用指标采集 |
| Prometheus Registry | 暴露 Prometheus 指标 |
| Testcontainers | Docker 可用后做 MySQL、Redis 集成测试 |
| Quartz | 如果 Spring Scheduling 无法满足复杂调度，再引入 |

Java 后端约束：

- REST API 路径、响应结构、错误码与本文档保持一致。
- WebSocket 消息结构与 `docs-SuMon/Protocol-SuMon/websocket-protocol.md` 保持一致。
- SQL 查询禁止使用 `SELECT *`，需要明确字段。
- 密码不得明文存储，必须使用 BCrypt。
- SSH 敏感凭据不得明文存储，必须使用 AES-GCM 加密。
- 日志中不得输出密码、Token、SSH 私钥、SSH 密码、AES 密钥等敏感信息。
- 当前本机开发阶段不强制依赖 Docker。

## 十五、Java 改造后的项目结构与配置细节

### 15.1 目标根目录结构

Java 改造完成后的项目只保留当前阶段需要的正式目录、配置、脚本和文档。不再需要的旧后端目录、旧构建产物、旧缓存目录不纳入目标结构。

```text
SuSuMonitor/
├── .editorconfig
├── .gitattributes
├── .gitignore
├── .dockerignore
├── api-test/
├── docs-SuMon/
├── scripts/
├── server-java-SuMon/
├── web-vue-SuMon/
├── app-kt-SuMon/
├── 项目需求与规范.md
└── 远程Linux主机部署配置清单.md
```

目标根目录说明：

| 路径 | 作用 | 当前阶段 |
|------|------|---------|
| `.editorconfig` | 统一编辑器格式、缩进、换行、编码规则 | 需要保留 |
| `.gitattributes` | 统一 Git 换行符处理规则 | 需要保留 |
| `.gitignore` | 统一 Git 忽略规则，避免提交本机配置、密钥、构建产物、依赖目录 | 需要保留 |
| `.dockerignore` | Docker 构建上下文忽略规则，容器化阶段使用 | 增强阶段使用 |
| `api-test/` | 本机 HTTP API 调试文件目录 | 当前需要 |
| `docs-SuMon/` | 项目正式文档目录 | 当前需要 |
| `scripts/` | 数据库初始化、本机辅助脚本目录 | 当前需要 |
| `server-java-SuMon/` | Java 后端工程目录 | 当前需要新建 |
| `web-vue-SuMon/` | Vue 3 Web 前端工程目录 | MVP-5 使用 |
| `app-kt-SuMon/` | Android App 工程目录 | 增强阶段使用 |
| `项目需求与规范.md` | 项目需求、规范、结构、配置汇总文档 | 当前需要持续维护 |
| `远程Linux主机部署配置清单.md` | 后续远程部署配置参考 | 后续部署阶段使用 |

### 15.2 Java 后端工程结构

Java 后端工程目录固定为：

```text
server-java-SuMon/
```

目标结构：

```text
server-java-SuMon/
├── pom.xml
├── README.md
├── .env.example
├── src/
│   ├── main/
│   │   ├── java/
│   │   │   └── com/
│   │   │       └── susumonitor/
│   │   │           └── server/
│   │   │               ├── SuSuMonitorServerApplication.java
│   │   │               ├── config/
│   │   │               ├── common/
│   │   │               ├── security/
│   │   │               ├── module/
│   │   │               ├── websocket/
│   │   │               ├── ssh/
│   │   │               └── scheduler/
│   │   └── resources/
│   │       ├── application.yml
│   │       ├── application-local.yml
│   │       ├── logback-spring.xml
│   │       └── db/
│   │           └── migration/
│   └── test/
│       └── java/
└── target/
```

Java 后端文件和目录作用：

| 路径 | 作用 |
|------|------|
| `pom.xml` | Maven 项目配置，管理依赖、插件、构建、测试 |
| `README.md` | Java 后端启动、配置、测试、接口调试说明 |
| `.env.example` | 本机环境变量示例，不包含真实密钥 |
| `src/main/java/.../SuSuMonitorServerApplication.java` | Spring Boot 启动入口 |
| `src/main/java/.../config/` | 配置类目录，例如 Web、跨域、Jackson、OpenAPI、WebSocket、加密配置 |
| `src/main/java/.../common/` | 通用能力目录，例如统一响应、错误码、异常处理、分页对象、request_id 工具 |
| `src/main/java/.../security/` | Spring Security、JWT、登录认证、管理员权限、密码哈希 |
| `src/main/java/.../module/` | 业务模块根目录 |
| `src/main/java/.../websocket/` | WebSocket 连接、消息编解码、Agent 通道、Web 客户端通道 |
| `src/main/java/.../ssh/` | SSH 连接测试、PTY 会话、终端输入输出转发、会话关闭 |
| `src/main/java/.../scheduler/` | 定时任务，例如 Agent 离线检测、metrics 清理、告警检测 |
| `src/main/resources/application.yml` | 默认配置文件，保存通用配置和 profile 配置 |
| `src/main/resources/application-local.yml` | 本机开发配置文件，连接本机 MySQL |
| `src/main/resources/logback-spring.xml` | 日志格式、日志级别、控制台和文件输出配置 |
| `src/main/resources/db/migration/` | Flyway 数据库迁移脚本目录 |
| `src/test/java/` | 单元测试和集成测试源码目录 |
| `target/` | Maven 构建产物目录，不提交 Git |

### 15.3 Java 后端业务模块结构

`module/` 下按业务边界拆分。

```text
module/
├── auth/
├── admin/
├── server/
├── metrics/
├── alert/
├── dashboard/
└── sshsession/
```

业务模块作用：

| 模块 | 作用 |
|------|------|
| `auth/` | 注册、登录、退出、当前用户信息 |
| `admin/` | 管理员审核用户、通过、拒绝 |
| `server/` | 服务器新增、列表、详情、更新、删除、状态查询 |
| `metrics/` | 最新指标、历史指标、指标写入和查询 |
| `alert/` | 告警规则、告警记录、告警推送 |
| `dashboard/` | 仪表盘汇总数据 |
| `sshsession/` | SSH 会话记录查询和关闭 |

每个业务模块内部推荐结构：

```text
模块名/
├── controller/
├── service/
├── mapper/
├── entity/
├── dto/
└── vo/
```

业务模块内部目录作用：

| 目录 | 作用 |
|------|------|
| `controller/` | REST API 入口，只处理请求参数、鉴权上下文、响应封装 |
| `service/` | 业务逻辑，处理事务、规则校验、跨表协作 |
| `mapper/` | MyBatis-Plus Mapper 和自定义 SQL 查询 |
| `entity/` | 数据库表映射实体 |
| `dto/` | 请求参数对象，例如注册请求、新增服务器请求 |
| `vo/` | 响应对象，例如用户信息、服务器详情、指标结果 |

### 15.4 Java 后端配置项

真实密钥只放本机环境或本机配置文件，不提交到 Git。

应用配置：

| 配置项 | 示例 | 说明 |
|--------|------|------|
| `APP_ENV` | `local` | 当前运行环境 |
| `APP_NAME` | `susumonitor` | 应用名称 |
| `SERVER_PORT` | `18080` | 后端 HTTP 端口 |

数据库配置：

| 配置项 | 示例 | 说明 |
|--------|------|------|
| `DB_HOST` | `127.0.0.1` | MySQL 地址 |
| `DB_PORT` | `3306` | MySQL 端口 |
| `DB_NAME` | `susumonitor` | 数据库名 |
| `DB_USER` | `susumonitor` | 数据库用户 |
| `DB_PASSWORD` | `susumonitor_dev_2026` | 本机开发数据库密码 |
| `DB_CHARSET` | `utf8mb4` | 字符集 |
| `DB_TIMEZONE` | `Asia/Shanghai` | 数据库时区 |

JWT 配置：

| 配置项 | 示例 | 说明 |
|--------|------|------|
| `JWT_SECRET` | `本机开发密钥` | JWT 签名密钥，本机自行配置 |
| `JWT_EXPIRE_HOURS` | `24` | JWT 有效期，单位小时 |

SSH 加密配置：

| 配置项 | 示例 | 说明 |
|--------|------|------|
| `AES_GCM_KEY` | `本机开发密钥` | SSH 密码、私钥、私钥口令加密密钥 |
| `SSH_CONNECT_TIMEOUT_SECONDS` | `10` | SSH 连接超时 |
| `SSH_IDLE_TIMEOUT_MINUTES` | `20` | SSH 会话无输入超时 |

Agent 配置：

| 配置项 | 示例 | 说明 |
|--------|------|------|
| `AGENT_REGISTER_KEY` | `本机开发注册密钥` | Agent 首次注册校验密钥 |
| `AGENT_HEARTBEAT_TIMEOUT_SECONDS` | `90` | Agent 离线判定超时 |
| `AGENT_STATUS_SCAN_SECONDS` | `30` | 后端扫描 Agent 状态频率 |

调度配置：

| 配置项 | 示例 | 说明 |
|--------|------|------|
| `METRICS_RETENTION_DAYS` | `10` | 指标保留天数 |
| `METRICS_CLEANUP_CRON` | `0 0 3 * * ?` | 指标清理定时任务 |
| `ALERT_CHECK_SECONDS` | `10` | 告警检测频率 |

### 15.5 文档目录结构

`docs-SuMon/` 是项目正式文档目录。

```text
docs-SuMon/
├── Develop-plans/
├── Develop-log/
├── OpenApi-SuMon/
├── Protocol-SuMon/
└── 本机开发环境配置.md
```

文档目录和文件作用：

| 路径 | 作用 |
|------|------|
| `docs-SuMon/Develop-plans/` | 开发计划目录，记录阶段目标、任务清单、依赖关系 |
| `docs-SuMon/Develop-log/` | 开发日志目录，记录每次开发活动、变更摘要、涉及文件 |
| `docs-SuMon/OpenApi-SuMon/` | REST API OpenAPI 3.0 JSON 文档目录 |
| `docs-SuMon/Protocol-SuMon/` | WebSocket、Agent、SSH 终端协议文档目录 |
| `docs-SuMon/本机开发环境配置.md` | 当前本机开发环境、MySQL、后端启动和验证说明 |

当前 OpenAPI 文件：

| 文件 | 作用 |
|------|------|
| `docs-SuMon/OpenApi-SuMon/openapi.json` | 合并或总览 OpenAPI 文档 |
| `docs-SuMon/OpenApi-SuMon/openapi-system.json` | 健康检查、就绪检查接口文档 |
| `docs-SuMon/OpenApi-SuMon/openapi-auth.json` | 注册、登录、退出、当前用户、管理员审核接口文档 |

当前协议文件：

| 文件 | 作用 |
|------|------|
| `docs-SuMon/Protocol-SuMon/websocket-protocol.md` | Agent 注册、心跳、指标上报、告警推送、SSH 终端 WebSocket 协议 |

### 15.6 API 调试目录

`api-test/` 用于保存本机接口调试文件。

```text
api-test/
└── susumonitor.http
```

| 文件 | 作用 |
|------|------|
| `api-test/susumonitor.http` | HTTP Client 调试文件，包含 health、ready、register、login、me、logout、管理员审核、服务器、指标、告警等请求示例 |

调试变量：

| 变量 | 示例 | 说明 |
|------|------|------|
| `baseUrl` | `http://localhost:18080` | 本机后端地址 |
| `token` | `REPLACE_WITH_JWT_TOKEN` | 登录后替换为 JWT |
| `serverId` | `1` | 调试用服务器 ID |

### 15.7 脚本目录

`scripts/` 用于保存本机初始化和运维辅助脚本。

```text
scripts/
└── local-mysql-init.sql
```

| 文件 | 作用 |
|------|------|
| `scripts/local-mysql-init.sql` | 初始化本机 MySQL 数据库、开发账号、授权和基础命令记录表 |

当前 SQL 脚本内容：

| 内容 | 说明 |
|------|------|
| 创建数据库 `susumonitor` | 使用 `utf8mb4` 字符集和 `utf8mb4_unicode_ci` 排序规则 |
| 创建用户 `susumonitor` | 支持 `localhost` 和 `127.0.0.1` 登录 |
| 授权 | 授予 `susumonitor` 数据库全部权限 |
| 创建 `commands` 表 | 命令记录表，增强阶段命令历史使用 |

### 15.8 Web 前端目录

`web-vue-SuMon/` 是 Web 前端目录，MVP-5 阶段使用。

```text
web-vue-SuMon/
├── package.json
├── vite.config.ts
├── index.html
├── src/
│   ├── main.ts
│   ├── App.vue
│   ├── router/
│   ├── stores/
│   ├── api/
│   ├── views/
│   ├── components/
│   ├── layouts/
│   ├── utils/
│   └── styles/
└── dist/
```

前端目录和文件作用：

| 路径 | 作用 |
|------|------|
| `package.json` | 前端依赖和脚本配置 |
| `vite.config.ts` | Vite 配置、开发代理、构建配置 |
| `index.html` | 前端入口 HTML |
| `src/main.ts` | Vue 应用入口 |
| `src/App.vue` | 根组件 |
| `src/router/` | 路由配置 |
| `src/stores/` | Pinia 状态管理 |
| `src/api/` | REST API 和 WebSocket 客户端封装 |
| `src/views/` | 页面组件，例如登录、仪表盘、服务器、告警、终端 |
| `src/components/` | 通用组件 |
| `src/layouts/` | 页面布局 |
| `src/utils/` | 工具函数 |
| `src/styles/` | 全局样式 |
| `dist/` | 构建产物，不提交 Git |

### 15.9 Android 目录

`app-kt-SuMon/` 是 Android App 目录，增强阶段使用。

```text
app-kt-SuMon/
├── settings.gradle.kts
├── build.gradle.kts
├── app/
│   ├── build.gradle.kts
│   └── src/
│       ├── main/
│       │   ├── AndroidManifest.xml
│       │   └── java/
│       └── test/
└── gradle/
```

Android 目录和文件作用：

| 路径 | 作用 |
|------|------|
| `settings.gradle.kts` | Gradle 项目模块配置 |
| `build.gradle.kts` | 根构建配置 |
| `app/build.gradle.kts` | Android App 模块依赖和构建配置 |
| `AndroidManifest.xml` | App 权限、Activity、Service 声明 |
| `app/src/main/java/` | Android 业务源码 |
| `app/src/test/` | 单元测试 |
| `gradle/` | Gradle Wrapper 文件 |

### 15.10 根配置文件细节

`.editorconfig` 规则：

| 规则 | 当前值 | 说明 |
|------|--------|------|
| `charset` | `utf-8` | 统一 UTF-8 |
| `end_of_line` | `lf` | 默认 LF 换行 |
| `insert_final_newline` | `true` | 文件末尾保留换行 |
| `trim_trailing_whitespace` | `true` | 默认删除行尾空格 |
| 默认缩进 | 2 空格 | 大多数文件使用 2 空格 |
| Markdown | 不删除行尾空格 | 兼容 Markdown 换行 |
| JSON/YAML | 2 空格 | 配置文件缩进 |
| Kotlin | 4 空格 | Android 代码缩进 |
| SQL | 4 空格 | SQL 文件缩进 |

`.gitignore` 应忽略：

| 类别 | 忽略内容 |
|------|---------|
| 系统文件 | `.DS_Store`、`Thumbs.db`、`desktop.ini` |
| IDE | `.idea/`、`.vscode/`、临时交换文件 |
| 环境和密钥 | `.env`、`.env.*`、证书、私钥、`secrets/` |
| 日志 | `*.log`、`logs/` |
| Java 构建产物 | `target/` |
| 前端依赖和产物 | `node_modules/`、`dist/`、`.vite/` |
| Android 产物 | `.gradle/`、`local.properties`、`app/build/`、`captures/` |
| Docker/k8s 本地产物 | `*.tar`、`*.tgz`、`tmp/` |

`.dockerignore` 应排除：

| 类别 | 作用 |
|------|------|
| Git 和编辑器配置 | 减少 Docker 构建上下文无关文件 |
| 环境和密钥 | 防止 `.env`、私钥、证书进入镜像上下文 |
| 依赖和构建产物 | 排除 `node_modules/`、`dist/`、`target/` 等 |
| IDE 和系统文件 | 排除 `.idea/`、`.vscode/`、系统临时文件 |
| 日志和临时目录 | 排除 `*.log`、`logs/`、`tmp/` |

`.gitattributes` 规则：

| 规则 | 说明 |
|------|------|
| `* text=auto eol=lf` | 默认文本文件使用 LF |
| `*.bat text eol=crlf` | Windows bat 脚本使用 CRLF |
| `*.cmd text eol=crlf` | Windows cmd 脚本使用 CRLF |
| `*.ps1 text eol=crlf` | PowerShell 脚本使用 CRLF |

### 15.11 不纳入 Java 改造后目标结构的内容

以下内容不写入正式项目结构，不作为后续开发依赖：

| 内容 | 处理方式 |
|------|---------|
| 旧后端目录 | Java 后端创建完成后清理或归档 |
| 旧后端 lint 配置 | Java 后端不使用，后续清理 |
| 本地缓存目录 | 不提交，不作为项目结构 |
| 本地日志文件 | 不提交，不作为项目结构 |
| 本机构建产物 | 不提交，不作为项目结构 |
| 工具依赖目录 | 不提交，不作为项目结构 |

## 十六、MySQL 与 SQL 规范

- 当前开发使用本机 MySQL 8.4。
- 表结构需要有清晰的初始化和变更管理方式。
- SQL 查询禁止使用 `SELECT *`，必须指定字段。
- 需要结合查询场景设计索引。
- 指标清理依赖 `idx_metrics_collected_at`。
- 历史指标查询依赖 `idx_metrics_server_time`。
- 用户名需要唯一索引。
- 服务器 host 需要唯一索引。
- Agent ID 需要唯一索引。

## 十七、文档规范

文档根目录为 `docs-SuMon/`。

```text
docs-SuMon/
├── Develop-plans/
├── Develop-log/
├── OpenApi-SuMon/
└── Protocol-SuMon/
```

| 文档类型 | 路径 | 规则 |
|---------|------|------|
| 开发计划 | `docs-SuMon/Develop-plans/` | 文件名格式 `YYYYMMDD-计划标题.md` |
| 开发日志 | `docs-SuMon/Develop-log/` | 文件名格式 `YYYYMMDD-日志标题.md` |
| OpenAPI | `docs-SuMon/OpenApi-SuMon/` | OpenAPI 3.0，JSON 格式 |
| 协议文档 | `docs-SuMon/Protocol-SuMon/` | WebSocket、Agent 通信、SSH 终端协议 |

文档维护要求：

- 新增或修改 API 时，同步更新 OpenAPI 文档。
- 新增或修改 WebSocket/Agent 协议时，同步更新协议文档。
- 开发活动完成后记录开发日志。
- 文档中的服务器地址示例统一使用 `SERVER_IP_OR_DOMAIN`，不使用真实 IP 或内网 IP。

## 十八、验证与启动说明

### 18.1 后端启动

后端改为 Java 新实现后，启动方式以后端实际工程为准。

### 18.2 健康检查

```bash
curl http://localhost:18080/api/health
curl http://localhost:18080/api/ready
```

`/api/ready` 返回 `database=ok` 表示后端已连接本机 MySQL。

### 18.3 后端验证

后端开发完成后应至少验证健康检查、就绪检查、认证流程、管理员审核流程和数据库连通性。

## 十九、后续增强方向

以下内容属于后续增强方向，不作为当前主机开发阶段的前置要求：

- Redis：指标缓存、JWT 黑名单、API 限流、Agent 在线状态缓存。
- 远程命令增强：Agent 命令下发、实时输出回传、命令历史。
- Android App：Kotlin、Jetpack Compose、Retrofit、OkHttp、前台 Service。
- Agent 一键安装脚本：安装、卸载、升级、重装、systemd 管理。
- k8s + minikube：Namespace、Deployment、Service、Ingress、ConfigMap、Secret、PVC。
- Helm Chart：后端、前端、MySQL、Redis 打包和 values 配置。
- Prometheus + Grafana：后端 `/metrics`、k8s 集群监控、Grafana 仪表盘。
- GitHub Actions：自动测试、构建镜像、推送镜像。
- 审计与数据优化：SSH 审计日志、命令审计、指标聚合、历史数据归档。

## 二十、信息来源

本文档依据以下项目内资料和用户补充整理：

- `docs-SuMon/Develop-plans/20260630-SuSuMonitor开发计划.md`
- `docs-SuMon/Develop-plans/20260701-MVP详细设计草案.md`
- `docs-SuMon/Develop-plans/20260705-MVP1后端基础开发计划.md`
- `docs-SuMon/Protocol-SuMon/websocket-protocol.md`
- `docs-SuMon/OpenApi-SuMon/openapi-system.json`
- `docs-SuMon/OpenApi-SuMon/openapi-auth.json`
- `docs-SuMon/本机开发环境配置.md`
- 用户补充：完整项目需求都要保留；当前仅在本机开发和调试，当前调试主机只有当前主机。
